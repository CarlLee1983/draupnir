[test]
# Phase 20 guardrail: Bun 1.3.10 enforces coverage as an aggregate threshold across files.
coverage = true
coverageThreshold = 0.8
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"
coverageSkipTestFiles = true
