/**
 * 型別宣告：擴充 @gravito/atlas 的匯出型別。
 *
 * @gravito/atlas dist/index.d.ts 已提供大多數型別，
 * 此檔僅作為需要額外補充或覆蓋時的 fallback。
 *
 * 注意：若此檔使用 `declare module` 會覆蓋 dist 的型別，
 * 因此改為空檔，讓 TypeScript 直接讀取套件的 dist/index.d.ts。
 */
export {}
