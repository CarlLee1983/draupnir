---
phase: quick-260413-wo3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # P1 — Domain cleanup
  - src/Modules/Organization/Domain/Aggregates/Organization.ts
  - src/Modules/Organization/Domain/Entities/OrganizationMember.ts
  - src/Modules/Organization/Domain/Entities/OrganizationInvitation.ts
  # P1 — Infrastructure Mappers (receive fromDatabase logic)
  - src/Modules/Organization/Infrastructure/Mappers/OrganizationMapper.ts
  - src/Modules/Organization/Infrastructure/Mappers/OrganizationMemberMapper.ts
  - src/Modules/Organization/Infrastructure/Mappers/OrganizationInvitationMapper.ts
  # P1 — Repository: remove markAsAccepted/cancel, callers updated
  - src/Modules/Organization/Domain/Repositories/IOrganizationInvitationRepository.ts
  - src/Modules/Organization/Infrastructure/Repositories/OrganizationInvitationRepository.ts
  - src/Modules/Organization/Application/Services/AcceptInvitationService.ts
  - src/Modules/Organization/Application/Services/CancelInvitationService.ts
  # P1 — DTO split
  - src/Modules/Organization/Application/DTOs/OrganizationDTO.ts
  - src/Modules/Organization/Application/DTOs/OrganizationRequestDTO.ts
  - src/Modules/Organization/Application/DTOs/OrganizationResponseDTO.ts
  - src/Modules/Organization/Application/DTOs/OrganizationPresenterDTO.ts
  # P1 — VO typing on Entities
  - src/Modules/Organization/Infrastructure/Repositories/OrganizationRepository.ts
  - src/Modules/Organization/Infrastructure/Repositories/OrganizationMemberRepository.ts
  # P2 — Domain Service extraction + authorization
  - src/Modules/Organization/Domain/Services/OrgInvitationRules.ts
  - src/Modules/Organization/Application/Services/UpdateOrganizationService.ts
  - src/Modules/Organization/Application/Services/ChangeOrgStatusService.ts
  # P3 — Tests
  - src/Modules/Organization/__tests__/OrgMemberRole.test.ts
  - src/Modules/Organization/__tests__/InvitationStatus.test.ts
  - src/Modules/Organization/__tests__/OrgStatus.test.ts
  - src/Modules/Organization/__tests__/OrgMembershipRules.test.ts
  - src/Modules/Organization/__tests__/ChangeOrgMemberRoleService.test.ts
  - src/Modules/Organization/__tests__/ChangeOrgStatusService.test.ts
  - src/Modules/Organization/__tests__/UpdateOrganizationService.test.ts
  - src/Modules/Organization/__tests__/CancelInvitationService.test.ts
autonomous: true
must_haves:
  truths:
    - "Domain Entities (Organization, OrganizationMember, OrganizationInvitation) have no fromDatabase() method"
    - "OrganizationInvitation.role 和 .status getter 回傳 OrgMemberRole 和 InvitationStatus VO 而非 string"
    - "IOrganizationInvitationRepository 不含 markAsAccepted()/cancel() 方法"
    - "AcceptInvitationService 和 CancelInvitationService 呼叫 invitation.markAsAccepted()/cancel() 再 repo.save()"
    - "Application/DTOs/ 按 Request/Response/Presenter 拆分為獨立檔案"
    - "AcceptInvitationService email/duplicate 驗證邏輯移至 OrgInvitationRules Domain Service"
    - "UpdateOrganizationService 和 ChangeOrgStatusService 加入 callerUserId/callerSystemRole 授權檢查"
    - "所有 Value Object 與 Domain Service 均有對應測試檔案"
    - "新增的 4 個 Application Service 測試覆蓋主要 happy path 與 error case"
  artifacts:
    - path: "src/Modules/Organization/Domain/Aggregates/Organization.ts"
      provides: "Organization Aggregate without fromDatabase()"
    - path: "src/Modules/Organization/Infrastructure/Mappers/OrganizationMapper.ts"
      provides: "row → Organization.reconstitute() mapping logic"
    - path: "src/Modules/Organization/Domain/Services/OrgInvitationRules.ts"
      provides: "validateEmailMatch() and validateNotAlreadyMember() domain rules"
    - path: "src/Modules/Organization/Application/DTOs/OrganizationRequestDTO.ts"
      provides: "Request interfaces: CreateOrganizationRequest, UpdateOrganizationRequest, InviteMemberRequest, AcceptInvitationRequest"
    - path: "src/Modules/Organization/Application/DTOs/OrganizationResponseDTO.ts"
      provides: "Response interfaces: OrganizationResponse, ListOrganizationsResponse"
    - path: "src/Modules/Organization/Application/DTOs/OrganizationPresenterDTO.ts"
      provides: "Presenter classes: OrganizationPresenter, OrganizationMemberPresenter, OrganizationInvitationPresenter"
  key_links:
    - from: "OrganizationInvitationMapper.ts"
      to: "OrganizationInvitation.reconstitute()"
      via: "mapRowToEntity() private method"
    - from: "AcceptInvitationService.ts"
      to: "invitation.markAsAccepted()"
      via: "call before repo.save()"
    - from: "CancelInvitationService.ts"
      to: "invitation.cancel()"
      via: "call before repo.save()"
---

<objective>
修正 Organization 模組所有 9 項 DDD 架構問題，涵蓋 Priority 1（架構邊界）、Priority 2（Domain 完整性）、Priority 3（測試覆蓋）。

Purpose: 讓 Organization 模組符合本專案 DDD 四層架構規範，消除 Domain 知道 DB Schema、Repository 含業務邏輯、VO 型別不一致等架構缺陷。
Output: 乾淨的 Domain 層、正確的 Infrastructure Mapping、拆分的 DTOs、Domain Service 驗證邏輯、授權檢查、完整測試套件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@skills/ddd-module/SKILL.md
@skills/ddd-module/references/domain.md
@skills/ddd-module/references/infrastructure.md
@skills/ddd-module/references/application.md

<interfaces>
<!-- 現有 Value Objects（已存在，直接使用） -->

From src/Modules/Organization/Domain/ValueObjects/OrgMemberRole.ts:
```typescript
export enum OrgMemberRoleType { MANAGER = 'manager', MEMBER = 'member' }
export class OrgMemberRole {
  constructor(role: string)         // throws on invalid
  getValue(): OrgMemberRoleType
  isManager(): boolean
  equals(other: OrgMemberRole): boolean
  toString(): string
}
```

From src/Modules/Organization/Domain/ValueObjects/InvitationStatus.ts:
```typescript
export enum InvitationStatusType { PENDING = 'pending', ACCEPTED = 'accepted', EXPIRED = 'expired', CANCELLED = 'cancelled' }
export class InvitationStatus {
  constructor(status: string)       // throws on invalid
  getValue(): InvitationStatusType
  isPending(): boolean
  isAccepted(): boolean
  toString(): string
}
```

From src/Modules/Organization/Domain/ValueObjects/OrgStatus.ts:
```typescript
export class OrgStatus {
  static from(value: string): OrgStatus
  static active(): OrgStatus
  static suspended(): OrgStatus
  isActive(): boolean
  isSuspended(): boolean
}
```

From src/Modules/Organization/Application/Services/OrgAuthorizationHelper.ts:
```typescript
export class OrgAuthorizationHelper {
  requireOrgMembership(orgId, callerUserId, callerSystemRole): Promise<OrgAuthResult>
  requireOrgManager(orgId, callerUserId, callerSystemRole): Promise<OrgAuthResult>
}
```

From src/Modules/Organization/Domain/Entities/OrganizationInvitation.ts (現有方法，保留):
```typescript
markAsAccepted(): OrganizationInvitation  // 回傳新實例，status='accepted'
cancel(): OrganizationInvitation          // 回傳新實例，status='cancelled'
isPending(): boolean
isExpired(): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: P1 — 移除 Domain fromDatabase()，搬至 Infrastructure Mapper；Entity 換用 VO 型別</name>
  <files>
    src/Modules/Organization/Domain/Aggregates/Organization.ts,
    src/Modules/Organization/Domain/Entities/OrganizationMember.ts,
    src/Modules/Organization/Domain/Entities/OrganizationInvitation.ts,
    src/Modules/Organization/Infrastructure/Mappers/OrganizationMapper.ts,
    src/Modules/Organization/Infrastructure/Mappers/OrganizationMemberMapper.ts,
    src/Modules/Organization/Infrastructure/Mappers/OrganizationInvitationMapper.ts,
    src/Modules/Organization/Infrastructure/Repositories/OrganizationRepository.ts,
    src/Modules/Organization/Infrastructure/Repositories/OrganizationMemberRepository.ts,
    src/Modules/Organization/Infrastructure/Repositories/OrganizationInvitationRepository.ts
  </files>
  <action>
**步驟 1：Organization.ts**
- 刪除 `static fromDatabase()` 方法（第 56-66 行）
- 新增 `static reconstitute(props: OrganizationProps): Organization`，直接傳入 props 物件
- OrganizationProps 的 `status` 保持 `'active' | 'suspended'`（純 string union，不改用 OrgStatus VO，因為 suspend()/activate() 已處理業務語意）
- 所有 getter 維持不變

**步驟 2：OrganizationMember.ts**
- 刪除 `static fromDatabase()` 方法（第 45-54 行）
- 新增 `static reconstitute(props: OrganizationMemberProps): OrganizationMember`
- 將 props interface 的 `role: string` 改為 `role: OrgMemberRole`（import OrgMemberRole）
- 更新 `create()` 接受 `role: OrgMemberRole`（不再是 string）
- 更新 `changeRole(newRole: OrgMemberRole): OrganizationMember`（不再是 string）
- getter `get role(): OrgMemberRole`（回傳 VO）
- `isManager()` 改為 `return this.props.role.isManager()`

**步驟 3：OrganizationInvitation.ts**
- 刪除 `static fromDatabase()` 方法（第 57-70 行）
- 新增 `static reconstitute(props: OrganizationInvitationProps): OrganizationInvitation`
- 將 props interface 的 `role: string` 改為 `role: OrgMemberRole`，`status: string` 改為 `status: InvitationStatus`（import 兩個 VO）
- `create()` factory 內部 `status` 改用 `new InvitationStatus('pending')`，`role` 接受 `OrgMemberRole`（呼叫方傳入 VO）
- `markAsAccepted()` 改為 `new OrganizationInvitation({ ...this.props, status: new InvitationStatus('accepted') })`
- `cancel()` 改為 `new OrganizationInvitation({ ...this.props, status: new InvitationStatus('cancelled') })`
- `isPending()` 改為 `return this.props.status.isPending() && !this.isExpired()`
- getter `get role(): OrgMemberRole`、`get status(): InvitationStatus`

**步驟 4：OrganizationMapper.ts** — 新增 `static toEntity(row)` 方法
```typescript
static toEntity(row: Record<string, unknown>): Organization {
  return Organization.reconstitute({
    id: String(row.id),
    name: String(row.name),
    slug: String(row.slug),
    description: String(row.description ?? ''),
    status: (row.status as 'active' | 'suspended'),
    createdAt: toDate(row.created_at),
    updatedAt: toDate(row.updated_at),
  })
}
// 私有 toDate helper（同 infrastructure.md 範例）
```
保留現有 `toDatabaseRow()`。

**步驟 5：OrganizationMemberMapper.ts** — 新增 `static toEntity(row)` 方法
```typescript
static toEntity(row: Record<string, unknown>): OrganizationMember {
  return OrganizationMember.reconstitute({
    id: String(row.id),
    organizationId: String(row.organization_id),
    userId: String(row.user_id),
    role: new OrgMemberRole(String(row.role)),
    joinedAt: toDate(row.joined_at),
    createdAt: toDate(row.created_at),
  })
}
```
更新 `toDatabaseRow()` 改為 `role: entity.role.getValue()`（因 getter 現在回傳 VO）。

**步驟 6：OrganizationInvitationMapper.ts** — 新增 `static toEntity(row)` 方法
```typescript
static toEntity(row: Record<string, unknown>): OrganizationInvitation {
  return OrganizationInvitation.reconstitute({
    id: String(row.id),
    organizationId: String(row.organization_id),
    email: String(row.email),
    token: '',
    tokenHash: String(row.token_hash),
    role: new OrgMemberRole(String(row.role)),
    invitedByUserId: String(row.invited_by_user_id),
    status: new InvitationStatus(String(row.status)),
    expiresAt: toDate(row.expires_at),
    createdAt: toDate(row.created_at),
  })
}
```
更新 `toDatabaseRow()` 改為 `role: entity.role.getValue(), status: entity.status.getValue()`。

**步驟 7：Repository 更新** — 所有 `XxxRepository.ts` 將 `Xxx.fromDatabase(row)` 替換為 `XxxMapper.toEntity(row)`。

**步驟 8：P1 Repository 業務邏輯** — `OrganizationInvitationRepository.ts`
- 刪除 `markAsAccepted(invitationId: string)` 和 `cancel(invitationId: string)` 方法（第 37-49 行）
- 新增 `update(invitation: OrganizationInvitation): Promise<void>`，以 Mapper 映射後 UPDATE

更新 `IOrganizationInvitationRepository.ts`：
- 移除 `markAsAccepted(invitationId: string): Promise<void>`
- 移除 `cancel(invitationId: string): Promise<void>`
- 新增 `update(invitation: OrganizationInvitation): Promise<void>`

更新 **AcceptInvitationService.ts** 的 transaction 塊：
```typescript
// 替換 txInvitationRepo.markAsAccepted(invitation.id)
const accepted = invitation.markAsAccepted()
await txInvitationRepo.update(accepted)
```

更新 **CancelInvitationService.ts**：
```typescript
// 替換 this.invitationRepository.cancel(invitationId)
const cancelled = invitation.cancel()
await this.invitationRepository.update(cancelled)
```
注意：CancelInvitationService 需要先 `findById` 取得 invitation 物件再呼叫 `.cancel()`。

**步驟 9：OrganizationInvitation.create() 呼叫方更新**
InviteMemberService 目前傳入 `role: string`（從 HTTP 請求）。更新為在 Service 內建立 `new OrgMemberRole(request.role ?? 'member')` 後傳入 `create()`。同樣地，AcceptInvitationService 建立 OrganizationMember 時改用 `invitation.role`（已是 OrgMemberRole VO）傳給 `OrganizationMember.create()`。ChangeOrgMemberRoleService 的 `member.changeRole()` 改為傳 `new OrgMemberRole(newRole)`。
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit 2>&1 | head -60</automated>
  </verify>
  <done>
    - Organization、OrganizationMember、OrganizationInvitation 無 fromDatabase() 方法
    - OrganizationMember.role getter 回傳 OrgMemberRole；OrganizationInvitation.role/status getter 回傳對應 VO
    - 三個 Mapper 各有 toEntity() 方法
    - IOrganizationInvitationRepository 無 markAsAccepted/cancel，改為 update()
    - AcceptInvitationService 和 CancelInvitationService 使用 Domain 方法後 repo.update()
    - TypeScript 編譯通過（零 error）
  </done>
</task>

<task type="auto">
  <name>Task 2: P1 — 拆分 DTOs；P2 — 提取 Domain Service 驗證邏輯 + 補授權檢查</name>
  <files>
    src/Modules/Organization/Application/DTOs/OrganizationDTO.ts,
    src/Modules/Organization/Application/DTOs/OrganizationRequestDTO.ts,
    src/Modules/Organization/Application/DTOs/OrganizationResponseDTO.ts,
    src/Modules/Organization/Application/DTOs/OrganizationPresenterDTO.ts,
    src/Modules/Organization/Domain/Services/OrgInvitationRules.ts,
    src/Modules/Organization/Application/Services/AcceptInvitationService.ts,
    src/Modules/Organization/Application/Services/UpdateOrganizationService.ts,
    src/Modules/Organization/Application/Services/ChangeOrgStatusService.ts
  </files>
  <action>
**步驟 1：拆分 DTOs（P1-3）**

建立 `OrganizationRequestDTO.ts`，包含：
```typescript
export interface CreateOrganizationRequest { name: string; description?: string; slug?: string; managerUserId: string }
export interface UpdateOrganizationRequest { name?: string; description?: string }
export interface InviteMemberRequest { email: string; role?: string }
export interface AcceptInvitationRequest { token: string }
export interface ChangeMemberRoleRequest { newRole: string }
export interface ChangeOrgStatusRequest { status: string }
```

建立 `OrganizationResponseDTO.ts`，包含：
```typescript
export interface OrganizationResponse { success: boolean; message: string; data?: Record<string, unknown>; error?: string }
export interface ListOrganizationsResponse { success: boolean; message: string; data?: { organizations: Record<string, unknown>[]; meta: { total: number; page: number; limit: number; totalPages: number } }; error?: string }
```

建立 `OrganizationPresenterDTO.ts`，包含現有三個 Presenter class（OrganizationPresenter、OrganizationMemberPresenter、OrganizationInvitationPresenter）。

更新 `OrganizationDTO.ts`：將所有 export 改為 re-export（barrel 檔）：
```typescript
export * from './OrganizationRequestDTO'
export * from './OrganizationResponseDTO'
export * from './OrganizationPresenterDTO'
```
這樣既不破壞現有 import，又讓各類型有獨立檔案。

**步驟 2：提取 AcceptInvitationService 驗證邏輯至 Domain Service（P2-5）**

新增 `src/Modules/Organization/Domain/Services/OrgInvitationRules.ts`：
```typescript
import type { OrganizationInvitation } from '../Entities/OrganizationInvitation'
import type { OrganizationMember } from '../Entities/OrganizationMember'

export class OrgInvitationRules {
  /**
   * 驗證邀請的 email 與使用者 email 一致。
   * @throws Error 'EMAIL_MISMATCH' 如不一致
   */
  static assertEmailMatches(invitation: OrganizationInvitation, userEmail: string): void {
    if (userEmail.toLowerCase() !== invitation.email.toLowerCase()) {
      throw new Error('EMAIL_MISMATCH')
    }
  }

  /**
   * 驗證使用者尚未是組織成員。
   * @throws Error 'USER_ALREADY_IN_ORG' 如已是成員
   */
  static assertNotAlreadyMember(existingMembership: OrganizationMember | null): void {
    if (existingMembership !== null) {
      throw new Error('USER_ALREADY_IN_ORG')
    }
  }
}
```

更新 **AcceptInvitationService.ts**：
- 刪除第 51-57 行（email 比對邏輯）和第 59-68 行（重複成員邏輯）的 inline 業務規則
- 改用 `OrgInvitationRules.assertEmailMatches(invitation, user.emailValue)` 和 `OrgInvitationRules.assertNotAlreadyMember(existingMembership)`
- 將兩個 domain rule 呼叫包在 try/catch 中，根據 error.message 回傳對應的 `{ success: false, error: 'EMAIL_MISMATCH' }` 或 `{ success: false, error: 'USER_ALREADY_IN_ORG' }`

**步驟 3：補授權檢查（P2-6）**

更新 **UpdateOrganizationService.ts**：
- constructor 新增 `private orgAuth: OrgAuthorizationHelper` 參數（inject OrgAuthorizationHelper）
- `execute()` 簽名新增 `callerUserId: string, callerSystemRole: string` 參數
- 在 `findById` 之前加入：
  ```typescript
  const authResult = await this.orgAuth.requireOrgManager(orgId, callerUserId, callerSystemRole)
  if (!authResult.authorized) {
    return { success: false, message: 'Insufficient permissions', error: authResult.error }
  }
  ```

更新 **ChangeOrgStatusService.ts**：
- constructor 新增 `private orgAuth: OrgAuthorizationHelper` 參數
- `execute()` 簽名新增 `callerUserId: string, callerSystemRole: string` 參數
- 在 `findById` 之前加入同樣的 `requireOrgManager` 授權檢查

更新 **OrganizationServiceProvider.ts**（如有）：確認 UpdateOrganizationService 和 ChangeOrgStatusService 注入時傳入 orgAuth 依賴。
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit 2>&1 | head -60</automated>
  </verify>
  <done>
    - OrganizationDTO.ts 成為 barrel re-export，三個獨立 DTO 檔案存在
    - OrgInvitationRules.ts 存在，含 assertEmailMatches() 和 assertNotAlreadyMember()
    - AcceptInvitationService 使用 OrgInvitationRules 而非 inline 邏輯
    - UpdateOrganizationService 和 ChangeOrgStatusService 含授權檢查
    - TypeScript 編譯通過（零 error）
  </done>
</task>

<task type="auto">
  <name>Task 3: P3 — 補 Value Object / Domain Service / Application Service 測試</name>
  <files>
    src/Modules/Organization/__tests__/OrgMemberRole.test.ts,
    src/Modules/Organization/__tests__/InvitationStatus.test.ts,
    src/Modules/Organization/__tests__/OrgStatus.test.ts,
    src/Modules/Organization/__tests__/OrgMembershipRules.test.ts,
    src/Modules/Organization/__tests__/ChangeOrgMemberRoleService.test.ts,
    src/Modules/Organization/__tests__/ChangeOrgStatusService.test.ts,
    src/Modules/Organization/__tests__/UpdateOrganizationService.test.ts,
    src/Modules/Organization/__tests__/CancelInvitationService.test.ts
  </files>
  <action>
參考現有 `__tests__/OrgSlug.test.ts`、`Organization.test.ts` 的測試風格（mock pattern、describe/it 結構）。

**OrgMemberRole.test.ts**
```typescript
describe('OrgMemberRole', () => {
  it('accepts valid role "manager"')
  it('accepts valid role "member"')
  it('throws on invalid role')
  it('isManager() returns true for manager')
  it('isManager() returns false for member')
  it('equals() compares by value')
  it('toString() returns string value')
})
```

**InvitationStatus.test.ts**
```typescript
describe('InvitationStatus', () => {
  it('accepts all valid statuses: pending, accepted, expired, cancelled')
  it('throws on invalid status')
  it('isPending() returns true for pending')
  it('isAccepted() returns true for accepted')
  it('equals() compares by value')
})
```

**OrgStatus.test.ts**
```typescript
describe('OrgStatus', () => {
  it('OrgStatus.from() accepts "active" and "suspended"')
  it('OrgStatus.from() throws on invalid status')
  it('OrgStatus.active() creates active status')
  it('OrgStatus.suspended() creates suspended status')
  it('isActive() / isSuspended() work correctly')
  it('equals() compares by value')
})
```

**OrgMembershipRules.test.ts**
```typescript
describe('OrgMembershipRules.assertNotLastManager', () => {
  it('throws when demoting last manager')
  it('does not throw when there are multiple managers')
  it('does not throw when member is not a manager')
})
```
建立最小 OrganizationMember mock：直接用 `OrganizationMember.reconstitute()` 建立測試 fixture（role 傳 `new OrgMemberRole('manager')` 或 `'member'`）。

**ChangeOrgMemberRoleService.test.ts**
Mock `IOrganizationMemberRepository`、`IDatabaseAccess`（含 transaction mock）。
```typescript
describe('ChangeOrgMemberRoleService', () => {
  it('changes role successfully (member → manager)')
  it('returns MEMBER_NOT_FOUND when member does not exist')
  it('returns error when demoting last manager')
  it('returns error on invalid role string')
})
```

**ChangeOrgStatusService.test.ts**
Mock `IOrganizationRepository`、`OrgAuthorizationHelper`。
```typescript
describe('ChangeOrgStatusService', () => {
  it('suspends organization successfully')
  it('activates organization successfully')
  it('returns INVALID_STATUS on bad status value')
  it('returns ORG_NOT_FOUND when org not found')
  it('returns Insufficient permissions when not authorized')
})
```

**UpdateOrganizationService.test.ts**
Mock `IOrganizationRepository`、`OrgAuthorizationHelper`。
```typescript
describe('UpdateOrganizationService', () => {
  it('updates name and description')
  it('returns ORG_NOT_FOUND when org not found')
  it('returns Insufficient permissions when not authorized')
})
```

**CancelInvitationService.test.ts**
Mock `IOrganizationInvitationRepository`、`OrgAuthorizationHelper`。
```typescript
describe('CancelInvitationService', () => {
  it('cancels a pending invitation successfully')
  it('returns INVITATION_NOT_FOUND when invitation does not exist')
  it('returns INVITATION_NOT_FOUND when invitation belongs to different org')
  it('returns Insufficient permissions when not authorized')
})
```

注意：CancelInvitationService 呼叫 `invitation.cancel()` 再 `repo.update()` ——測試需驗證 update() 被呼叫且傳入 cancelled invitation（可用 vi.spyOn 或 mock.calls 驗證）。
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && npx vitest run src/Modules/Organization/__tests__ 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 8 個新測試檔案存在
    - 所有新測試通過（pass）
    - 舊測試不被破壞（全部 Organization __tests__ 綠燈）
  </done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript 編譯
cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit

# 2. Organization 模組所有測試
cd /Users/carl/Dev/CMG/Draupnir && npx vitest run src/Modules/Organization/__tests__

# 3. 確認 fromDatabase 已完全移除
grep -rn "fromDatabase" /Users/carl/Dev/CMG/Draupnir/src/Modules/Organization/Domain/
# 預期：零結果

# 4. 確認 markAsAccepted/cancel 從 Repository Interface 移除
grep -n "markAsAccepted\|cancel" /Users/carl/Dev/CMG/Draupnir/src/Modules/Organization/Domain/Repositories/IOrganizationInvitationRepository.ts
# 預期：零結果
```
</verification>

<success_criteria>
- `npx tsc --noEmit` 零 error
- `npx vitest run src/Modules/Organization/__tests__` 全部 pass（含新增 8 個測試檔案）
- Domain Entities 無任何 DB 知識（無 `fromDatabase`、無 `import` Infrastructure）
- `IOrganizationInvitationRepository` 只有 `save()` / `update()` / `findXxx()` / `deleteExpired()` / `withTransaction()`
- `AcceptInvitationService` 和 `CancelInvitationService` 呼叫 Domain method 後 `repo.update()`
- `UpdateOrganizationService` 和 `ChangeOrgStatusService` 在操作前執行授權檢查
- `Application/DTOs/` 有三個獨立 DTO 檔案 + 一個 barrel re-export
</success_criteria>

<output>
完成後建立 `.planning/quick/260413-wo3-organization-ddd-9-priority/260413-wo3-SUMMARY.md`，記錄：
- 每個 Priority 修正的實際做法
- 任何與計畫偏差的決策
- 新建測試的覆蓋重點
</output>
