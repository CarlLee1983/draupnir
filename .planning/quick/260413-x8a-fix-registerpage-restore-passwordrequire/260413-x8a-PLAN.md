# Quick Task 260413-x8a: Fix RegisterPage Bugs

**Created:** 2026-04-13
**Description:** Fix RegisterPage: restore passwordRequirements on error rerenders and fix flash persistence across redirects

## Tasks

### Task 1: Restore passwordRequirements on all RegisterPage render paths

**Files:** `src/Pages/Auth/RegisterPage.ts`

**Action:**
Extract `passwordRequirements` as a module-level constant and include it in all three render calls: GET handle(), POST validation-failure branch, POST service-error branch.

**Verify:** All render calls pass `passwordRequirements` to `Auth/Register`.

**Done:** RegisterPage renders password policy hints on every path.

---

### Task 2: Fix flash persistence across redirects

**Files:** `src/Pages/SharedDataMiddleware.ts`, `src/Pages/Auth/RegisterPage.ts`

**Action:**
1. In SharedDataMiddleware.injectSharedData(), add cookie-based flash fallback: read flash:success / flash:error from cookies when not in context, and queue a clearing cookie after reading (maxAge: 0).
2. In RegisterPage.store(), replace ctx.set('flash:success', ...) with ctx.setCookie('flash:success', ...) so the flash survives the POST redirect GET cycle.

**Verify:** After successful registration, flash cookie set on redirect response, cleared on next page.

**Done:** Flash message visible on login page after registration redirect.
