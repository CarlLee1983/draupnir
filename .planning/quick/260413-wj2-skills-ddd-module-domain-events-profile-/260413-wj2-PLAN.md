---
phase: quick-260413-wj2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/Profile/Domain/Events/UserProfileCreated.ts
  - src/Modules/Profile/Domain/Events/UserProfileUpdated.ts
  - src/Modules/Profile/Domain/Aggregates/UserProfile.ts
  - src/Modules/Profile/Application/DTOs/UserProfileDTO.ts
  - src/Modules/Profile/Application/Services/UpdateProfileService.ts
  - src/Modules/Profile/Application/Services/GetProfileService.ts
  - src/Modules/Profile/Application/EventHandlers/UserRegisteredHandler.ts
  - src/Modules/Profile/index.ts
  - skills/ddd-module/references/domain.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Profile Domain Events 各自獨立成檔，位於 Domain/Events/"
    - "Profile Domain Events 繼承 Shared DomainEvent abstract class"
    - "Application Services 在 use case 末尾派發 domain events 並 clear"
    - "Application Services 不再 import Infrastructure Mapper"
    - "reconstitute() 不再有冗餘 null 條件判斷"
    - "skill domain.md 補充 Domain Events 目錄結構與派發模式說明"
  artifacts:
    - path: "src/Modules/Profile/Domain/Events/UserProfileCreated.ts"
      provides: "UserProfileCreated event class（繼承 Shared DomainEvent）"
    - path: "src/Modules/Profile/Domain/Events/UserProfileUpdated.ts"
      provides: "UserProfileUpdated event class（繼承 Shared DomainEvent）"
    - path: "src/Modules/Profile/Application/DTOs/UserProfileDTO.ts"
      provides: "新增 profileToDTO() 函數供 Application Services 使用"
  key_links:
    - from: "UpdateProfileService.ts"
      to: "DomainEventDispatcher"
      via: "dispatcher.dispatchAll() + clearDomainEvents()"
    - from: "UserRegisteredHandler.ts"
      to: "DomainEventDispatcher"
      via: "dispatcher.dispatchAll() + clearDomainEvents()"
---

<objective>
補充 Profile 模組 4 個 P3/P4 DDD 問題，並更新 ddd-module skill 說明。

Purpose: 上一輪（260413-vzi）修正了 9 個 Critical/High 問題，本次收尾剩餘 4 個 Medium/Low 問題，確保 Profile 模組完全符合 DDD 規範。
Output:
- 新建 Domain/Events/ 目錄含 2 個事件 class（繼承 Shared DomainEvent）
- UserProfile.ts 移除 local event interfaces，改用 Shared DomainEvent
- Application Services 末尾派發 events 並 clear
- profileToDTO() 移至 Application/DTOs 層，Services 改為從 DTOs import
- reconstitute() 去除冗餘 null 判斷
- skills/ddd-module/references/domain.md 補充 Domain Events 指引
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/Shared/Domain/DomainEvent.ts
@src/Modules/Auth/Domain/Events/UserRegistered.ts
@src/Modules/Auth/Application/Services/RegisterUserService.ts

<interfaces>
<!-- Shared DomainEvent abstract class（所有 Profile events 必須繼承此） -->
From src/Shared/Domain/DomainEvent.ts:
```typescript
export abstract class DomainEvent {
  readonly eventId: string          // auto UUID
  readonly aggregateId: string      // constructor arg 1
  readonly eventType: string        // constructor arg 2
  readonly occurredAt: Date
  readonly version: number          // constructor arg 4, default 1
  readonly data: Record<string, unknown>  // constructor arg 3, default {}

  constructor(aggregateId: string, eventType: string, data?: Record<string, unknown>, version?: number)
  abstract toJSON(): Record<string, unknown>
}
```

<!-- DomainEventDispatcher API -->
From src/Shared/Domain/DomainEventDispatcher.ts:
```typescript
export class DomainEventDispatcher {
  static getInstance(): DomainEventDispatcher
  on(eventType: string, handler: (event: DomainEvent) => Promise<void>): void
  async dispatch(event: DomainEvent): Promise<void>
  async dispatchAll(events: DomainEvent[]): Promise<void>
}
```

<!-- 現有 UserProfile Aggregate（重要：clearDomainEvents() 回傳新實例） -->
From src/Modules/Profile/Domain/Aggregates/UserProfile.ts:
```typescript
// domainEvents getter 回傳 DomainEvent[]（目前是 local interface，需改為 Shared abstract class）
get domainEvents(): DomainEvent[]
clearDomainEvents(): UserProfile  // 回傳新實例（immutable）
```

<!-- Auth 模組的 event 寫法參考 -->
From src/Modules/Auth/Domain/Events/UserRegistered.ts:
```typescript
export class UserRegistered extends DomainEvent {
  constructor(userId: string, email: string) {
    super(userId, 'auth.user_registered', { userId, email })
  }
  toJSON(): Record<string, unknown> { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1：建立 Domain/Events/ 並重構 UserProfile events</name>
  <files>
    src/Modules/Profile/Domain/Events/UserProfileCreated.ts
    src/Modules/Profile/Domain/Events/UserProfileUpdated.ts
    src/Modules/Profile/Domain/Aggregates/UserProfile.ts
  </files>
  <action>
**步驟 A：建立 Domain/Events/ 目錄並新增兩個 event class**

1. 新建 `src/Modules/Profile/Domain/Events/UserProfileCreated.ts`：
```typescript
import { DomainEvent } from '@/Shared/Domain/DomainEvent'

/**
 * Emitted when a new user profile is created.
 */
export class UserProfileCreated extends DomainEvent {
  constructor(profileId: string, userId: string, email: string) {
    super(profileId, 'profile.user_profile_created', { profileId, userId, email })
  }

  get profileId(): string { return this.data.profileId as string }
  get userId(): string { return this.data.userId as string }
  get email(): string { return this.data.email as string }

  toJSON(): Record<string, unknown> {
    return {
      eventId: this.eventId,
      aggregateId: this.aggregateId,
      eventType: this.eventType,
      occurredAt: this.occurredAt.toISOString(),
      data: this.data,
    }
  }
}
```

2. 新建 `src/Modules/Profile/Domain/Events/UserProfileUpdated.ts`：
```typescript
import { DomainEvent } from '@/Shared/Domain/DomainEvent'

/**
 * Emitted when a user profile is updated.
 */
export class UserProfileUpdated extends DomainEvent {
  constructor(profileId: string, userId: string, fields: string[]) {
    super(profileId, 'profile.user_profile_updated', { profileId, userId, fields })
  }

  get profileId(): string { return this.data.profileId as string }
  get userId(): string { return this.data.userId as string }
  get fields(): string[] { return this.data.fields as string[] }

  toJSON(): Record<string, unknown> {
    return {
      eventId: this.eventId,
      aggregateId: this.aggregateId,
      eventType: this.eventType,
      occurredAt: this.occurredAt.toISOString(),
      data: this.data,
    }
  }
}
```

**步驟 B：重構 UserProfile.ts**

修改 `src/Modules/Profile/Domain/Aggregates/UserProfile.ts`：

1. 移除 local `DomainEvent` interface 定義（第 12-17 行）
2. 移除 local `UserProfileCreated` interface 定義（第 22-26 行）
3. 移除 local `UserProfileUpdated` interface 定義（第 31-35 行）
4. 新增 import：
   ```typescript
   import { DomainEvent } from '@/Shared/Domain/DomainEvent'
   import { UserProfileCreated } from '../Events/UserProfileCreated'
   import { UserProfileUpdated } from '../Events/UserProfileUpdated'
   ```
5. `createDefault()` 改為：
   ```typescript
   const event = new UserProfileCreated(id, userId, email)
   // props.domainEvents: [event]
   ```
6. `updateProfile()` 改為：
   ```typescript
   const event = new UserProfileUpdated(
     this.props.id,
     this.props.userId,
     Object.keys(fields).filter(k => fields[k as keyof UpdateProfileFields] !== undefined)
   )
   // props.domainEvents: [event]
   ```
7. `reconstitute()` 修正冗餘 null 檢查（P4 問題）：
   ```typescript
   // 舊（冗餘）:  phone: props.phone ? Phone.fromNullable(props.phone) : null,
   // 新（直接）:  phone: Phone.fromNullable(props.phone),
   ```
   `Phone.fromNullable` 已正確處理 null/undefined/empty，無需外層三元判斷。
8. `UserProfileProps.domainEvents` 型別確認為 `DomainEvent[]`（Shared abstract class）

確保 `export interface DomainEvent` 和 `export interface UserProfileCreated`/`UserProfileUpdated` 不再存在於此檔案。`UpdateProfileFields` 和 `ReconstitutionProps` export 保持不變。
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - Domain/Events/ 目錄存在，含 UserProfileCreated.ts 和 UserProfileUpdated.ts
    - 兩個 event class 都繼承 @/Shared/Domain/DomainEvent
    - UserProfile.ts 不再定義 local DomainEvent/UserProfileCreated/UserProfileUpdated interfaces
    - reconstitute() 改為 `phone: Phone.fromNullable(props.phone)`（無條件判斷）
    - tsc --noEmit 無錯誤
  </done>
</task>

<task type="auto">
  <name>Task 2：Application 層 — 移動 toDTO、加入 event dispatch</name>
  <files>
    src/Modules/Profile/Application/DTOs/UserProfileDTO.ts
    src/Modules/Profile/Application/Services/UpdateProfileService.ts
    src/Modules/Profile/Application/Services/GetProfileService.ts
    src/Modules/Profile/Application/EventHandlers/UserRegisteredHandler.ts
    src/Modules/Profile/index.ts
  </files>
  <action>
**步驟 A：在 DTOs 層新增 profileToDTO() 函數**

修改 `src/Modules/Profile/Application/DTOs/UserProfileDTO.ts`，在檔尾新增：

```typescript
import type { UserProfile } from '../../Domain/Aggregates/UserProfile'

/**
 * Maps a UserProfile aggregate to a UserProfileDTO.
 * Defined here to avoid Application Services importing from Infrastructure.
 */
export function profileToDTO(profile: UserProfile): UserProfileDTO {
  return {
    id: profile.id,
    displayName: profile.displayName,
    avatarUrl: profile.avatarUrl,
    phone: profile.phone,
    bio: profile.bio,
    timezone: profile.timezone,
    locale: profile.locale,
    notificationPreferences: profile.notificationPreferences,
    createdAt: profile.createdAt.toISOString(),
    updatedAt: profile.updatedAt.toISOString(),
  }
}
```

注意：只做 aggregate→DTO 的純映射（getter 轉換），不複製 Mapper 中的 DB 相關邏輯。`UserProfileMapper.toDTO()` 在 Infrastructure 中保留不動（供未來或其他用途），僅讓 Application Services 改用此函數。

**步驟 B：修改 UpdateProfileService.ts**

1. 移除 `import { UserProfileMapper } from '../../Infrastructure/Mappers/UserProfileMapper'`
2. 新增 imports：
   ```typescript
   import { DomainEventDispatcher } from '@/Shared/Domain/DomainEventDispatcher'
   import { profileToDTO } from '../DTOs/UserProfileDTO'
   ```
3. 修改 execute() 的 update 段落：
   ```typescript
   const updated = profile.updateProfile(request)
   await this.profileRepository.update(updated)

   // Dispatch domain events and clear
   const dispatcher = DomainEventDispatcher.getInstance()
   await dispatcher.dispatchAll(updated.domainEvents)

   return { success: true, message: 'Update successful', data: profileToDTO(updated) }
   ```
   注意：`clearDomainEvents()` 回傳新實例，而 `updated` 已持久化，此處只需 dispatch，不需要儲存 cleared 版本（aggregate 已寫入 DB）。

**步驟 C：修改 GetProfileService.ts**

1. 移除 `import { UserProfileMapper } from '../../Infrastructure/Mappers/UserProfileMapper'`
2. 新增 `import { profileToDTO } from '../DTOs/UserProfileDTO'`
3. 將 `UserProfileMapper.toDTO(profile)` 改為 `profileToDTO(profile)`

**步驟 D：修改 UserRegisteredHandler.ts**

在 `execute()` 末尾 `save(profile)` 之後，新增 event dispatch：
```typescript
const profile = UserProfile.createDefault(userId, email)
await this.userProfileRepository.save(profile)

// Dispatch domain events created during aggregate creation
const dispatcher = DomainEventDispatcher.getInstance()
await dispatcher.dispatchAll(profile.domainEvents)
```
新增 import：
```typescript
import { DomainEventDispatcher } from '@/Shared/Domain/DomainEventDispatcher'
```

**步驟 E：更新 index.ts exports**

在 `src/Modules/Profile/index.ts` 中：
1. 新增 export（Domain Events）：
   ```typescript
   export { UserProfileCreated } from './Domain/Events/UserProfileCreated'
   export { UserProfileUpdated } from './Domain/Events/UserProfileUpdated'
   ```
2. 現有的 `export type { UpdateProfileFields }` 和 `export { UserProfile }` 保持不變
3. 移除 `export type { DomainEvent }` 的 export（若存在），因為現在使用 Shared 版本
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - UpdateProfileService 和 GetProfileService 不再 import UserProfileMapper
    - UpdateProfileService.execute() 在 update 後呼叫 dispatcher.dispatchAll()
    - UserRegisteredHandler.execute() 在 save 後呼叫 dispatcher.dispatchAll()
    - profileToDTO() 定義於 Application/DTOs/UserProfileDTO.ts
    - index.ts 匯出兩個新 event class
    - tsc --noEmit 無錯誤
  </done>
</task>

<task type="auto">
  <name>Task 3：補充 skill domain.md Domain Events 說明</name>
  <files>
    skills/ddd-module/references/domain.md
  </files>
  <action>
修改 `skills/ddd-module/references/domain.md`，在第 4 節（Domain Event）擴充內容。

在現有 `## 4. Domain Event` 內容之後，補充下列段落（保留現有內容，只在末尾追加）：

```markdown
### Domain Events 目錄結構

每個事件獨立一個檔案，放置於 `Domain/Events/` 目錄：

```
src/Modules/<ModuleName>/Domain/Events/
├── UserProfileCreated.ts    # 一個事件一個檔案
└── UserProfileUpdated.ts
```

**禁止**：在 Aggregate 檔案內定義 event interface（污染領域模型，難以單獨測試）。

### Aggregate 宣告事件 vs Application Service 直接 dispatch 的取捨

| 方式 | 適用場景 | 範例 |
|------|----------|------|
| **Aggregate 收集事件**（推薦） | 事件與業務狀態變更強耦合，需確保原子性 | `updateProfile()` 回傳含事件的新實例 |
| **Application Service 直接 dispatch** | 跨 Aggregate 協調，事件不代表 Aggregate 內部狀態 | `RegisterUserService` dispatch `UserRegistered` |

本專案採用 **Aggregate 收集、Application Service 派發** 的混合模式：
- Aggregate 的工廠/變更方法負責收集事件（`domainEvents: [event]`）
- Application Service 在 use case 完成後統一 dispatch 並 clear

### Application Service 派發模式

```typescript
// Application Service 末尾（在 repository save 之後）
const updated = aggregate.updateSomething(data)
await this.repository.save(updated)

// 派發並 clear
const dispatcher = DomainEventDispatcher.getInstance()
await dispatcher.dispatchAll(updated.domainEvents)
// 注意：clearDomainEvents() 回傳新實例（immutable），
// 已持久化的 aggregate 不需要再次儲存 cleared 版本
```

### Event Class 規範

繼承 `@/Shared/Domain/DomainEvent`（**不要**定義 local interface）：

```typescript
// Domain/Events/UserProfileCreated.ts
import { DomainEvent } from '@/Shared/Domain/DomainEvent'

export class UserProfileCreated extends DomainEvent {
  constructor(profileId: string, userId: string, email: string) {
    // super(aggregateId, eventType, data)
    super(profileId, 'profile.user_profile_created', { profileId, userId, email })
  }

  // Typed accessors（從 data 取得，避免 as unknown）
  get profileId(): string { return this.data.profileId as string }
  get userId(): string { return this.data.userId as string }

  toJSON(): Record<string, unknown> {
    return {
      eventId: this.eventId,
      aggregateId: this.aggregateId,
      eventType: this.eventType,
      occurredAt: this.occurredAt.toISOString(),
      data: this.data,
    }
  }
}
```

**eventType 命名規範**：`<module>.<past_tense_event>` 格式（如 `profile.user_profile_created`）
```

同時，在 SKILL.md 的「常見陷阱」清單（末尾）補充：

在 `skills/ddd-module/SKILL.md` 的 `## 常見陷阱` 區塊末尾新增：
- **陷阱6**：Domain Event 定義在 Aggregate 檔案內（local interface）而非 `Domain/Events/` 獨立檔案 → 應獨立成檔，繼承 `@/Shared/Domain/DomainEvent`
- **陷阱7**：Application Service 收集 Aggregate 的 domainEvents 但忘記呼叫 `dispatcher.dispatchAll()` → 事件永遠不會被派發；use case 完成後必須派發
- **陷阱8**：Application Service import Infrastructure Mapper（如 `UserProfileMapper.toDTO()`）→ 應將 `toDTO` 邏輯移至 `Application/DTOs/` 層的 helper 函數
  </action>
  <verify>
    <automated>grep -n "Domain/Events/" /Users/carl/Dev/CMG/Draupnir/skills/ddd-module/references/domain.md | head -5</automated>
  </verify>
  <done>
    - domain.md 第 4 節新增了目錄結構、取捨說明、派發模式、event class 規範
    - SKILL.md 常見陷阱補充陷阱 6、7、8
    - 內容清晰，後續開發者可直接依照範例寫新事件
  </done>
</task>

</tasks>

<verification>
完成後執行：
```bash
cd /Users/carl/Dev/CMG/Draupnir && npx tsc --noEmit
```
確認無型別錯誤。

確認 Domain/Events/ 目錄存在且包含正確 class：
```bash
ls src/Modules/Profile/Domain/Events/
```

確認 Services 不再 import Mapper：
```bash
grep -n "UserProfileMapper" src/Modules/Profile/Application/Services/*.ts
# 應無輸出
```

確認 dispatch 已加入：
```bash
grep -n "dispatchAll" src/Modules/Profile/Application/Services/UpdateProfileService.ts src/Modules/Profile/Application/EventHandlers/UserRegisteredHandler.ts
```
</verification>

<success_criteria>
- [ ] `src/Modules/Profile/Domain/Events/` 目錄含 UserProfileCreated.ts 和 UserProfileUpdated.ts，兩者繼承 @/Shared/Domain/DomainEvent
- [ ] `UserProfile.ts` 移除 3 個 local event interface，改 import 兩個新 class
- [ ] `reconstitute()` 改為 `phone: Phone.fromNullable(props.phone)` 無條件判斷
- [ ] `profileToDTO()` 函數存在於 Application/DTOs/UserProfileDTO.ts
- [ ] `UpdateProfileService` 和 `GetProfileService` 不 import UserProfileMapper
- [ ] `UpdateProfileService.execute()` 在 update 後呼叫 `dispatcher.dispatchAll()`
- [ ] `UserRegisteredHandler.execute()` 在 save 後呼叫 `dispatcher.dispatchAll()`
- [ ] `index.ts` 匯出 UserProfileCreated 和 UserProfileUpdated
- [ ] `skills/ddd-module/references/domain.md` 新增 Domain Events 完整說明
- [ ] `skills/ddd-module/SKILL.md` 新增陷阱 6-8
- [ ] `npx tsc --noEmit` 無錯誤
</success_criteria>

<output>
完成後 git commit：
```
refactor: [profile] P3/P4 DDD 問題修正 — Domain Events 獨立、Application Service 解耦、dispatch 加入
docs: [ddd-module skill] 補充 Domain Events 目錄規範與派發模式說明
```

不需要產生 SUMMARY.md（quick task）。
</output>
