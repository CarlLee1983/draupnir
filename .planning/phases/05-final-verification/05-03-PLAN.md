---
phase: 05-final-verification
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: []
autonomous: false
requirements: [TEST-04]

must_haves:
  truths:
    - "Playwright E2E suite passes unchanged after SDK extraction and gateway abstraction refactor"
    - "All critical user flows (API key management, usage dashboard, credit operations, CLI auth) work correctly"
  artifacts:
    - path: "playwright.config.ts"
      provides: "E2E test configuration"
  key_links:
    - from: "bun run test:e2e"
      to: "Playwright test runner"
      via: "playwright.config.ts"
      pattern: "playwright test"
---

<objective>
Run the Playwright E2E suite against a live Draupnir instance to verify all critical user flows work correctly after four phases of gateway abstraction, domain rename, and SDK extraction.

Purpose: Provide the final human-verified confirmation that the milestone has not broken any end-user-visible behavior. This is the only quality gate that requires a live server and cannot be verified programmatically.
Output: Playwright test results confirming all E2E flows pass (TEST-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-final-verification/05-CONTEXT.md

Phase 5 success criterion #4: "Playwright E2E suite (bun run test:e2e) passes unchanged — all critical user flows verified"

Prior phase note (Phase 4 VERIFICATION.md): E2E tests were deferred at Phase 4 completion because they require a running server and external Bifrost API. Phase 5 is the designated gate for E2E verification.

E2E test command: bun run test:e2e
Alternative UI mode: bun run test:e2e:ui
Debug mode: bun run test:e2e:debug
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt automated E2E run</name>
  <files></files>
  <read_first>
    - playwright.config.ts (check baseURL and test file locations)
  </read_first>
  <action>
    Attempt to run the Playwright E2E suite:
    ```
    bun run test:e2e 2>&1
    ```

    If the server is already running and Bifrost is accessible: record the results and proceed to document them.

    If the server is not running (connection refused, no server found): start the development server in the background and retry:
    ```
    bun run dev &
    sleep 3
    bun run test:e2e 2>&1
    ```

    If tests require Bifrost API credentials that are not configured in the environment: capture the error output and note "ENVIRONMENT-BLOCKED" — the checkpoint task below requires human execution.

    Capture the test output: count of passing/failing tests, any error messages.
  </action>
  <verify>
    <automated>bun run test:e2e 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - If E2E suite runs: output contains "passed" with zero "failed" test count
    - If E2E suite cannot run (missing server/credentials): output is captured and documented for the human checkpoint
  </acceptance_criteria>
  <done>E2E run attempted; results captured (pass or environment-blocked)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human E2E verification</name>
  <files></files>
  <action>
    Human runs bun run test:e2e against a live Draupnir instance connected to Bifrost gateway and confirms all tests pass.
  </action>
  <verify>
    <automated>MISSING — E2E requires live server; human runs bun run test:e2e and reports results</automated>
  </verify>
  <acceptance_criteria>
    - bun run test:e2e exits 0 with all Playwright tests passing
    - No failures in critical user flows: API key creation/deletion, usage dashboard, credit top-up, CLI device flow
  </acceptance_criteria>
  <done>Human confirms E2E suite passes with zero failures</done>
  <what-built>
    All Phase 5 verification work:
    - Plan 01: Fixed CliApiServiceProvider BifrostClient type violation; reverted IHttpContext.ts import; verified grep clean, lint diff, typecheck diff, no new any/ts-ignore, route test baseline
    - Plan 02: Updated CONCERNS.md with resolution markers for items #1, #2, #3 (Resolved) and #6 (Partially Resolved)
    - Plan 03 Task 1: Attempted automated Playwright E2E run (result captured above)
  </what-built>
  <how-to-verify>
    1. Ensure you have a Draupnir instance running with Bifrost gateway accessible
    2. Run: `bun run test:e2e`
    3. Expected: All Playwright tests pass — no failures in critical user flows (API key creation/deletion, usage dashboard, credit top-up, CLI auth device flow)
    4. If any test fails: note which test and the error message, then type "ISSUES: [description]"
    5. If all tests pass: type "approved"

    Optional — run with UI for visual inspection:
    `bun run test:e2e:ui`
  </how-to-verify>
  <resume-signal>Type "approved" if E2E passes, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
E2E verification is the primary output of this plan. After human approval, run the full quality-gate summary:

```bash
# TEST-02: Grep clean
grep -rn "BifrostClient\|bifrostClient" src/Modules/ src/Foundation/Application/ \
  | grep -v BifrostGatewayAdapter.ts || echo "CLEAN"

# QUAL-01: Lint
biome lint src tests packages 2>&1 | grep "Found"

# QUAL-02: Typecheck
bun run typecheck 2>&1

# QUAL-05: CONCERNS.md resolution markers
grep "STATUS:" .planning/codebase/CONCERNS.md
```
</verification>

<success_criteria>
- Playwright E2E suite passes with zero test failures (TEST-04)
- Human approves the E2E run results
- Full milestone quality gate summary confirms all 5 Phase 5 success criteria from ROADMAP.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-verification/05-03-SUMMARY.md`
</output>
