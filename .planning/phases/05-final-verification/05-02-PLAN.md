---
phase: 05-final-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [QUAL-05]

must_haves:
  truths:
    - "CONCERNS.md item #1 (Tight Coupling to BifrostClient) is marked Resolved with Phase 1-4 references"
    - "CONCERNS.md item #2 (Leaky Snake_case) is marked Resolved with Phase 1 and Phase 3 references"
    - "CONCERNS.md item #3 (Missing ILLMGatewayClient) is marked Resolved with Phase 1 reference"
    - "CONCERNS.md item #6 (Bifrost Env Coupling) is marked Partially Resolved with explanation of remaining items"
    - "Items #4, #5, #7-#17 are untouched (per D-07)"
  artifacts:
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concern statuses with resolution history"
      contains: "Resolved"
  key_links:
    - from: ".planning/codebase/CONCERNS.md"
      to: "Phase 1-4 execution"
      via: "Resolution summaries with phase references"
      pattern: "Phase [1-4]"
---

<objective>
Update CONCERNS.md to mark items #1, #2, #3 as Resolved and item #6 as Partially Resolved, documenting what was done in Phases 1-4 and what remains for future milestones.

Purpose: Provide a lasting record that the core BifrostClient coupling concern has been addressed, so future developers know the milestone achieved its stated goal.
Output: Updated CONCERNS.md with resolution markers for 4 items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
@.planning/phases/05-final-verification/05-CONTEXT.md

CONCERNS.md update rules (from CONTEXT.md decisions):
- D-05: Items #1, #2, #3 get a "Resolved" marker with date (2026-04-10), phase references, and 1-2 line summary. Original description preserved below for historical context
- D-06: Item #6 gets a "Partially Resolved" marker — proxy URL moved into SDK package (Phase 4), but BIFROST_* env var names and ErrorCodes constants remain. Note remaining items as future milestone work
- D-07: Items #4-#5, #7-#17 are NOT touched

Resolution summaries to use:
- Item #1: Resolved in Phases 1-4 (2026-04-10). ILLMGatewayClient interface + BifrostGatewayAdapter introduced (Phase 1); all 7 application services migrated (Phase 2); BifrostClient extracted to packages/bifrost-sdk/ workspace package (Phase 4). No file under src/Modules/ or src/Foundation/Application/ imports BifrostClient.
- Item #2: Resolved in Phases 1, 3 (2026-04-10). camelCase DTOs in ILLMGatewayClient/types.ts; snake_case conversion isolated to BifrostGatewayAdapter boundary (Phase 1). bifrostVirtualKeyId renamed to gatewayKeyId in domain aggregates (Phase 3).
- Item #3: Resolved in Phase 1 (2026-04-10). ILLMGatewayClient interface created at src/Foundation/Infrastructure/Services/LLMGateway/; BifrostGatewayAdapter and MockGatewayClient implemented; llmGatewayClient DI singleton registered.
- Item #6: Partially Resolved in Phase 4 (2026-04-10). Hardcoded Bifrost proxy URL moved into packages/bifrost-sdk/ config (BifrostClientConfig.proxyBaseUrl). Remaining: BIFROST_API_URL / BIFROST_MASTER_KEY env var names, ErrorCodes.BIFROST_ERROR / BIFROST_TIMEOUT constants — deferred to future milestone (breaking API change).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resolution markers to CONCERNS.md items #1, #2, #3, #6</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file — need to locate exact headings and insert resolution blocks)
  </read_first>
  <action>
    For each of the four items, insert a resolution block immediately AFTER the item's heading line and BEFORE the "**Issue:**" line. Do not remove or modify any existing content.

    **Item #1 — "1. Tight Coupling to BifrostClient"**
    Insert after the heading:
    ```
    > **STATUS: Resolved — 2026-04-10**
    > Resolved across Phases 1-4. `ILLMGatewayClient` interface and `BifrostGatewayAdapter` introduced (Phase 1); all 7 application services migrated to the interface (Phase 2); `bifrostVirtualKeyId` renamed to `gatewayKeyId` in domain aggregates (Phase 3); `BifrostClient` extracted to `packages/bifrost-sdk/` workspace package (Phase 4). Zero files under `src/Modules/` or `src/Foundation/Application/` import `BifrostClient`.
    ```

    **Item #2 — "2. Leaky Snake_case Abstractions from Bifrost"**
    Insert after the heading:
    ```
    > **STATUS: Resolved — 2026-04-10**
    > Resolved in Phases 1 and 3. `ILLMGatewayClient` DTOs use camelCase exclusively (`keyId`, `maxLimit`, `resetDuration`, etc.); snake_case conversion is isolated to the `BifrostGatewayAdapter` boundary (Phase 1). `bifrostVirtualKeyId` field renamed to `gatewayKeyId` in `AppApiKey` and `ApiKey` domain aggregates; repositories map to the unchanged DB column `bifrost_virtual_key_id` (Phase 3).
    ```

    **Item #3 — "3. Missing ILLMGatewayClient Abstraction Layer"**
    Insert after the heading:
    ```
    > **STATUS: Resolved — 2026-04-10**
    > Resolved in Phase 1. `ILLMGatewayClient` interface created at `src/Foundation/Infrastructure/Services/LLMGateway/ILLMGatewayClient.ts`. `BifrostGatewayAdapter` and `MockGatewayClient` implemented. `llmGatewayClient` singleton registered in `FoundationServiceProvider` and injected throughout the application layer.
    ```

    **Item #6 — "6. Bifrost-Specific Environment Variables Hardcoded Across Services"**
    Insert after the heading:
    ```
    > **STATUS: Partially Resolved — 2026-04-10**
    > Partially resolved in Phase 4. Hardcoded Bifrost proxy URL moved from `SdkApiServiceProvider` into `BifrostClientConfig.proxyBaseUrl` in `packages/bifrost-sdk/` (Phase 4). Remaining: `BIFROST_API_URL` / `BIFROST_MASTER_KEY` env var names, `ErrorCodes.BIFROST_ERROR` / `BIFROST_TIMEOUT` constants — renaming is a breaking API change deferred to a future milestone.
    ```

    Use blockquote (`>`) format for the resolution block so it visually stands out from the existing "**Issue:**" content. Preserve all original content below each resolution block.
  </action>
  <verify>
    <automated>grep -n "STATUS: Resolved\|STATUS: Partially Resolved" /Users/carl/Dev/CMG/Draupnir/.planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "STATUS: Resolved" .planning/codebase/CONCERNS.md returns exactly 3 matches (items #1, #2, #3)
    - grep "STATUS: Partially Resolved" .planning/codebase/CONCERNS.md returns exactly 1 match (item #6)
    - grep "2026-04-10" .planning/codebase/CONCERNS.md returns at least 4 matches (one per resolved item)
    - File still contains the original "**Issue:**" section for all 4 modified items (original content preserved)
    - Items #4, #5, #7-#17 headings are unchanged — grep "### 4.\|### 5.\|### 7." returns same matches as before
    - Item #1 resolution text contains "Phase 1-4" and "src/Modules/"
    - Item #3 resolution text contains "ILLMGatewayClient" and "Phase 1"
    - Item #6 resolution text contains "Partially" and "BIFROST_API_URL"
  </acceptance_criteria>
  <done>CONCERNS.md has 4 resolution markers: 3 Resolved (#1, #2, #3) and 1 Partially Resolved (#6). Original content preserved. Items #4-#5 and #7-#17 untouched.</done>
</task>

</tasks>

<verification>
```bash
# Confirm all 4 resolution markers present
grep -n "STATUS:" /Users/carl/Dev/CMG/Draupnir/.planning/codebase/CONCERNS.md

# Confirm items #4, #5, #7 headings untouched
grep -n "### [4-9]\." /Users/carl/Dev/CMG/Draupnir/.planning/codebase/CONCERNS.md | head -10

# Confirm original Issue text still present for resolved items
grep -n "Issue:" /Users/carl/Dev/CMG/Draupnir/.planning/codebase/CONCERNS.md | head -6
```
</verification>

<success_criteria>
- .planning/codebase/CONCERNS.md contains exactly 3 "STATUS: Resolved" markers (items #1, #2, #3) and 1 "STATUS: Partially Resolved" marker (item #6)
- All resolution text includes a 2026-04-10 date and phase references
- Items #4-#5 and #7-#17 content is unchanged
- QUAL-05 is satisfied: items #1, #2, #3 resolved; #6 partially resolved with explanation
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-verification/05-02-SUMMARY.md`
</output>
