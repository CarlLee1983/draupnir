---
phase: 07-framework-capability-docs-and-improvement
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/Modules/SdkApi/Presentation/Controllers/SdkApiController.ts
  - src/Modules/SdkApi/Infrastructure/Middleware/AppAuthMiddleware.ts
  - src/Modules/SdkApi/Application/UseCases/AuthenticateApp.ts
  - src/Modules/SdkApi/Application/UseCases/ProxyModelCall.ts
  - src/Modules/SdkApi/Application/UseCases/QueryUsage.ts
  - src/Modules/SdkApi/Application/UseCases/QueryBalance.ts
  - src/Modules/Health/Presentation/Controllers/HealthController.ts
  - src/Modules/Health/Application/Services/PerformHealthCheckService.ts
  - src/Modules/Dashboard/Presentation/Controllers/DashboardController.ts
  - src/Modules/Dashboard/Application/Services/GetDashboardSummaryService.ts
  - src/Modules/Dashboard/Application/Services/GetUsageChartService.ts
  - src/Modules/DevPortal/Presentation/Controllers/DevPortalController.ts
  - src/Modules/DevPortal/Application/Services/GetApiDocsService.ts
  - src/Modules/DevPortal/Application/Services/ListAppsService.ts
  - src/Modules/DevPortal/Application/Services/ConfigureWebhookService.ts
  - src/Modules/DevPortal/Application/Services/RegisterAppService.ts
  - src/Modules/DevPortal/Application/Services/ManageAppKeysService.ts
  - src/Modules/DevPortal/Infrastructure/Services/WebhookDispatcher.ts
  - src/Modules/AppApiKey/Application/Services/RevokeAppKeyService.ts
  - src/Modules/AppApiKey/Application/Services/SetAppKeyScopeService.ts
  - src/Modules/AppApiKey/Application/Services/RotateAppKeyService.ts
  - src/Modules/AppApiKey/Application/Services/ListAppKeysService.ts
  - src/Modules/AppApiKey/Application/Services/GetAppKeyUsageService.ts
  - src/Modules/CliApi/Application/Services/ExchangeDeviceCodeService.ts
  - src/Modules/CliApi/Application/Services/RevokeCliSessionService.ts
  - src/Modules/CliApi/Application/Services/AuthorizeDeviceService.ts
  - src/Modules/CliApi/Application/Services/ProxyCliRequestService.ts
autonomous: true
requirements: [API-01]
user_setup: []

must_haves:
  truths:
    - "All SdkApi error messages are English only"
    - "All Health check messages are English only"
    - "All Dashboard service messages are English only"
    - "All DevPortal service messages are English only"
    - "All AppApiKey service messages are English only"
    - "All CliApi service messages are English only"
    - "No Chinese strings remain in API-facing code"
  artifacts:
    - path: "src/Modules/SdkApi/**/*.ts"
      provides: "English-only API messages"
    - path: "src/Modules/Health/**/*.ts"
      provides: "English-only health check messages"
    - path: "src/Modules/Dashboard/**/*.ts"
      provides: "English-only dashboard messages"
    - path: "src/Modules/DevPortal/**/*.ts"
      provides: "English-only dev portal messages"
    - path: "src/Modules/AppApiKey/**/*.ts"
      provides: "English-only API key messages"
    - path: "src/Modules/CliApi/**/*.ts"
      provides: "English-only CLI messages"

---

<objective>
Complete the API English-only enforcement by standardizing remaining modules (SdkApi, Health, Dashboard, DevPortal, AppApiKey, CliApi) to use only English in error messages and service responses.

**Purpose:** These modules are high-impact (used by external integrations, dashboards, CLI tools). English-only responses ensure language consistency across all APIs.

**Output:** All remaining modules use English-only messages. Global Chinese removal complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-framework-capability-docs-and-improvement/07-RESEARCH.md
@docs/superpowers/plans/2025-05-15-english-only-api-responses.md

## Modules in Scope

1. **SdkApi** — External SDK gateway, highest visibility
2. **Health** — Operational health checks
3. **Dashboard** — Backend dashboards
4. **DevPortal** — Developer portal integrations
5. **AppApiKey** — API key management
6. **CliApi** — CLI integration

All require consistent English messages for clients.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize SdkApi to English-only</name>
  <files>
    - src/Modules/SdkApi/Presentation/Controllers/SdkApiController.ts
    - src/Modules/SdkApi/Infrastructure/Middleware/AppAuthMiddleware.ts
    - src/Modules/SdkApi/Application/UseCases/AuthenticateApp.ts
    - src/Modules/SdkApi/Application/UseCases/ProxyModelCall.ts
    - src/Modules/SdkApi/Application/UseCases/QueryUsage.ts
    - src/Modules/SdkApi/Application/UseCases/QueryBalance.ts
  </files>
  <action>
Replace all Chinese error messages with English:

**SdkApiController:**
- '未經授權' → 'Unauthorized'
- Any SDK-specific Chinese → English equivalent

**AppAuthMiddleware:**
- '無效的應用程式認證' → 'Invalid app credentials'
- '缺少認證標頭' → 'Missing authentication header'

**Use cases (AuthenticateApp, ProxyModelCall, etc.):**
- '應用程式不存在' → 'Application not found'
- '密鑰已過期' → 'Key has expired'
- '調用模型失敗' → 'Failed to call model'
- '查詢使用量失敗' → 'Failed to query usage'
- '查詢餘額失敗' → 'Failed to query balance'

Keep error codes unchanged (UNAUTHORIZED, NOT_FOUND, EXPIRED, etc.).
  </action>
  <verify>
    <automated>bun test src/Modules/SdkApi/__tests__ -v</automated>
  </verify>
  <done>All SdkApi modules use English-only messages. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Health module to English</name>
  <files>
    - src/Modules/Health/Presentation/Controllers/HealthController.ts
    - src/Modules/Health/Application/Services/PerformHealthCheckService.ts
  </files>
  <action>
Replace health check messages:

**HealthController:**
- '系統健康' → 'System healthy'
- '系統異常' → 'System unhealthy'

**PerformHealthCheckService:**
- '資料庫連線失敗' → 'Database connection failed'
- '快取連線失敗' → 'Cache connection failed'
- '網關服務失敗' → 'Gateway service failed'

Keep status codes (OK, DEGRADED, DOWN) unchanged.
  </action>
  <verify>
    <automated>bun test src/Modules/Health/__tests__ -v</automated>
  </verify>
  <done>Health module uses English-only messages.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize Dashboard module to English</name>
  <files>
    - src/Modules/Dashboard/Presentation/Controllers/DashboardController.ts
    - src/Modules/Dashboard/Application/Services/GetDashboardSummaryService.ts
    - src/Modules/Dashboard/Application/Services/GetUsageChartService.ts
  </files>
  <action>
Replace dashboard service messages:

**DashboardController:**
- Any Chinese error message → English equivalent

**GetDashboardSummaryService:**
- '讀取摘要失敗' → 'Failed to load summary'
- '無資料' → 'No data available'

**GetUsageChartService:**
- '讀取圖表失敗' → 'Failed to load chart'
- '查詢期間無效' → 'Invalid query period'

Keep error codes unchanged.
  </action>
  <verify>
    <automated>bun test src/Modules/Dashboard/__tests__ -v</automated>
  </verify>
  <done>Dashboard module uses English-only messages.</done>
</task>

<task type="auto">
  <name>Task 4: Standardize DevPortal module to English</name>
  <files>
    - src/Modules/DevPortal/Presentation/Controllers/DevPortalController.ts
    - src/Modules/DevPortal/Application/Services/GetApiDocsService.ts
    - src/Modules/DevPortal/Application/Services/ListAppsService.ts
    - src/Modules/DevPortal/Application/Services/ConfigureWebhookService.ts
    - src/Modules/DevPortal/Application/Services/RegisterAppService.ts
    - src/Modules/DevPortal/Application/Services/ManageAppKeysService.ts
    - src/Modules/DevPortal/Infrastructure/Services/WebhookDispatcher.ts
  </files>
  <action>
Replace DevPortal messages:

**GetApiDocsService:**
- '讀取 API 文件失敗' → 'Failed to load API documentation'

**ListAppsService:**
- '讀取應用程式失敗' → 'Failed to load applications'

**ConfigureWebhookService:**
- 'Webhook 設定失敗' → 'Failed to configure webhook'

**RegisterAppService:**
- '註冊應用程式失敗' → 'Failed to register application'
- '應用程式名稱已被使用' → 'Application name already in use'

**ManageAppKeysService:**
- '管理密鑰失敗' → 'Failed to manage keys'

**WebhookDispatcher:**
- 'Webhook 發送失敗' → 'Failed to dispatch webhook'

Keep error codes unchanged.
  </action>
  <verify>
    <automated>bun test src/Modules/DevPortal/__tests__ -v</automated>
  </verify>
  <done>DevPortal module uses English-only messages.</done>
</task>

<task type="auto">
  <name>Task 5: Standardize AppApiKey module to English</name>
  <files>
    - src/Modules/AppApiKey/Application/Services/RevokeAppKeyService.ts
    - src/Modules/AppApiKey/Application/Services/SetAppKeyScopeService.ts
    - src/Modules/AppApiKey/Application/Services/RotateAppKeyService.ts
    - src/Modules/AppApiKey/Application/Services/ListAppKeysService.ts
    - src/Modules/AppApiKey/Application/Services/GetAppKeyUsageService.ts
  </files>
  <action>
Replace AppApiKey service messages:

**RevokeAppKeyService:**
- '撤銷密鑰失敗' → 'Failed to revoke key'

**SetAppKeyScopeService:**
- '設定密鑰範圍失敗' → 'Failed to set key scope'

**RotateAppKeyService:**
- '輪換密鑰失敗' → 'Failed to rotate key'

**ListAppKeysService:**
- '讀取密鑰失敗' → 'Failed to load keys'

**GetAppKeyUsageService:**
- '讀取密鑰用量失敗' → 'Failed to load key usage'

Keep error codes unchanged.
  </action>
  <verify>
    <automated>bun test src/Modules/AppApiKey/__tests__ -v</automated>
  </verify>
  <done>AppApiKey module uses English-only messages.</done>
</task>

<task type="auto">
  <name>Task 6: Standardize CliApi module to English</name>
  <files>
    - src/Modules/CliApi/Application/Services/ExchangeDeviceCodeService.ts
    - src/Modules/CliApi/Application/Services/RevokeCliSessionService.ts
    - src/Modules/CliApi/Application/Services/AuthorizeDeviceService.ts
    - src/Modules/CliApi/Application/Services/ProxyCliRequestService.ts
  </files>
  <action>
Replace CliApi service messages:

**ExchangeDeviceCodeService:**
- '設備代碼已過期' → 'Device code has expired'
- '交換代碼失敗' → 'Failed to exchange device code'

**RevokeCliSessionService:**
- '撤銷 CLI 工作階段失敗' → 'Failed to revoke CLI session'

**AuthorizeDeviceService:**
- '授權設備失敗' → 'Failed to authorize device'

**ProxyCliRequestService:**
- 'CLI 請求轉發失敗' → 'Failed to proxy CLI request'

Keep error codes unchanged.
  </action>
  <verify>
    <automated>bun test src/Modules/CliApi/__tests__ -v</automated>
  </verify>
  <done>CliApi module uses English-only messages.</done>
</task>

<task type="auto">
  <name>Task 7: Final verification - no Chinese in API code</name>
  <files>
    - (verification only, no direct edits)
  </files>
  <action>
Run a comprehensive scan to confirm no Chinese characters remain in API-facing code:

```bash
grep -r '[一-龥]' src/Modules/*/Presentation/ src/Modules/*/Application/ src/Modules/*/Infrastructure/ --include="*.ts" | grep -v test | grep -v __tests__ | grep -E "message|error|'\"|\`" | wc -l
```

Should return 0 (no Chinese). If any found, identify the module and update the remaining strings.

Also verify:
```bash
# No hardcoded Chinese in error messages or fallback strings
grep -r "message:.*'[一-龥]" src/Modules --include="*.ts" | wc -l
# Should be 0

# No Chinese in validation messages
grep -r "z\..*'[一-龥]" src/Modules --include="*.ts" | wc -l
# Should be 0
```
  </action>
  <verify>
    <automated>bun test src/Modules --v</automated>
  </verify>
  <done>Full module test suite passes. No Chinese characters remain in API-facing code.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
# Test each module
bun test src/Modules/SdkApi/__tests__ -v
bun test src/Modules/Health/__tests__ -v
bun test src/Modules/Dashboard/__tests__ -v
bun test src/Modules/DevPortal/__tests__ -v
bun test src/Modules/AppApiKey/__tests__ -v
bun test src/Modules/CliApi/__tests__ -v

# Global verification - no Chinese in non-test code
grep -r '[一-龥]' src/Modules/*/Presentation/ src/Modules/*/Application/ src/Modules/*/Infrastructure/ --include="*.ts" | grep -v __tests__ | grep -v "test.ts" | grep -E "message|error" && echo "❌ Found Chinese" || echo "✓ No Chinese in API code"

# Run full module test suite
bun test src/Modules --v

# Final check - typecheck and lint
bun run typecheck
bun run lint
```

Verify:
- All 6 modules pass tests
- 0 Chinese characters in API-facing code
- All error codes remain unchanged
- Full test suite passes
- No linting or type errors
</verification>

<success_criteria>
- [x] All SdkApi error messages are English
- [x] All Health check messages are English
- [x] All Dashboard service messages are English
- [x] All DevPortal service messages are English
- [x] All AppApiKey service messages are English
- [x] All CliApi service messages are English
- [x] All tests pass for these 6 modules
- [x] Global scan confirms 0 Chinese in API code
- [x] Error codes remain unchanged
- [x] No linting or type errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-framework-capability-docs-and-improvement/07-04-SUMMARY.md`

Summary should include:
- 6 modules standardized to English
- Count of message strings converted (estimated ~40+)
- Test pass/fail counts before and after
- Confirmation of zero Chinese in all API-facing code
- Phase gate: Ready for full test suite run
</output>
