---
phase: 19-alerts-module-decoupling
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts
  - src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts
  - src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts
  - src/Modules/Alerts/Application/Services/SendAlertService.ts
  - src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts
  - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
  - src/Modules/Alerts/MODULE.md
  - src/Modules/Alerts/__tests__/SendAlertService.test.ts
  - src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts
autonomous: true
requirements:
  - ALERTS-02
user_setup: []

must_haves:
  truths:
    - "SendAlertService no longer imports IOrganizationRepository / IOrganizationMemberRepository / IAuthRepository"
    - "SendAlertService and EvaluateThresholdsService use object-literal ctor (deps wrapped in a single readonly object)"
    - "IAlertRecipientResolver is defined in src/Modules/Alerts/Domain/Services/ (Alerts-owned Domain port)"
    - "AlertRecipientResolverImpl composes Organization + OrganizationMember + Auth repos internally"
    - "MODULE.md exists and lists every cross-module port consumed by Alerts with rationale"
    - "All previously-green Alerts tests remain green; SendAlertService and EvaluateThresholdsService tests updated to new ctor shape"
  artifacts:
    - path: "src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts"
      provides: "Alerts-owned port: resolveByOrg(orgId) → AlertRecipientContext"
    - path: "src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts"
      provides: "Composition of 3 cross-module repos behind the port"
    - path: "src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts"
      provides: "DI-less fake for resolver in unit tests"
    - path: "src/Modules/Alerts/MODULE.md"
      provides: "Audit contract: lists all cross-module port dependencies + rationale (per D-09)"
  key_links:
    - from: "SendAlertService"
      to: "IAlertRecipientResolver"
      via: "constructor dep { recipientResolver }; calls resolveByOrg(orgId) once per send"
      pattern: "recipientResolver\\.resolveByOrg"
    - from: "AlertRecipientResolverImpl"
      to: "IOrganizationRepository + IOrganizationMemberRepository + IAuthRepository"
      via: "constructor object-literal { orgRepo, orgMemberRepo, authRepo }"
      pattern: "orgRepo\\.findById|orgMemberRepo\\.findByOrgId|authRepo\\.findById"
    - from: "AlertsServiceProvider"
      to: "alertRecipientResolver binding"
      via: "container.bind('alertRecipientResolver', c => new AlertRecipientResolverImpl({ orgRepo: c.make('organizationRepository'), ... }))"
      pattern: "container\\.(bind|singleton)\\('alertRecipientResolver'"
---

<objective>
收斂 Alerts 模組對核心業務模組的直接依賴：引入 `IAlertRecipientResolver` port（Alerts Domain 擁有），將 SendAlertService 對 Organization/OrgMember/Auth 三個 repo 的直接依賴替換成單一語意化 port；統一 SendAlertService 與 EvaluateThresholdsService 為 object-literal ctor；新增 `src/Modules/Alerts/MODULE.md` 作為可審查的跨模組依賴契約（per D-05, D-06, D-07, D-08, D-09, D-18）。

Purpose: 完成 ALERTS-02（不依賴核心業務邏輯層實現細節），建立 Plan 3 可直接取用的 DI-less 測試 fake (`InMemoryAlertRecipientResolver`) 與 object-literal ctor 基底（讓 Plan 3 再換掉 mailer/notifier 依賴時不再需要再改 ctor 形式）。
Output: 新增 1 個 Domain port、1 個 Infrastructure 實作、1 個 test fake、1 個 MODULE.md；重構 2 個 Application service 的 constructor；更新 AlertsServiceProvider + 2 個測試。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-alerts-module-decoupling/19-CONTEXT.md
@.planning/phases/19-alerts-module-decoupling/19-RESEARCH.md
@.planning/phases/19-alerts-module-decoupling/19-01-SUMMARY.md

@src/Modules/Alerts/Application/Services/SendAlertService.ts
@src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts
@src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts
@src/Modules/Alerts/Application/Services/ResendDeliveryService.ts
@src/Modules/Organization/Domain/Repositories/IOrganizationRepository.ts
@src/Modules/Organization/Domain/Repositories/IOrganizationMemberRepository.ts
@src/Modules/Auth/Domain/Repositories/IAuthRepository.ts
@src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
@src/Modules/Alerts/__tests__/SendAlertService.test.ts
@src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IAlertRecipientResolver port + AlertRecipientResolverImpl + InMemoryAlertRecipientResolver fake</name>
  <files>
    src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts
    src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts
    src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (lines where Org/OrgMember/Auth repos are used — this determines resolver contract)
    - src/Modules/Organization/Domain/Repositories/IOrganizationRepository.ts (full)
    - src/Modules/Organization/Domain/Repositories/IOrganizationMemberRepository.ts (full — `isManager()` and `findByOrgId`)
    - src/Modules/Auth/Domain/Repositories/IAuthRepository.ts (full — user email retrieval)
    - src/Modules/Alerts/Domain/ (check existing directory layout — if `Services/` does not yet exist under Domain, create it)
    - src/Modules/Dashboard/Application/Ports/IUsageRepository.ts (port style reference from Phase 17)
    - .planning/phases/19-alerts-module-decoupling/19-RESEARCH.md Pattern 2 section (concrete shape + recommended implementation)
  </read_first>
  <action>
    1. Create `src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts` (per D-05 + research Pattern 2):

       export interface AlertRecipientContext {
         readonly orgId: string
         readonly orgName: string
         readonly emails: readonly string[]
         readonly locale?: string
       }

       export interface IAlertRecipientResolver {
         resolveByOrg(orgId: string): Promise<AlertRecipientContext>
       }

       - Pure TypeScript interface (no zod — research recommendation: payload is server-constructed from typed domain values; zod is dead weight)
       - NO imports from Infrastructure; Domain-only

    2. Create `src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts` (per D-06 + research recommendation on placement path):

       import type { IOrganizationRepository } from '@/Modules/Organization/Domain/Repositories/IOrganizationRepository'
       import type { IOrganizationMemberRepository } from '@/Modules/Organization/Domain/Repositories/IOrganizationMemberRepository'
       import type { IAuthRepository } from '@/Modules/Auth/Domain/Repositories/IAuthRepository'
       import type { IAlertRecipientResolver, AlertRecipientContext } from '../../Domain/Services/IAlertRecipientResolver'

       export class AlertRecipientResolverImpl implements IAlertRecipientResolver {
         constructor(
           private readonly deps: {
             readonly orgRepo: IOrganizationRepository
             readonly orgMemberRepo: IOrganizationMemberRepository
             readonly authRepo: IAuthRepository
           },
         ) {}

         async resolveByOrg(orgId: string): Promise<AlertRecipientContext> {
           const org = await this.deps.orgRepo.findById(orgId)
           const members = await this.deps.orgMemberRepo.findByOrgId(orgId)
           const managers = members.filter((m) => m.isManager())
           const emailSet = new Set<string>()
           for (const manager of managers) {
             const user = await this.deps.authRepo.findById(manager.userId)
             const email = user?.emailValue?.trim()
             if (email) emailSet.add(email)
           }
           return { orgId, orgName: org?.name ?? 'Unknown', emails: [...emailSet] }
         }
       }

       - Confirm method names (`findById`, `findByOrgId`, `isManager`, `emailValue`) match actual Domain interfaces read in `read_first`; adjust if signatures differ
       - If IOrganizationMemberRepository's method has different name (e.g., `listByOrgId`), use the actual name

    3. Create `src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts` (per D-16, partial — only this fake in Plan 2; rest come in Plan 3):

       import type { IAlertRecipientResolver, AlertRecipientContext } from '../../Domain/Services/IAlertRecipientResolver'

       export class InMemoryAlertRecipientResolver implements IAlertRecipientResolver {
         constructor(private readonly store: Record<string, AlertRecipientContext> = {}) {}
         async resolveByOrg(orgId: string): Promise<AlertRecipientContext> {
           return this.store[orgId] ?? { orgId, orgName: 'Unknown', emails: [] }
         }
         setContext(orgId: string, ctx: AlertRecipientContext): void {
           this.store[orgId] = ctx
         }
       }

       - Pure in-memory lookup; no Organization/Auth repo references
       - Must work with `new InMemoryAlertRecipientResolver({ 'org-1': { orgId: 'org-1', orgName: 'Acme', emails: ['a@x.com'] } })` (seeded construction)

    Commit message: `feat: [alerts] 新增 IAlertRecipientResolver port 與實作`
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts` true
    - `test -f src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts` true
    - `test -f src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts` true
    - `grep -c "interface IAlertRecipientResolver" src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts` == 1
    - `grep -c "class AlertRecipientResolverImpl" src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts` == 1
    - `grep -c "class InMemoryAlertRecipientResolver" src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts` == 1
    - Domain port has ZERO Infrastructure imports: `grep -E "from '@/(Foundation|Shared/Infrastructure)'" src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts` returns empty
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    IAlertRecipientResolver port defined, concrete impl composes the 3 cross-module repos, in-memory fake available for DI-less tests. Not yet wired into SendAlertService — that's Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor SendAlertService and EvaluateThresholdsService to object-literal ctor; SendAlertService consumes IAlertRecipientResolver</name>
  <files>
    src/Modules/Alerts/Application/Services/SendAlertService.ts
    src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts
    src/Modules/Alerts/__tests__/SendAlertService.test.ts
    src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (full — 118 lines; see current 7 positional args + the inline Org/OrgMember/Auth composition logic that must move into resolver)
    - src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts (full — 121 lines)
    - src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts (reference: already uses object-literal ctor — mirror this style)
    - src/Modules/Alerts/Application/Services/ResendDeliveryService.ts (reference: already uses object-literal ctor)
    - src/Modules/Alerts/__tests__/SendAlertService.test.ts (current stub shape to replace)
    - src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts (current stub shape to replace)
    - src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts (created Task 1)
    - src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts (created Task 1)
    - .planning/phases/19-alerts-module-decoupling/19-RESEARCH.md Pattern 2 section (SendAlertService target surface shown)
  </read_first>
  <action>
    1. Refactor `src/Modules/Alerts/Application/Services/SendAlertService.ts` (per D-05 + D-18):
       - Replace 7-positional-arg constructor with object-literal:
         constructor(
           private readonly deps: {
             readonly recipientResolver: IAlertRecipientResolver
             readonly alertEventRepo: IAlertEventRepository
             readonly deliveryRepo: IAlertDeliveryRepository
             readonly mailer: IMailer
             readonly templates: AlertEmailTemplates   // kept for now; Plan 3 moves this inside EmailAlertNotifier
             readonly dispatchWebhooksService: DispatchAlertWebhooksService  // kept for now; Plan 3 replaces with notifiers
           },
         ) {}
       - Remove imports of IOrganizationRepository, IOrganizationMemberRepository, IAuthRepository from this file
       - In `send()`, replace the inline `org + members + manager-emails` composition with a single call: `const context = await this.deps.recipientResolver.resolveByOrg(params.orgId)`
       - Use `context.emails` and `context.orgName` throughout the rest of the method body (replacing whatever field names existed before)
       - Preserve all existing behavior: alertEventRepo.save, per-email dedup via deliveryRepo.existsSent, mailer.send + AlertDelivery row, webhook dispatch fire-and-forget (research confirms this must be preserved — do NOT await the webhook promise if current code didn't)
       - Update internal field access from `this.xxx` positional fields → `this.deps.xxx`

       NOTE: The Plan-3 final shape (object with `notifiers: readonly IAlertNotifier[]`) comes later; Plan 2 keeps mailer/templates/dispatchWebhooksService as explicit deps to minimize blast radius. This is intentional per the CONTEXT split — Plan 3 collapses them.

    2. Refactor `src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts` (per D-07 + D-18):
       - Per D-07, do NOT change the actual deps (IUsageRepository + IApiKeyRepository + others stay). ONLY change ctor signature to object-literal:
         constructor(
           private readonly deps: {
             readonly usageRepo: IUsageRepository
             readonly apiKeyRepo: IApiKeyRepository
             readonly alertConfigRepo: IAlertConfigRepository
             readonly sendAlertService: SendAlertService
             // ...any other existing deps
           },
         ) {}
       - Update internal field access from positional fields → `this.deps.xxx`
       - No semantic behavior changes

    3. Update `src/Modules/Alerts/__tests__/SendAlertService.test.ts` (per D-17):
       - Replace the hand-built `vi.fn()` stubs for Org/OrgMember/Auth repos with:
         const recipientResolver = new InMemoryAlertRecipientResolver({ 'org-1': { orgId: 'org-1', orgName: 'Acme', emails: ['a@x.com', 'b@x.com'] } })
       - Update test's `new SendAlertService(...)` call from positional args to `new SendAlertService({ recipientResolver, alertEventRepo, deliveryRepo, mailer, templates, dispatchWebhooksService })`
       - Keep other stubs (alertEventRepo, deliveryRepo, mailer, templates, dispatchWebhooksService) as existing vi.fn() patterns for now — full InMemory* fakes come in Plan 3
       - All existing assertions should pass after ctor migration

    4. Update `src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts`:
       - Update `new EvaluateThresholdsService(...)` from positional to object-literal construction
       - Keep existing stub implementations

    Commit message: `refactor: [alerts] SendAlertService 改用 IAlertRecipientResolver + object-literal ctor`
  </action>
  <verify>
    <automated>pnpm vitest run src/Modules/Alerts/__tests__/SendAlertService.test.ts src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IOrganizationRepository\|IOrganizationMemberRepository\|IAuthRepository" src/Modules/Alerts/Application/Services/SendAlertService.ts` == 0
    - `grep -c "IAlertRecipientResolver" src/Modules/Alerts/Application/Services/SendAlertService.ts` >= 1
    - `grep -c "private readonly deps:" src/Modules/Alerts/Application/Services/SendAlertService.ts` == 1
    - `grep -c "private readonly deps:" src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts` == 1
    - Tests pass: `pnpm vitest run src/Modules/Alerts/__tests__/SendAlertService.test.ts src/Modules/Alerts/__tests__/EvaluateThresholdsService.test.ts`
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    SendAlertService no longer directly depends on Org/OrgMember/Auth — only on IAlertRecipientResolver. Both services use object-literal ctor. Tests updated and passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire AlertRecipientResolverImpl in AlertsServiceProvider; write MODULE.md</name>
  <files>
    src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
    src/Modules/Alerts/MODULE.md
  </files>
  <read_first>
    - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts (current 181-line file post-Plan 1; see SendAlertService binding block in particular)
    - src/Modules/Alerts/Infrastructure/Services/AlertRecipientResolverImpl.ts (Task 1 output)
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (Task 2 output — new ctor shape)
    - src/Modules/Alerts/Application/Services/EvaluateThresholdsService.ts (Task 2 output)
    - Result of `grep -rn "from '@/Modules/\(Organization\|Auth\|Dashboard\|ApiKey\)'" src/Modules/Alerts/` (every cross-module import — this forms MODULE.md contract)
    - .planning/phases/18-uniform-background-jobs/18-CONTEXT.md (explain why Alerts event subscription is not on registerJobs hook — D-08 rationale for MODULE.md)
    - .planning/phases/19-alerts-module-decoupling/19-CONTEXT.md <decisions> block (D-05..D-09)
  </read_first>
  <action>
    1. Update `src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts`:
       - Add import: `import { AlertRecipientResolverImpl } from '../Services/AlertRecipientResolverImpl'`
       - Add import: `import type { IAlertRecipientResolver } from '../../Domain/Services/IAlertRecipientResolver'`
       - Add new binding (singleton — stateless):
         container.singleton('alertRecipientResolver', (c: IContainer) => new AlertRecipientResolverImpl({
           orgRepo: c.make('organizationRepository') as IOrganizationRepository,
           orgMemberRepo: c.make('organizationMemberRepository') as IOrganizationMemberRepository,
           authRepo: c.make('authRepository') as IAuthRepository,
         }))
         (Confirm exact binding names for Org/OrgMember/Auth repos by grepping their respective ServiceProvider files during read_first)
       - Update SendAlertService binding to the new object-literal ctor shape; replace the old Org/OrgMember/Auth injections with the single `recipientResolver: c.make('alertRecipientResolver')`
       - Update EvaluateThresholdsService binding to match the new object-literal shape (same deps as before, just wrapped)
       - Remove unused imports of IOrganizationRepository / IOrganizationMemberRepository / IAuthRepository IF no other binding in this provider uses them. If `alertRecipientResolver` binding uses them (typical), keep only those imports.
       - Preserve boot() `DomainEventDispatcher.on('bifrost.sync.completed', ...)` wiring (D-08: do not change).

    2. Create `src/Modules/Alerts/MODULE.md` (per D-09) with these sections (繁體中文；短而精確；一眼可審查):

       # Alerts Module — 跨模組依賴契約

       ## 背景
       (one-paragraph: Alerts 模組聚焦預算監控與通知分派；任何跨模組依賴必須在此記錄理由)

       ## 使用的跨模組 Port
       | Port | 擁有方 | 使用位置 | 用途 | 評估 |
       |------|--------|---------|------|------|
       | IAlertRecipientResolver → (內部 compose) IOrganizationRepository | Organization | AlertRecipientResolverImpl | 查詢 org 名稱 | Domain Repository port，ALERTS-02 合規（抽象契約） |
       | IAlertRecipientResolver → IOrganizationMemberRepository | Organization | AlertRecipientResolverImpl | 查詢 manager 成員 | Domain Repository port |
       | IAlertRecipientResolver → IAuthRepository | Auth | AlertRecipientResolverImpl | 由 user id 取得 email | Domain Repository port |
       | IUsageRepository | Dashboard | EvaluateThresholdsService | 查詢月度用量 | Application Port (Phase 17)；D-07 認定合規無需再包裝 |
       | IApiKeyRepository | ApiKey | EvaluateThresholdsService | 查詢 key breakdown | Domain Repository port |
       | IMailer | Foundation | (Plan 3: EmailAlertNotifier) | 發送警報 Email | Foundation infrastructure port |
       | IWebhookDispatcher | Foundation | (Plan 3: WebhookAlertNotifier) | 派送 webhook | Foundation infrastructure port |

       ## 跨模組事件訂閱
       - `bifrost.sync.completed`（DomainEventDispatcher，Shared Domain）
         - 訂閱位置：`AlertsServiceProvider.boot()`
         - 觸發邏輯：EvaluateThresholdsService.execute()
         - Why NOT registerJobs hook (Phase 18)：此為「事件驅動」而非「定時」；Phase 18 的 IScheduler 抽象只涵蓋 cron/interval 任務（JOBS-01 範圍）。事件訂閱保留在 boot() 是刻意設計（per D-08）

       ## 不依賴的項目（明確聲明）
       - Alerts Application layer 不直接 import 任何 Organization/Auth/Dashboard/ApiKey 模組的 Entity / VO / Service（只透過 Domain Repository port 間接接觸 Entity）
       - Alerts 不依賴 HTTP / Presentation 層外部細節

       ## 審查規則
       - 新增跨模組依賴必須更新本檔案
       - 新增的跨模組 port 應優先考慮是否應抽象成 Alerts-owned port（如 `IAlertRecipientResolver` 示範）再直接使用上游模組 port

    Commit message: `docs: [alerts] 新增 MODULE.md 記錄跨模組依賴契約；wire AlertRecipientResolver`
  </action>
  <verify>
    <automated>pnpm vitest run src/Modules/Alerts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/Modules/Alerts/MODULE.md` true
    - `grep -c "IAlertRecipientResolver\|IOrganizationRepository\|IOrganizationMemberRepository\|IAuthRepository\|IUsageRepository\|IApiKeyRepository\|IMailer\|IWebhookDispatcher" src/Modules/Alerts/MODULE.md` >= 7 (all 7+ listed ports mentioned)
    - `grep -c "registerJobs\|Phase 18" src/Modules/Alerts/MODULE.md` >= 1 (D-08 rationale documented)
    - `grep -c "alertRecipientResolver" src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts` >= 2 (binding registration + usage in SendAlertService)
    - `grep -c "class AlertRecipientResolverImpl" src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts` == 0 (class itself is imported from Services/, not redeclared)
    - Full Alerts tests green: `pnpm vitest run src/Modules/Alerts`
    - `pnpm tsc --noEmit` green
    - Manual verification item (logged in 19-VALIDATION.md): cross-reference MODULE.md vs actual imports by running `grep -rn "from '@/Modules/\(Organization\|Auth\|Dashboard\|ApiKey\)'" src/Modules/Alerts` and confirming every hit corresponds to an entry in MODULE.md
  </acceptance_criteria>
  <done>
    AlertRecipientResolver wired into container. SendAlertService and EvaluateThresholdsService use the new ctor shape end-to-end. MODULE.md serves as the ALERTS-02 audit contract. Alerts module integration intact.
  </done>
</task>

</tasks>

<verification>
Phase-scope checks after Plan 2:
- SendAlertService imports: `grep "from '@/Modules/\(Organization\|Auth\)'" src/Modules/Alerts/Application/Services/SendAlertService.ts` returns empty
- EvaluateThresholdsService ctor: object-literal confirmed
- MODULE.md exists and matches actual import graph
- All 13 existing Alerts tests remain green
- `pnpm vitest run` full suite green
- `pnpm tsc --noEmit` green
</verification>

<success_criteria>
- ALERTS-02 satisfied: Alerts Application layer does not reference implementation details of Organization/Auth — only the Alerts-owned `IAlertRecipientResolver` port
- D-18 ctor unification applied to SendAlertService + EvaluateThresholdsService
- MODULE.md published and lists every cross-module port
- All existing Alerts tests green; 2 test files updated for new ctor shape
- Plan 3 can proceed with clean object-literal SendAlertService ctor to collapse into `notifiers: readonly IAlertNotifier[]`
</success_criteria>

<output>
After completion, create `.planning/phases/19-alerts-module-decoupling/19-02-SUMMARY.md` with:
- Files created/modified
- Confirmed cross-module imports after refactor (grep output summary)
- Note any method-name mismatches encountered vs the research example (e.g., `findByOrgId` vs `listByOrgId`)
- MODULE.md final content outline
</output>
