---
phase: 19-alerts-module-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Shared/Infrastructure/Database/Adapters/Drizzle/schema.ts
  - drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql
  - src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts
  - src/Modules/Alerts/Infrastructure/Repositories/AlertEventRepository.ts
  - src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts
  - src/Modules/Alerts/Infrastructure/Repositories/WebhookEndpointRepository.ts
  - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
autonomous: true
requirements:
  - ALERTS-01
  - ALERTS-05
user_setup: []

must_haves:
  truths:
    - "src/Modules/Alerts/ contains zero `import ... from 'drizzle-orm'` statements (excluding .test.ts)"
    - "All 4 Alerts repos follow Phase 17 IDatabaseAccess + IQueryBuilder pattern (mirrors DrizzleAlertConfigRepository style)"
    - "DI bindings use non-prefixed names: alertConfigRepository, alertEventRepository, alertDeliveryRepository, webhookEndpointRepository"
    - "alert_deliveries table has org_id, month, tier columns populated for all existing rows"
    - "existsSent and listByOrg work WITHOUT JOIN (pure single-table queries)"
    - "pnpm vitest run src/Modules/Alerts passes green"
  artifacts:
    - path: "src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts"
      provides: "Phase 17-style CRUD repo (renamed from DrizzleAlertConfigRepository)"
    - path: "src/Modules/Alerts/Infrastructure/Repositories/AlertEventRepository.ts"
      provides: "Phase 17-style repo replacing DrizzleAlertEventRepository"
    - path: "src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts"
      provides: "Phase 17-style repo; uses denormalized org_id/month/tier columns (no JOIN)"
    - path: "src/Modules/Alerts/Infrastructure/Repositories/WebhookEndpointRepository.ts"
      provides: "Phase 17-style repo replacing DrizzleWebhookEndpointRepository"
    - path: "drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql"
      provides: "Denormalization migration: add org_id/month/tier to alert_deliveries + backfill"
  key_links:
    - from: "AlertsServiceProvider.register"
      to: "Repository classes"
      via: "container.singleton('alertConfigRepository' | 'alertEventRepository' | 'alertDeliveryRepository' | 'webhookEndpointRepository', ...)"
      pattern: "container\\.singleton\\('alert(Config|Event|Delivery)Repository'"
    - from: "AlertController / WebhookEndpointController / alert-related services"
      to: "Container binding names"
      via: "c.make('alertConfigRepository') etc. (no drizzle prefix)"
      pattern: "c\\.make\\('(alertConfig|alertEvent|alertDelivery|webhookEndpoint)Repository'\\)"
---

<objective>
將 Alerts 模組的 4 個 repository 從 Drizzle 直接耦合遷移到 Phase 17 的 IDatabaseAccess + IQueryBuilder 模式；移除 DI binding 的 `drizzle` 前綴；透過 denormalize `alert_deliveries` 的 org_id/month/tier 欄位消除 JOIN 依賴（per D-01, D-02, D-03, D-04 + research recommendation）。

Purpose: 完成 ALERTS-01（ORM-agnostic 模式）與 ALERTS-05 ORM 部分（delivery log 統一化），建立 Plan 2/3 需要的乾淨 repo 基礎。
Output: 4 個重命名後的 repo 檔案（無 `Drizzle` 前綴）、1 個 SQL migration、更新的 AlertsServiceProvider、更新的 schema.ts、全部 binding consumer 同步更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-alerts-module-decoupling/19-CONTEXT.md
@.planning/phases/19-alerts-module-decoupling/19-RESEARCH.md

@src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertConfigRepository.ts
@src/Modules/Dashboard/Infrastructure/Repositories/DrizzleUsageRepository.ts
@src/Modules/Dashboard/Application/Ports/IUsageRepository.ts
@src/Shared/Infrastructure/IDatabaseAccess.ts
@src/Modules/Alerts/Domain/Repositories/IAlertConfigRepository.ts
@src/Modules/Alerts/Domain/Repositories/IAlertEventRepository.ts
@src/Modules/Alerts/Domain/Repositories/IAlertDeliveryRepository.ts
@src/Modules/Alerts/Domain/Repositories/IWebhookEndpointRepository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Denormalize alert_deliveries schema + backfill migration</name>
  <files>
    src/Shared/Infrastructure/Database/Adapters/Drizzle/schema.ts
    drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql
  </files>
  <read_first>
    - src/Shared/Infrastructure/Database/Adapters/Drizzle/schema.ts (read alertDeliveries table definition around line 260-285, and alertEvents definition for reference column types)
    - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertDeliveryRepository.ts (understand current innerJoin usage on lines 47+71)
    - drizzle/migrations/ (list existing migrations to match naming convention and style)
    - .planning/phases/19-alerts-module-decoupling/19-RESEARCH.md Pitfall 1 section (denormalization path)
  </read_first>
  <action>
    1. Update `src/Shared/Infrastructure/Database/Adapters/Drizzle/schema.ts`: in the `alertDeliveries` table definition, add three new columns AFTER the existing `channel` column:
       - `org_id: text('org_id').notNull()`
       - `month: text('month').notNull()`
       - `tier: text('tier').notNull()`
       Keep all existing columns unchanged. Add a composite index on `(org_id, month, tier)` matching the existsSent query shape.

    2. Create `drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql` with the SQL equivalent. Since SQLite cannot add NOT NULL column directly, use this 3-step pattern (per research Pitfall 1):

       -- Step 1: add nullable columns
       ALTER TABLE alert_deliveries ADD COLUMN org_id TEXT;
       ALTER TABLE alert_deliveries ADD COLUMN month TEXT;
       ALTER TABLE alert_deliveries ADD COLUMN tier TEXT;

       -- Step 2: backfill from alert_events JOIN
       UPDATE alert_deliveries SET org_id = (SELECT org_id FROM alert_events WHERE alert_events.id = alert_deliveries.alert_event_id), month = (SELECT month FROM alert_events WHERE alert_events.id = alert_deliveries.alert_event_id), tier = (SELECT tier FROM alert_events WHERE alert_events.id = alert_deliveries.alert_event_id);

       -- Step 3: composite index
       CREATE INDEX IF NOT EXISTS idx_alert_deliveries_org_month_tier ON alert_deliveries(org_id, month, tier);

       NOT NULL enforcement is app-side via Drizzle schema (Step 1 columns stay nullable at DB level; Step 2 fills all existing rows; future writes driven by Drizzle types).

    3. If `AlertDelivery` entity (`src/Modules/Alerts/Domain/Entities/AlertDelivery.ts`) or its mapper exposes `toInsert()` or factory methods, extend them so new inserts include `org_id/month/tier`. These values originate from the parent `AlertEvent` at write time. For Plan 1, ensure insert path accepts them; actual threading from SendAlertService is finalized in Plan 3 (notifiers).

    Commit message: `refactor: [alerts] denormalize alert_deliveries org_id/month/tier columns`
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep result: schema.ts contains the three new column declarations in alertDeliveries — `grep -n "org_id\|month\|tier" src/Shared/Infrastructure/Database/Adapters/Drizzle/schema.ts` includes matches within the alertDeliveries block
    - `test -f drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql` returns true
    - `grep -c "ALTER TABLE alert_deliveries ADD COLUMN" drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql` == 3
    - `grep -c "UPDATE alert_deliveries" drizzle/migrations/2026_04_13_add_alert_deliveries_denorm.sql` == 1
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    alert_deliveries schema extended with org_id/month/tier. Migration file ready to run against existing DB. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 4 Alerts repos using IDatabaseAccess; drop Drizzle prefix from filenames and class names</name>
  <files>
    src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/AlertEventRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/WebhookEndpointRepository.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertConfigRepository.ts (already Phase 17-compliant — serves as in-module template)
    - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertEventRepository.ts (current Drizzle implementation to port)
    - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertDeliveryRepository.ts (lines 47+71 contain innerJoin calls to eliminate)
    - src/Modules/Alerts/Infrastructure/Repositories/DrizzleWebhookEndpointRepository.ts (onConflictDoUpdate on upsert — map to IQueryBuilder equivalent)
    - src/Modules/Dashboard/Infrastructure/Repositories/DrizzleUsageRepository.ts (canonical Phase 17 reference from Phase 17)
    - src/Modules/Alerts/Domain/Repositories/IAlertConfigRepository.ts
    - src/Modules/Alerts/Domain/Repositories/IAlertEventRepository.ts
    - src/Modules/Alerts/Domain/Repositories/IAlertDeliveryRepository.ts
    - src/Modules/Alerts/Domain/Repositories/IWebhookEndpointRepository.ts
    - src/Shared/Infrastructure/IDatabaseAccess.ts (IQueryBuilder method list)
    - src/Shared/Infrastructure/Database/Adapters/Drizzle/DrizzleQueryBuilder.ts (confirm available primitives: where, whereBetween, first, select, insert, insertOrIgnore, update, delete, orderBy, limit, offset, count, aggregate — NO join)
  </read_first>
  <action>
    Goal: Produce 4 new repository files WITHOUT `Drizzle` prefix. Keep the old `Drizzle*Repository.ts` files in place until Task 3 rewires the provider, then delete them in Task 3.

    1. Create `AlertConfigRepository.ts`:
       - DrizzleAlertConfigRepository is already Phase 17-compliant — this is essentially a file rename + class rename
       - Copy full content into the new file
       - Rename class `DrizzleAlertConfigRepository` → `AlertConfigRepository`
       - Keep `constructor(private readonly db: IDatabaseAccess)`
       - Confirm no `drizzle-orm` imports remain

    2. Create `AlertEventRepository.ts`:
       - `export class AlertEventRepository implements IAlertEventRepository`
       - Constructor: `constructor(private readonly db: IDatabaseAccess) {}`
       - Replace every `drizzle-orm` call with `this.db.table('alert_events').where(...).first()/select()/orderBy()/limit()/offset()/insert()/update()/count()`
       - Mirror every method present in DrizzleAlertEventRepository (save/findById/findByOrgAndMonth/listByOrg/etc.)
       - Map rows back via `AlertEvent.fromDatabase(row)` in select paths
       - ZERO `import ... from 'drizzle-orm'`

    3. Create `AlertDeliveryRepository.ts`:
       - `export class AlertDeliveryRepository implements IAlertDeliveryRepository`
       - Constructor: `constructor(private readonly db: IDatabaseAccess) {}`
       - `existsSent()` — single-table query against NEW denormalized columns (no JOIN):
         const count = await this.db.table('alert_deliveries').where('org_id', '=', orgId).where('month', '=', month).where('tier', '=', tier).where('channel', '=', channel).where('target', '=', target).where('status', '=', 'sent').count(); return count > 0;
       - `listByOrg()` — single-table query:
         const rows = await this.db.table('alert_deliveries').where('org_id', '=', orgId).orderBy('created_at', 'DESC').limit(opts?.limit ?? 50).offset(opts?.offset ?? 0).select(); return rows.map(AlertDelivery.fromDatabase);
       - `save()` must include `org_id/month/tier` in insert payload. Thread from the AlertDelivery entity (Task 1 ensured entity/mapper supports these fields)
       - Any other methods from the original repo must be ported one-for-one
       - ZERO `import ... from 'drizzle-orm'`, ZERO `innerJoin`

    4. Create `WebhookEndpointRepository.ts`:
       - `export class WebhookEndpointRepository implements IWebhookEndpointRepository`
       - Constructor: `constructor(private readonly db: IDatabaseAccess) {}`
       - Replace every `drizzle-orm` call with IQueryBuilder primitives
       - For any `onConflictDoUpdate` or upsert: use `insertOrIgnore` first; if a row matched (no insert), follow with `.where(...).update(...)`. If a clean upsert primitive is needed and not present, use a find-then-create-or-update two-step approach
       - Mirror every method (findById/findActiveByOrg/create/update/delete/etc.)
       - ZERO `import ... from 'drizzle-orm'`

    5. DO NOT delete old `Drizzle*Repository.ts` files — Task 3 handles that atomically with consumer updates.

    Commit message: `refactor: [alerts] 重構 4 個 Alerts repos 為 Phase 17 IDatabaseAccess 模式`
  </action>
  <verify>
    <automated>test -z "$(grep -rln \"from 'drizzle-orm'\" src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts src/Modules/Alerts/Infrastructure/Repositories/AlertEventRepository.ts src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts src/Modules/Alerts/Infrastructure/Repositories/WebhookEndpointRepository.ts 2>/dev/null)" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new files exist: `test -f src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts` etc.
    - `grep -l "from 'drizzle-orm'" src/Modules/Alerts/Infrastructure/Repositories/AlertConfigRepository.ts src/Modules/Alerts/Infrastructure/Repositories/AlertEventRepository.ts src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts src/Modules/Alerts/Infrastructure/Repositories/WebhookEndpointRepository.ts` returns empty
    - `grep -c "innerJoin\|leftJoin" src/Modules/Alerts/Infrastructure/Repositories/AlertDeliveryRepository.ts` == 0
    - `grep "class AlertConfigRepository\|class AlertEventRepository\|class AlertDeliveryRepository\|class WebhookEndpointRepository" src/Modules/Alerts/Infrastructure/Repositories/` — 4 matches across the new files
    - `pnpm tsc --noEmit` green (4 new repos type-check; old repos still present as orphans — handled Task 3)
  </acceptance_criteria>
  <done>
    4 new repo files exist with Phase 17 IDatabaseAccess pattern. Zero drizzle-orm imports in new files. AlertDeliveryRepository uses denormalized columns (no JOIN). Old Drizzle* files still exist temporarily.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewire AlertsServiceProvider bindings (drop drizzle prefix); update all consumers; delete old Drizzle* repo files</name>
  <files>
    src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
    src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertConfigRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertEventRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertDeliveryRepository.ts
    src/Modules/Alerts/Infrastructure/Repositories/DrizzleWebhookEndpointRepository.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts (full file — 181 lines; see current bindings + all c.make lookups)
    - src/Modules/Alerts/__tests__/ (list of test files — some reference binding names directly)
    - Result of `grep -rn "drizzleAlertConfigRepository\|drizzleAlertEventRepository" src/` (find every consumer to update)
    - Result of `grep -rn "DrizzleAlertConfigRepository\|DrizzleAlertEventRepository\|DrizzleAlertDeliveryRepository\|DrizzleWebhookEndpointRepository" src/` (find every import of old class names)
  </read_first>
  <action>
    1. Update `src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts`:
       - Replace imports of old classes with new ones:
         - `import { DrizzleAlertConfigRepository } from '../Repositories/DrizzleAlertConfigRepository'` → `import { AlertConfigRepository } from '../Repositories/AlertConfigRepository'`
         - Same pattern for AlertEvent, AlertDelivery, WebhookEndpoint
       - Rename binding keys per D-02:
         - `drizzleAlertConfigRepository` → `alertConfigRepository`
         - `drizzleAlertEventRepository` → `alertEventRepository`
         - `alertDeliveryRepository` → `alertDeliveryRepository` (already clean, just change class instantiation to `new AlertDeliveryRepository(db)`)
         - `webhookEndpointRepository` → `webhookEndpointRepository` (already clean, change class instantiation to `new WebhookEndpointRepository(db)`)
       - Update every `c.make('drizzleAlertConfigRepository')` → `c.make('alertConfigRepository')` inside this file
       - Update every `c.make('drizzleAlertEventRepository')` → `c.make('alertEventRepository')` inside this file
       - Preserve all other bindings (services, controllers, boot() subscription wiring — D-08 says keep DomainEventDispatcher wiring untouched)

    2. Search-and-update all other consumers project-wide:
       - Run equivalent of `grep -rln "drizzleAlertConfigRepository\|drizzleAlertEventRepository" src/` (excluding .test.ts if binding names don't appear there; include if they do)
       - Replace each occurrence: `drizzleAlertConfigRepository` → `alertConfigRepository`, `drizzleAlertEventRepository` → `alertEventRepository`
       - Update any direct `import { DrizzleAlert...Repository }` imports to the new class names/files
       - Check Alerts test files (13 existing) — if they reference the old class name in imports, update. If they only reference the interface `IAlertConfigRepository`, no change needed.

    3. Delete the 4 old files (now unreferenced):
       - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertConfigRepository.ts
       - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertEventRepository.ts
       - src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertDeliveryRepository.ts
       - src/Modules/Alerts/Infrastructure/Repositories/DrizzleWebhookEndpointRepository.ts

    4. Run full test suite. Expected: all 13 existing Alerts tests still pass (interface signatures unchanged). If any fail due to binding-name references, update the test.

    Commit message: `refactor: [alerts] DI bindings 去除 drizzle 前綴並刪除舊 Drizzle*Repository 檔案`
  </action>
  <verify>
    <automated>pnpm vitest run src/Modules/Alerts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Old files deleted: `test ! -f src/Modules/Alerts/Infrastructure/Repositories/DrizzleAlertConfigRepository.ts` (and same for the other 3)
    - `grep -rln "drizzleAlertConfigRepository\|drizzleAlertEventRepository" src/` returns empty
    - `grep -rln "DrizzleAlertConfigRepository\|DrizzleAlertEventRepository\|DrizzleAlertDeliveryRepository\|DrizzleWebhookEndpointRepository" src/` returns empty
    - `grep -c "container.singleton('alertConfigRepository'" src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts` == 1
    - `grep -c "container.singleton('alertEventRepository'" src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts` == 1
    - Full module-scope tests pass: `pnpm vitest run src/Modules/Alerts`
    - `pnpm tsc --noEmit` green
    - Global check: `grep -rln "from 'drizzle-orm'" src/Modules/Alerts/ | grep -v '.test.ts'` returns empty
  </acceptance_criteria>
  <done>
    AlertsServiceProvider wires new repos under non-prefixed binding names. All consumers updated. Old Drizzle*Repository files deleted. Alerts module is entirely ORM-agnostic at the Infrastructure/Repositories boundary. All existing tests green.
  </done>
</task>

</tasks>

<verification>
Phase-scope checks after all tasks complete:
- `grep -rln "from 'drizzle-orm'" src/Modules/Alerts/ | grep -v '.test.ts'` returns empty (ALERTS-01 satisfied)
- `grep -rln "drizzle.*Repository" src/` (case-sensitive) returns empty outside .planning/
- `pnpm vitest run src/Modules/Alerts` green
- `pnpm vitest run` (full suite) green
- `pnpm tsc --noEmit` green
</verification>

<success_criteria>
- 4 Alerts repos successfully migrated to Phase 17 IDatabaseAccess pattern
- DI bindings renamed per D-02
- alert_deliveries denormalization migration created and schema updated
- existsSent/listByOrg work via single-table queries (zero JOIN)
- ALERTS-01 satisfied (repositories ORM-agnostic)
- ALERTS-05 partially advanced (delivery-log schema readiness for email rows — full satisfaction comes in Plan 3 when EmailAlertNotifier writes rows)
- All existing Alerts tests (13) remain green
</success_criteria>

<output>
After completion, create `.planning/phases/19-alerts-module-decoupling/19-01-SUMMARY.md` with:
- Files created/modified/deleted
- Any unexpected issues with IQueryBuilder primitives during port
- Confirmation of denormalization migration (and whether it was run against dev DB)
- AtlasQueryBuilder touches, if any (folded todo tracking)
</output>
