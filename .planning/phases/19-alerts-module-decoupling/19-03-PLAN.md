---
phase: 19-alerts-module-decoupling
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/Modules/Alerts/Domain/Services/IAlertNotifier.ts
  - src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts
  - src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts
  - src/Modules/Alerts/Application/Services/SendAlertService.ts
  - src/Modules/Alerts/Application/Services/ResendDeliveryService.ts
  - src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts
  - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
  - src/Modules/Alerts/__tests__/fakes/InMemoryAlertConfigRepository.ts
  - src/Modules/Alerts/__tests__/fakes/InMemoryAlertEventRepository.ts
  - src/Modules/Alerts/__tests__/fakes/InMemoryAlertDeliveryRepository.ts
  - src/Modules/Alerts/__tests__/fakes/InMemoryWebhookEndpointRepository.ts
  - src/Modules/Alerts/__tests__/fakes/FakeAlertNotifier.ts
  - src/Modules/Alerts/__tests__/SendAlertService.test.ts
  - src/Modules/Alerts/__tests__/ResendDeliveryService.test.ts
  - src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts
  - src/Modules/Alerts/__tests__/WebhookAlertNotifier.test.ts
  - src/Modules/Alerts/__tests__/EmailAlertNotifier.test.ts
  - src/Modules/Alerts/MODULE.md
autonomous: true
requirements:
  - ALERTS-03
  - ALERTS-04
  - ALERTS-05
user_setup: []

must_haves:
  truths:
    - "IAlertNotifier port defined in Alerts Domain with two concrete impls (EmailAlertNotifier, WebhookAlertNotifier)"
    - "SendAlertService holds readonly notifiers: readonly IAlertNotifier[] and dispatches via Promise.allSettled"
    - "ResendDeliveryService routes via notifierRegistry keyed by delivery.channel"
    - "DispatchAlertWebhooksService file deleted; its logic lives inside WebhookAlertNotifier"
    - "Email deliveries now persist in alert_deliveries with channel='email' (row per recipient email)"
    - "src/Modules/Alerts/__tests__/fakes/ contains 5 InMemory* fakes + FakeAlertNotifier; new tests instantiate services with zero DI container"
    - "Fire-and-forget webhook latency preserved (SendAlertService does not await the webhook notifier promise if original behavior was fire-and-forget)"
  artifacts:
    - path: "src/Modules/Alerts/Domain/Services/IAlertNotifier.ts"
      provides: "Channel-strategy port + AlertPayload DTO + DeliveryResult DTO + DeliveryChannel union"
    - path: "src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts"
      provides: "Absorbs DispatchAlertWebhooksService logic; channel='webhook'"
    - path: "src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts"
      provides: "Extracts email path from SendAlertService; channel='email'; writes alert_deliveries rows"
    - path: "src/Modules/Alerts/__tests__/fakes/"
      provides: "5 InMemory* repos + FakeAlertNotifier for DI-less service tests"
  key_links:
    - from: "SendAlertService.send"
      to: "IAlertNotifier[]"
      via: "Promise.allSettled(this.deps.notifiers.map(n => n.notify(payload)))"
      pattern: "Promise\\.allSettled.*notifiers\\.map"
    - from: "ResendDeliveryService"
      to: "notifierRegistry[delivery.channel]"
      via: "registry lookup keyed by DeliveryChannel"
      pattern: "notifierRegistry\\[delivery\\.channel\\]|notifiers\\.find.*channel"
    - from: "EmailAlertNotifier.notify"
      to: "IAlertDeliveryRepository.save"
      via: "AlertDelivery row with channel='email'"
      pattern: "deliveryRepo\\.save.*channel.*email|AlertDelivery\\.create.*'email'"
    - from: "AlertsServiceProvider"
      to: "notifiers array"
      via: "container.bind('sendAlertService', c => new SendAlertService({ ..., notifiers: [c.make('emailAlertNotifier'), c.make('webhookAlertNotifier')] }))"
      pattern: "notifiers:\\s*\\["
---

<objective>
統一通知管道為 `IAlertNotifier` strategy port，實作 `EmailAlertNotifier` 與 `WebhookAlertNotifier` 兩個 adapter；SendAlertService 改持 `readonly notifiers: IAlertNotifier[]` 並以 `Promise.allSettled` 並行派送；ResendDeliveryService 透過 notifierRegistry 依 channel 重試；email 發送記錄寫入 `alert_deliveries`（channel='email'）完成 delivery log 一致化。建立 `__tests__/fakes/` 共用 InMemory fixtures 讓新 tests 完全不需要 DI container（per D-10..D-19）。

Purpose: 完成 ALERTS-03（DI-less 可測性）、ALERTS-04（Webhook/Email adapter 解耦）、ALERTS-05 最終段（email delivery 一致化進入 alert_deliveries）。
Output: 1 個 Domain port、2 個 Infrastructure notifier、5 個 InMemory fake + 1 FakeAlertNotifier、SendAlertService/ResendDeliveryService 重構、DispatchAlertWebhooksService 檔案刪除、AlertsServiceProvider 綁定更新、相關測試拆分 (WebhookAlertNotifier.test / EmailAlertNotifier.test 新建) + 現有測試更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-alerts-module-decoupling/19-CONTEXT.md
@.planning/phases/19-alerts-module-decoupling/19-RESEARCH.md
@.planning/phases/19-alerts-module-decoupling/19-01-SUMMARY.md
@.planning/phases/19-alerts-module-decoupling/19-02-SUMMARY.md

@src/Modules/Alerts/Application/Services/SendAlertService.ts
@src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts
@src/Modules/Alerts/Application/Services/ResendDeliveryService.ts
@src/Modules/Alerts/Infrastructure/Services/AlertEmailTemplates.ts
@src/Modules/Alerts/Domain/Repositories/IAlertDeliveryRepository.ts
@src/Modules/Alerts/Domain/Repositories/IWebhookEndpointRepository.ts
@src/Modules/Alerts/Domain/Repositories/IAlertEventRepository.ts
@src/Modules/Alerts/Domain/Repositories/IAlertConfigRepository.ts
@src/Modules/Alerts/Domain/Services/IAlertRecipientResolver.ts
@src/Foundation/Infrastructure/Ports/IMailer.ts
@src/Foundation/Infrastructure/Ports/IWebhookDispatcher.ts
@src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
@src/Modules/Alerts/__tests__/SendAlertService.test.ts
@src/Modules/Alerts/__tests__/ResendDeliveryService.test.ts
@src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IAlertNotifier port + AlertPayload/DeliveryResult DTOs; build __tests__/fakes/ scaffolding</name>
  <files>
    src/Modules/Alerts/Domain/Services/IAlertNotifier.ts
    src/Modules/Alerts/__tests__/fakes/InMemoryAlertConfigRepository.ts
    src/Modules/Alerts/__tests__/fakes/InMemoryAlertEventRepository.ts
    src/Modules/Alerts/__tests__/fakes/InMemoryAlertDeliveryRepository.ts
    src/Modules/Alerts/__tests__/fakes/InMemoryWebhookEndpointRepository.ts
    src/Modules/Alerts/__tests__/fakes/FakeAlertNotifier.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Domain/Repositories/IAlertConfigRepository.ts (port methods to implement in fake)
    - src/Modules/Alerts/Domain/Repositories/IAlertEventRepository.ts
    - src/Modules/Alerts/Domain/Repositories/IAlertDeliveryRepository.ts (most complex — filter + pagination)
    - src/Modules/Alerts/Domain/Repositories/IWebhookEndpointRepository.ts
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (current — extract AlertPayload shape from existing mailer.send / dispatch args + resolver context)
    - src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts (webhook payload shape)
    - src/Modules/Alerts/Infrastructure/Services/AlertEmailTemplates.ts (what fields email templates consume — determines AlertPayload completeness)
    - src/Modules/Alerts/Domain/Entities/AlertDelivery.ts (for InMemoryAlertDeliveryRepository backing store type)
    - .planning/phases/19-alerts-module-decoupling/19-RESEARCH.md Pattern 3 & Pattern 4 sections (concrete interface shapes)
    - src/Modules/Alerts/__tests__/fakes/InMemoryAlertRecipientResolver.ts (Plan 2 output — style reference for new fakes)
  </read_first>
  <action>
    1. Create `src/Modules/Alerts/Domain/Services/IAlertNotifier.ts` (per D-10 + research Pattern 3):

       import type { AlertRecipientContext } from './IAlertRecipientResolver'

       export type DeliveryChannel = 'email' | 'webhook'

       export interface AlertKeyBreakdownItem {
         readonly apiKeyId: string
         readonly apiKeyName: string
         readonly costUsd: string
       }

       export interface AlertPayload {
         readonly orgId: string
         readonly orgName: string
         readonly alertEventId: string
         readonly tier: 'warning' | 'critical'
         readonly budgetUsd: string
         readonly actualCostUsd: string
         readonly percentage: string
         readonly month: string
         readonly keyBreakdown: readonly AlertKeyBreakdownItem[]
         readonly emails: readonly string[]
       }

       export interface DeliveryResult {
         readonly channel: DeliveryChannel
         readonly successes: number
         readonly failures: number
       }

       export interface IAlertNotifier {
         readonly channel: DeliveryChannel
         notify(payload: AlertPayload): Promise<DeliveryResult>
       }

       - Pure TypeScript (no zod, per research recommendation)
       - Domain-only; NO imports from Infrastructure / Foundation adapters
       - Exact field names/types should match what SendAlertService currently constructs and what AlertEmailTemplates renders — verify against read_first findings and adjust (e.g., if existing code uses `budgetUsd: Decimal` not `string`, reflect that)

    2. Create `src/Modules/Alerts/__tests__/fakes/InMemoryAlertConfigRepository.ts`:

       export class InMemoryAlertConfigRepository implements IAlertConfigRepository {
         private store = new Map<string, AlertConfig>()
         async findByOrgId(orgId: string): Promise<AlertConfig | null> { return this.store.get(orgId) ?? null }
         async save(config: AlertConfig): Promise<void> { this.store.set(config.orgId, config) }
         // ...mirror every method on IAlertConfigRepository
         all(): AlertConfig[] { return [...this.store.values()] }   // test helper
       }

       Implement every IAlertConfigRepository method. Use simple Map-backed storage (keyed appropriately). Filter/sort: only implement what existing test coverage needs — extension allowed later (planner discretion per D).

    3. Create `src/Modules/Alerts/__tests__/fakes/InMemoryAlertEventRepository.ts`:
       - Mirror every method on IAlertEventRepository
       - Back with `private events: AlertEvent[] = []`
       - Provide `all()` test helper
       - Implement filters (orgId + month + tier) and sort (orderBy created_at DESC) via JS array filter/sort where tests need it

    4. Create `src/Modules/Alerts/__tests__/fakes/InMemoryAlertDeliveryRepository.ts`:
       - Mirror every method on IAlertDeliveryRepository including `existsSent` and `listByOrg`
       - Back with `private rows: AlertDelivery[] = []`
       - `existsSent({ orgId, month, tier, channel, target })` = `this.rows.some(r => r.orgId === orgId && r.month === month && ... && r.status === 'sent')`
       - `listByOrg(orgId, opts)` = slice after sort by createdAt DESC
       - Provide `all()` test helper

    5. Create `src/Modules/Alerts/__tests__/fakes/InMemoryWebhookEndpointRepository.ts`:
       - Mirror every method on IWebhookEndpointRepository
       - Back with Map-by-id storage
       - `findActiveByOrg(orgId)` = filter by orgId + active flag
       - Provide `all()` test helper

    6. Create `src/Modules/Alerts/__tests__/fakes/FakeAlertNotifier.ts`:

       import type { IAlertNotifier, AlertPayload, DeliveryResult, DeliveryChannel } from '../../Domain/Services/IAlertNotifier'

       export class FakeAlertNotifier implements IAlertNotifier {
         readonly calls: AlertPayload[] = []
         private forcedResult?: DeliveryResult
         constructor(readonly channel: DeliveryChannel) {}
         async notify(payload: AlertPayload): Promise<DeliveryResult> {
           this.calls.push(payload)
           return this.forcedResult ?? { channel: this.channel, successes: payload.emails.length, failures: 0 }
         }
         forceResult(r: DeliveryResult): void { this.forcedResult = r }
       }

    Commit message: `feat: [alerts] 新增 IAlertNotifier port 與 InMemory test fakes`
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 6 files exist: IAlertNotifier.ts + 5 fakes
    - `grep -c "interface IAlertNotifier" src/Modules/Alerts/Domain/Services/IAlertNotifier.ts` == 1
    - `grep -c "interface AlertPayload" src/Modules/Alerts/Domain/Services/IAlertNotifier.ts` == 1
    - Domain port clean: `grep -E "from '@/(Foundation|Shared/Infrastructure)'" src/Modules/Alerts/Domain/Services/IAlertNotifier.ts` returns empty
    - Each fake: `grep -c "class InMemory" src/Modules/Alerts/__tests__/fakes/InMemory*.ts` — 4 matches across 4 files
    - `grep -c "class FakeAlertNotifier" src/Modules/Alerts/__tests__/fakes/FakeAlertNotifier.ts` == 1
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    IAlertNotifier port + DTOs defined. 5 InMemory* fakes + FakeAlertNotifier ready for use. No behavior wired yet — Task 2 implements notifiers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement WebhookAlertNotifier + EmailAlertNotifier; relocate logic; write notifier tests</name>
  <files>
    src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts
    src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts
    src/Modules/Alerts/__tests__/WebhookAlertNotifier.test.ts
    src/Modules/Alerts/__tests__/EmailAlertNotifier.test.ts
  </files>
  <read_first>
    - src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts (full — logic relocates into WebhookAlertNotifier)
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (full — email-path logic currently inline, relocates into EmailAlertNotifier)
    - src/Modules/Alerts/Infrastructure/Services/AlertEmailTemplates.ts (full — used by EmailAlertNotifier)
    - src/Modules/Alerts/Domain/Services/IAlertNotifier.ts (Task 1 output)
    - src/Foundation/Infrastructure/Ports/IMailer.ts
    - src/Foundation/Infrastructure/Ports/IWebhookDispatcher.ts
    - src/Modules/Alerts/Domain/Repositories/IAlertDeliveryRepository.ts
    - src/Modules/Alerts/Domain/Repositories/IWebhookEndpointRepository.ts
    - src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts (assertions to migrate into WebhookAlertNotifier.test.ts)
    - src/Modules/Alerts/__tests__/SendAlertService.test.ts (email assertions to migrate into EmailAlertNotifier.test.ts)
  </read_first>
  <action>
    1. Create `src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts` (per D-11 + D-12):

       export class WebhookAlertNotifier implements IAlertNotifier {
         readonly channel: DeliveryChannel = 'webhook'
         constructor(
           private readonly deps: {
             readonly dispatcher: IWebhookDispatcher
             readonly endpointRepo: IWebhookEndpointRepository
             readonly deliveryRepo: IAlertDeliveryRepository
           },
         ) {}

         async notify(payload: AlertPayload): Promise<DeliveryResult> {
           const endpoints = await this.deps.endpointRepo.findActiveByOrg(payload.orgId)
           let successes = 0
           let failures = 0
           await Promise.allSettled(endpoints.map(async (endpoint) => {
             // dedup check: existsSent with channel='webhook', target=endpoint.url
             const already = await this.deps.deliveryRepo.existsSent({
               orgId: payload.orgId, month: payload.month, tier: payload.tier,
               channel: 'webhook', target: endpoint.url,
             })
             if (already) return
             try {
               await this.deps.dispatcher.dispatch(endpoint, /* webhook body derived from payload */)
               await this.deps.deliveryRepo.save(AlertDelivery.create({ alertEventId: payload.alertEventId, orgId: payload.orgId, month: payload.month, tier: payload.tier, channel: 'webhook', target: endpoint.url, status: 'sent' }))
               // update endpoint success timestamp via endpointRepo.markSuccess / update, matching existing DispatchAlertWebhooksService behavior
               successes++
             } catch (err) {
               await this.deps.deliveryRepo.save(AlertDelivery.create({ ..., status: 'failed', error: String(err) }))
               // endpointRepo.markFailure / similar
               failures++
             }
           }))
           return { channel: 'webhook', successes, failures }
         }
       }

       - Copy/adapt every piece of logic from DispatchAlertWebhooksService.ts into this class
       - Preserve [Alerts] log prefix if original code logs
       - If DispatchAlertWebhooksService's current contract takes different argument shape, adapt the payload→invocation mapping here; never change external behavior

    2. Create `src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts` (per D-11 + D-14):

       export class EmailAlertNotifier implements IAlertNotifier {
         readonly channel: DeliveryChannel = 'email'
         constructor(
           private readonly deps: {
             readonly mailer: IMailer
             readonly templates: AlertEmailTemplates
             readonly deliveryRepo: IAlertDeliveryRepository
           },
         ) {}

         async notify(payload: AlertPayload): Promise<DeliveryResult> {
           let successes = 0
           let failures = 0
           for (const email of payload.emails) {
             const already = await this.deps.deliveryRepo.existsSent({
               orgId: payload.orgId, month: payload.month, tier: payload.tier, channel: 'email', target: email,
             })
             if (already) continue
             try {
               const rendered = this.deps.templates.render(payload)   // adapt to actual AlertEmailTemplates API
               await this.deps.mailer.send({ to: email, subject: rendered.subject, html: rendered.html })
               await this.deps.deliveryRepo.save(AlertDelivery.create({ alertEventId: payload.alertEventId, orgId: payload.orgId, month: payload.month, tier: payload.tier, channel: 'email', target: email, status: 'sent' }))
               successes++
             } catch (err) {
               await this.deps.deliveryRepo.save(AlertDelivery.create({ ..., status: 'failed', error: String(err), channel: 'email', target: email }))
               failures++
             }
           }
           return { channel: 'email', successes, failures }
         }
       }

       - Adapt template call to whatever AlertEmailTemplates actually exposes (e.g., `renderWarning(payload)` vs `renderCritical(payload)` branching on `payload.tier`)
       - Previously the email dedup + write logic lived inline in SendAlertService (lines 70-108 per research); relocate exactly

    3. Create `src/Modules/Alerts/__tests__/WebhookAlertNotifier.test.ts`:
       - Use `new WebhookAlertNotifier({ dispatcher: vi.fn(...) as IWebhookDispatcher, endpointRepo: new InMemoryWebhookEndpointRepository(), deliveryRepo: new InMemoryAlertDeliveryRepository() })`
       - Migrate assertions from DispatchAlertWebhooksService.test.ts (dedup / success path / failure path / endpoint-level error capture)
       - Use a stub/spy for IWebhookDispatcher (no in-memory dispatcher fake needed — verify via spy call count + args)

    4. Create `src/Modules/Alerts/__tests__/EmailAlertNotifier.test.ts`:
       - Use `new EmailAlertNotifier({ mailer: vi.fn() as IMailer, templates: realOrStubTemplates, deliveryRepo: new InMemoryAlertDeliveryRepository() })`
       - Assertions: per-email dedup via deliveryRepo, mailer.send invocation count/args, alert_deliveries row count with channel='email'

    Commit message: `feat: [alerts] 新增 WebhookAlertNotifier 與 EmailAlertNotifier 並遷移既有邏輯`
  </action>
  <verify>
    <automated>pnpm vitest run src/Modules/Alerts/__tests__/WebhookAlertNotifier.test.ts src/Modules/Alerts/__tests__/EmailAlertNotifier.test.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts` true
    - `test -f src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts` true
    - `grep -c "class WebhookAlertNotifier implements IAlertNotifier" src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts` == 1
    - `grep -c "class EmailAlertNotifier implements IAlertNotifier" src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts` == 1
    - `grep -c "channel.*'webhook'" src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts` >= 1
    - `grep -c "channel.*'email'" src/Modules/Alerts/Infrastructure/Services/EmailAlertNotifier.ts` >= 1
    - New notifier tests pass
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    Both notifiers implemented and testable in isolation. Existing DispatchAlertWebhooksService still exists (deleted Task 3). SendAlertService still uses old email/webhook path inline (updated Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: Collapse SendAlertService + ResendDeliveryService onto IAlertNotifier; delete DispatchAlertWebhooksService; wire provider; update tests; update MODULE.md</name>
  <files>
    src/Modules/Alerts/Application/Services/SendAlertService.ts
    src/Modules/Alerts/Application/Services/ResendDeliveryService.ts
    src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts
    src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts
    src/Modules/Alerts/__tests__/SendAlertService.test.ts
    src/Modules/Alerts/__tests__/ResendDeliveryService.test.ts
    src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts
    src/Modules/Alerts/MODULE.md
  </files>
  <read_first>
    - src/Modules/Alerts/Application/Services/SendAlertService.ts (Plan 2 output — object-literal ctor with mailer/templates/dispatchWebhooksService)
    - src/Modules/Alerts/Application/Services/ResendDeliveryService.ts (full — 162 lines; channel branching lines 64+112 per research)
    - src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts (scheduled for deletion)
    - src/Modules/Alerts/Infrastructure/Services/WebhookAlertNotifier.ts + EmailAlertNotifier.ts (Task 2 output)
    - src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts (Plan 2 output — has alertRecipientResolver binding)
    - src/Modules/Alerts/__tests__/SendAlertService.test.ts
    - src/Modules/Alerts/__tests__/ResendDeliveryService.test.ts
    - src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts
    - src/Modules/Alerts/MODULE.md (Plan 2 output — add notifier entries)
  </read_first>
  <action>
    1. Refactor `src/Modules/Alerts/Application/Services/SendAlertService.ts` (per D-12):
       - New ctor shape (final):
         constructor(
           private readonly deps: {
             readonly recipientResolver: IAlertRecipientResolver
             readonly alertEventRepo: IAlertEventRepository
             readonly notifiers: readonly IAlertNotifier[]
           },
         ) {}
       - Remove `mailer`, `templates`, `deliveryRepo`, `dispatchWebhooksService` from deps (they move into notifiers)
       - `send()` now:
         a. Resolve recipients: `const context = await this.deps.recipientResolver.resolveByOrg(params.orgId)`
         b. Create and save AlertEvent: `await this.deps.alertEventRepo.save(alertEvent)`
         c. Build payload: `const payload: AlertPayload = { ...params, ...context, alertEventId: alertEvent.id, emails: context.emails }`
         d. Dispatch to all notifiers: `await Promise.allSettled(this.deps.notifiers.map(n => n.notify(payload)))`
            - Research recommendation: Promise.allSettled — a single failing notifier must not block the others; matches existing DispatchAlertWebhooksService.dispatchAll line 35 behavior
            - Fire-and-forget preserved: if original SendAlertService sent webhook WITHOUT await, preserve that by separating sync vs async notifier execution — wrap webhook notifier call in a `void` / unawaited `.catch(...)` promise. (Check original code; research notes this is a critical preserved behavior.)

    2. Refactor `src/Modules/Alerts/Application/Services/ResendDeliveryService.ts` (per D-13):
       - Replace channel branching (lines ~64, ~112) with a notifierRegistry lookup:
         constructor(
           private readonly deps: {
             readonly deliveryRepo: IAlertDeliveryRepository
             readonly alertEventRepo: IAlertEventRepository
             readonly recipientResolver: IAlertRecipientResolver
             readonly notifierRegistry: Record<DeliveryChannel, IAlertNotifier>
           },
         ) {}
       - Resend flow:
         a. Load delivery by id
         b. Load parent AlertEvent → rebuild AlertPayload
         c. Resolve recipients (for email channel, `payload.emails = [delivery.target]`; for webhook, pass orgId through and the notifier will look up endpoints)
         d. `await this.deps.notifierRegistry[delivery.channel].notify(payload)`
       - Remove any `if (delivery.channel === 'webhook')` / `else` branching
       - Preserve existing error-handling/logging

    3. Delete `src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts` (logic now lives in WebhookAlertNotifier).

    4. Delete `src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts` (assertions migrated to WebhookAlertNotifier.test.ts in Task 2). If any assertions in this file were NOT migrated, migrate them now before deletion.

    5. Update `src/Modules/Alerts/__tests__/SendAlertService.test.ts` to final DI-less form:

       const alertEventRepo = new InMemoryAlertEventRepository()
       const deliveryRepo = new InMemoryAlertDeliveryRepository()
       const recipientResolver = new InMemoryAlertRecipientResolver({ 'org-1': { orgId: 'org-1', orgName: 'Acme', emails: ['a@x.com', 'b@x.com'] } })
       const emailNotifier = new FakeAlertNotifier('email')
       const webhookNotifier = new FakeAlertNotifier('webhook')
       const service = new SendAlertService({ recipientResolver, alertEventRepo, notifiers: [emailNotifier, webhookNotifier] })
       await service.send({ orgId: 'org-1', tier: 'warning', ... })
       expect(alertEventRepo.all()).toHaveLength(1)
       expect(emailNotifier.calls).toHaveLength(1)
       expect(webhookNotifier.calls).toHaveLength(1)

       - Zero DI container usage
       - Test names and scenarios preserved — just inputs reshaped

    6. Update `src/Modules/Alerts/__tests__/ResendDeliveryService.test.ts`:
       - Construct via `new ResendDeliveryService({ deliveryRepo: new InMemoryAlertDeliveryRepository(), alertEventRepo: new InMemoryAlertEventRepository(), recipientResolver: new InMemoryAlertRecipientResolver(...), notifierRegistry: { email: new FakeAlertNotifier('email'), webhook: new FakeAlertNotifier('webhook') } })`
       - Assertions reflect notifierRegistry dispatch

    7. Update `src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts`:
       - Import WebhookAlertNotifier, EmailAlertNotifier, IAlertNotifier
       - New bindings:
         container.singleton('webhookAlertNotifier', (c) => new WebhookAlertNotifier({ dispatcher: c.make('webhookDispatcher') as IWebhookDispatcher, endpointRepo: c.make('webhookEndpointRepository') as IWebhookEndpointRepository, deliveryRepo: c.make('alertDeliveryRepository') as IAlertDeliveryRepository }))
         container.singleton('emailAlertNotifier', (c) => new EmailAlertNotifier({ mailer: c.make('mailer') as IMailer, templates: c.make('alertEmailTemplates') as AlertEmailTemplates, deliveryRepo: c.make('alertDeliveryRepository') as IAlertDeliveryRepository }))
       - Update `sendAlertService` binding:
         container.bind('sendAlertService', (c) => new SendAlertService({ recipientResolver: c.make('alertRecipientResolver') as IAlertRecipientResolver, alertEventRepo: c.make('alertEventRepository') as IAlertEventRepository, notifiers: [c.make('emailAlertNotifier') as IAlertNotifier, c.make('webhookAlertNotifier') as IAlertNotifier] }))
       - Update `resendDeliveryService` binding with `notifierRegistry: { email: c.make('emailAlertNotifier'), webhook: c.make('webhookAlertNotifier') }`
       - REMOVE `dispatchAlertWebhooksService` binding and any related imports
       - Preserve `boot()` DomainEventDispatcher subscription untouched (D-08)

    8. Update `src/Modules/Alerts/MODULE.md`:
       - Under "使用的跨模組 Port"表格，move IMailer + IWebhookDispatcher rows from "(Plan 3: ...)" placeholder to EmailAlertNotifier / WebhookAlertNotifier concrete reference
       - Add a section "通知管道 Strategy (Phase 19)" explaining IAlertNotifier registry and extensibility

    Commit message: `refactor: [alerts] SendAlertService 改用 IAlertNotifier[]；刪除 DispatchAlertWebhooksService；ResendDeliveryService 接入 notifierRegistry`
  </action>
  <verify>
    <automated>pnpm vitest run src/Modules/Alerts &amp;&amp; pnpm vitest run &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Deleted: `test ! -f src/Modules/Alerts/Application/Services/DispatchAlertWebhooksService.ts`
    - Deleted: `test ! -f src/Modules/Alerts/__tests__/DispatchAlertWebhooksService.test.ts` (assertions migrated)
    - `grep -c "notifiers: readonly IAlertNotifier\[\]" src/Modules/Alerts/Application/Services/SendAlertService.ts` == 1
    - `grep -c "notifierRegistry" src/Modules/Alerts/Application/Services/ResendDeliveryService.ts` >= 1
    - `grep -c "mailer\|dispatchWebhooksService\|AlertEmailTemplates" src/Modules/Alerts/Application/Services/SendAlertService.ts` == 0 (all moved into notifiers)
    - `grep -c "DispatchAlertWebhooksService" src/Modules/Alerts/` project-wide returns empty (no orphan references)
    - `grep -c "webhookAlertNotifier\|emailAlertNotifier" src/Modules/Alerts/Infrastructure/Providers/AlertsServiceProvider.ts` >= 4 (2 bindings + 2 references)
    - `grep -c "Promise.allSettled" src/Modules/Alerts/Application/Services/SendAlertService.ts` >= 1
    - SendAlertService.test.ts contains zero `vi.fn()` for repo stubs — only InMemory* + FakeAlertNotifier: `grep -c "vi.fn" src/Modules/Alerts/__tests__/SendAlertService.test.ts` is small (ideally 0; only mailer-shaped stubs if any survived)
    - `grep -rn "from 'drizzle-orm'" src/Modules/Alerts/ | grep -v '.test.ts'` empty (regression check for Plan 1)
    - Full Alerts tests green: `pnpm vitest run src/Modules/Alerts`
    - Full suite green: `pnpm vitest run`
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>
    Notifier strategy fully wired: SendAlertService dispatches via notifiers array, ResendDeliveryService routes via notifierRegistry, DispatchAlertWebhooksService deleted, email deliveries land in alert_deliveries. All tests DI-less for touched services. MODULE.md reflects final dependency graph.
  </done>
</task>

</tasks>

<verification>
Phase-scope checks after Plan 3:
- ALERTS-03: new/touched Application service tests construct services with zero container
- ALERTS-04: SendAlertService uses only IAlertNotifier[] — no direct IMailer/IWebhookDispatcher dependencies
- ALERTS-05: `SELECT DISTINCT channel FROM alert_deliveries` (conceptual) now includes both 'email' and 'webhook' after a send operation
- DispatchAlertWebhooksService fully removed
- Fire-and-forget behavior preserved for webhooks (per research Pitfall)
- Full suite green
- `pnpm tsc --noEmit` green
- `grep -rln "from 'drizzle-orm'" src/Modules/Alerts/ | grep -v '.test.ts'` still empty (Plan 1 invariant preserved)
</verification>

<success_criteria>
- IAlertNotifier port + 2 concrete notifiers implemented
- SendAlertService collapsed onto notifiers array
- ResendDeliveryService on notifierRegistry
- DispatchAlertWebhooksService deleted
- `__tests__/fakes/` with 5 InMemory* repos + FakeAlertNotifier; new tests are DI-less
- Email deliveries write to alert_deliveries with channel='email'
- MODULE.md updated to final state
- ALERTS-03, ALERTS-04, ALERTS-05 satisfied (full-suite verification)
</success_criteria>

<output>
After completion, create `.planning/phases/19-alerts-module-decoupling/19-03-SUMMARY.md` with:
- Files created / modified / deleted (notifiers, fakes, tests, provider)
- Confirmation that DispatchAlertWebhooksService is fully removed (grep result)
- Confirmation that SendAlertService has zero direct Mailer/Dispatcher/Templates deps
- Sample of InMemory fake behavior and any deferred filter/sort features noted
- Fire-and-forget webhook behavior verification approach
- Phase 19 completion note: all ALERTS-01..05 delivered
</output>
