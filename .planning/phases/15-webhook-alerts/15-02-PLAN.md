---
phase: 15-webhook-alerts
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - resources/js/Pages/Member/Alerts/Index.tsx
  - resources/js/Pages/Member/Alerts/tabs/BudgetsTab.tsx
  - resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx
  - resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx
  - resources/js/Pages/Member/Alerts/components/WebhookEndpointForm.tsx
  - resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx
  - resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx
  - resources/js/Pages/Member/Alerts/components/DeliveryStatusBadge.tsx
  - resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx
  - resources/js/Pages/Member/Alerts/types.ts
  - resources/js/Pages/Member/Alerts/api.ts
  - resources/js/layouts/MemberLayout.tsx
  - src/Pages/Member/MemberAlertsPage.ts
  - src/Pages/routing/member/memberPageKeys.ts
  - src/Pages/routing/member/registerMemberPageBindings.ts
  - src/Pages/routing/registerMemberPageRoutes.ts
autonomous: false
requirements: [ALRT-06, ALRT-07, ALRT-08]

must_haves:
  truths:
    - "Navigating to /alerts (as a manager) renders a page with three tabs: Budgets, Webhooks, History."
    - "Budgets tab allows reading and updating monthly budget (existing Phase 13 backend)."
    - "Webhooks tab lists all registered webhook endpoints with masked secrets, supports create (HTTPS URL + optional description), activate/deactivate toggle, rotate-secret, send-test, and delete."
    - "Creating or rotating an endpoint surfaces the plaintext secret in a SecretRevealModal that shows ONCE and is never re-fetched."
    - "History tab shows a reverse-chronological list of alert_events; each row expands to show per-channel alert_deliveries with status badges (sent/failed/pending)."
    - "Failed webhook deliveries have a 'Resend' button that triggers POST /deliveries/:id/resend and refreshes the history."
    - "All write actions handle 422 errors from backend (HTTPS, SSRF, max-5) and show the error message inline via a toast or form error."
  artifacts:
    - path: "resources/js/Pages/Member/Alerts/Index.tsx"
      provides: "Tabs container for /alerts page"
      min_lines: 30
    - path: "resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx"
      provides: "Webhook CRUD UI with secret reveal"
    - path: "resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx"
      provides: "Alert history timeline with per-channel delivery rows + resend"
    - path: "resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx"
      provides: "One-time plaintext secret display with copy-to-clipboard"
    - path: "resources/js/Pages/Member/Alerts/api.ts"
      provides: "Typed fetch helpers for /alerts/webhooks, /history, /deliveries/:id/resend"
  key_links:
    - from: "resources/js/Pages/Member/Alerts/Index.tsx"
      to: "member/alerts page key"
      via: "Inertia page registration"
      pattern: "member/alerts"
    - from: "resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx"
      to: "POST /api/organizations/:orgId/alerts/webhooks"
      via: "api.ts createWebhook helper"
      pattern: "alerts/webhooks"
    - from: "resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx"
      to: "POST /api/organizations/:orgId/alerts/deliveries/:id/resend"
      via: "api.ts resendDelivery helper"
      pattern: "deliveries/.*/resend"
    - from: "resources/js/layouts/MemberLayout.tsx"
      to: "member/alerts page"
      via: "Nav link added to sidebar"
      pattern: "/alerts"
---

<objective>
Frontend for Phase 15. Build the unified `/alerts` page with Budgets / Webhooks / History tabs. Provides ALRT-06 CRUD UX (one-time secret reveal, send-test button), ALRT-07 verification UI (signature visible for debugging in SecretRevealModal copy), and ALRT-08 history timeline with resend button.

Purpose: Deliver the user-facing surface for webhook alerts per decisions D-13 and D-14 and complete Phase 15 success criteria (User can register webhook URL, all payloads HMAC-signed visible to user as secret, user views per-channel history and delivery status).

Output: New React/Inertia page under `resources/js/Pages/Member/Alerts/` with four tabs and supporting components; MemberLayout nav updated; E2E-verifiable via checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-webhook-alerts/15-CONTEXT.md
@.planning/phases/15-webhook-alerts/15-RESEARCH.md
@.planning/phases/15-webhook-alerts/15-01-SUMMARY.md
@resources/js/layouts/MemberLayout.tsx
@resources/js/Pages/Member/CostBreakdown/Index.tsx
@resources/js/Pages/Member/Settings/Index.tsx
@src/Pages/Member/MemberCostBreakdownPage.ts
@src/Pages/routing/member/memberPageKeys.ts
@src/Pages/routing/member/registerMemberPageBindings.ts
@src/Pages/routing/registerMemberPageRoutes.ts

<interfaces>
<!-- Plan 01 backend contracts — DTOs and endpoints this plan consumes -->

From Plan 01 `src/Modules/Alerts/Application/DTOs/WebhookEndpointDTO.ts`:
```typescript
// List/get response shape (masked secret)
export interface WebhookEndpointListDTO {
  id: string
  orgId: string
  url: string
  active: boolean
  description: string | null
  createdAt: string
  lastSuccessAt: string | null
  lastFailureAt: string | null
  secretMask: string                 // e.g. 'whsec_****abc4'
}
// Create/rotate response shape (plaintext secret — shown ONCE)
export interface WebhookEndpointCreatedDTO extends Omit<WebhookEndpointListDTO, 'secretMask'> {
  secret: string                     // plaintext 'whsec_...' — MUST be shown in modal once
}
```

From Plan 01 `src/Modules/Alerts/Application/DTOs/AlertHistoryDTO.ts`:
```typescript
export interface AlertDeliveryDTO {
  id: string
  channel: 'email' | 'webhook'
  target: string                     // email address OR endpoint id
  targetUrl: string | null
  status: 'pending' | 'sent' | 'failed'
  attempts: number
  statusCode: number | null
  errorMessage: string | null
  dispatchedAt: string
  deliveredAt: string | null
}
export interface AlertEventHistoryDTO {
  id: string
  orgId: string
  tier: 'warning' | 'critical'
  month: string                       // 'YYYY-MM'
  budgetUsd: string
  actualCostUsd: string
  percentage: string
  createdAt: string
  deliveries: AlertDeliveryDTO[]
}
```

Backend endpoints (from Plan 01 alert.routes.ts):
- `GET    /api/organizations/:orgId/alerts/webhooks` → `{ success, data: WebhookEndpointListDTO[] }`
- `POST   /api/organizations/:orgId/alerts/webhooks` body `{ url, description? }` → 201 `{ success, data: WebhookEndpointCreatedDTO }` | 422 on HTTPS/SSRF/max-5 failure
- `PATCH  /api/organizations/:orgId/alerts/webhooks/:endpointId` body `{ active?, description? }` → 200
- `POST   /api/organizations/:orgId/alerts/webhooks/:endpointId/rotate-secret` → 200 `{ success, data: WebhookEndpointCreatedDTO }`
- `POST   /api/organizations/:orgId/alerts/webhooks/:endpointId/test` → 200 `{ success: boolean, data: { statusCode?, attempts, error? } }`
- `DELETE /api/organizations/:orgId/alerts/webhooks/:endpointId` → 204
- `GET    /api/organizations/:orgId/alerts/history?limit=50&offset=0` → `{ success, data: AlertEventHistoryDTO[] }`
- `POST   /api/organizations/:orgId/alerts/deliveries/:deliveryId/resend` → 200 | 410 (endpoint gone) | 422

Inertia page key (from Plan 01): `'member/alerts'` — resolved by `src/Pages/Member/MemberAlertsPage.ts`; the handler already props in `budget`, `webhookEndpoints`, `alertHistory`.

Existing Phase 14 page pattern (reference for structure):
- `resources/js/Pages/Member/CostBreakdown/Index.tsx` — how props are typed, how MemberLayout wraps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Alerts page + Budgets tab + shared types/api + nav link</name>
  <read_first>
    - resources/js/Pages/Member/CostBreakdown/Index.tsx
    - resources/js/Pages/Member/Settings/Index.tsx
    - resources/js/layouts/MemberLayout.tsx
    - src/Pages/Member/MemberAlertsPage.ts (from Plan 01)
    - src/Modules/Alerts/Application/DTOs/WebhookEndpointDTO.ts (from Plan 01)
    - src/Modules/Alerts/Application/DTOs/AlertHistoryDTO.ts (from Plan 01)
  </read_first>
  <action>
    1. Create `resources/js/Pages/Member/Alerts/types.ts` — re-declare the DTO interfaces above (`WebhookEndpointListDTO`, `WebhookEndpointCreatedDTO`, `AlertDeliveryDTO`, `AlertEventHistoryDTO`) plus:
       ```typescript
       export interface AlertsPageProps {
         orgId: string
         budget: { budgetUsd: string | null; warningPct: number; criticalPct: number } | null
         webhookEndpoints: WebhookEndpointListDTO[]
         alertHistory: AlertEventHistoryDTO[]
       }
       export type AlertsTab = 'budgets' | 'webhooks' | 'history'
       ```

    2. Create `resources/js/Pages/Member/Alerts/api.ts` — typed fetch helpers (use the project's existing fetch wrapper if present; else native fetch with CSRF header). Each returns a typed promise and throws on non-2xx with the `message` body field:
       - `listWebhooks(orgId)`
       - `createWebhook(orgId, { url, description? })`
       - `updateWebhook(orgId, endpointId, { active?, description? })`
       - `rotateSecret(orgId, endpointId)`
       - `testWebhook(orgId, endpointId)`
       - `deleteWebhook(orgId, endpointId)`
       - `listHistory(orgId, { limit, offset })`
       - `resendDelivery(orgId, deliveryId)`
       All URLs exactly as per Plan 01 route table.

    3. Create `resources/js/Pages/Member/Alerts/Index.tsx`:
       ```tsx
       import { useState } from 'react'
       import MemberLayout from '@/layouts/MemberLayout'
       import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
       import BudgetsTab from './tabs/BudgetsTab'
       import WebhooksTab from './tabs/WebhooksTab'
       import HistoryTab from './tabs/HistoryTab'
       import type { AlertsPageProps, AlertsTab } from './types'

       export default function AlertsIndex(props: AlertsPageProps) {
         const [tab, setTab] = useState<AlertsTab>('budgets')
         return (
           <MemberLayout>
             <div className="p-6 space-y-4">
               <h1 className="text-2xl font-semibold">Alerts</h1>
               <Tabs value={tab} onValueChange={(v) => setTab(v as AlertsTab)}>
                 <TabsList>
                   <TabsTrigger value="budgets">Budgets</TabsTrigger>
                   <TabsTrigger value="webhooks">Webhooks</TabsTrigger>
                   <TabsTrigger value="history">History</TabsTrigger>
                 </TabsList>
                 <TabsContent value="budgets"><BudgetsTab orgId={props.orgId} initial={props.budget} /></TabsContent>
                 <TabsContent value="webhooks"><WebhooksTab orgId={props.orgId} initial={props.webhookEndpoints} /></TabsContent>
                 <TabsContent value="history"><HistoryTab orgId={props.orgId} initial={props.alertHistory} /></TabsContent>
               </Tabs>
             </div>
           </MemberLayout>
         )
       }
       ```
       (Use the existing shadcn/ui Tabs component — check `resources/js/components/ui/tabs.tsx` exists; if not, use a simple role=tablist div with buttons.)

    4. Create `resources/js/Pages/Member/Alerts/tabs/BudgetsTab.tsx`:
       - Wraps the existing Phase 13 `/api/organizations/:orgId/alerts/budget` GET/PUT endpoints.
       - Form with `budgetUsd` input, submit button.
       - On 200, show "Saved" toast; on 422, show field error.
       - Reuse any existing budget form component if already present; else inline.

    5. Update `resources/js/layouts/MemberLayout.tsx`:
       - Add an "Alerts" nav link pointing to `/alerts` in the sidebar, visible to organization managers (check existing role-based nav pattern).
       - Use whatever icon convention is established (e.g. lucide `Bell`).

    6. Wire the page in Inertia:
       - Confirm `src/Pages/Member/MemberAlertsPage.ts` (created in Plan 01) returns `inertia('member/alerts', { orgId, budget, webhookEndpoints, alertHistory })`. If it currently stubs empty data, update it to actually resolve `GetBudgetService`, `ListWebhookEndpointsService`, `GetAlertHistoryService` from the container.
       - Confirm `registerMemberPageBindings.ts` maps `'member/alerts'` → the component path `'Pages/Member/Alerts/Index'` following the existing convention.
  </action>
  <verify>
    <automated>bun run typecheck &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f resources/js/Pages/Member/Alerts/Index.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/types.ts` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/api.ts` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/tabs/BudgetsTab.tsx` succeeds.
    - `grep -q "Tabs" resources/js/Pages/Member/Alerts/Index.tsx` matches.
    - `grep -q "budgets\\|webhooks\\|history" resources/js/Pages/Member/Alerts/Index.tsx` matches.
    - `grep -q "/alerts" resources/js/layouts/MemberLayout.tsx` matches.
    - `grep -q "WebhookEndpointListDTO" resources/js/Pages/Member/Alerts/types.ts` matches.
    - `grep -q "AlertEventHistoryDTO" resources/js/Pages/Member/Alerts/types.ts` matches.
    - `grep -q "createWebhook\\|listWebhooks" resources/js/Pages/Member/Alerts/api.ts` matches.
    - `grep -q "resendDelivery" resources/js/Pages/Member/Alerts/api.ts` matches.
    - `bun run typecheck` succeeds.
    - `bun run build` succeeds (Vite bundles the new page).
  </acceptance_criteria>
  <done>`/alerts` page routable, three tabs visible, Budgets tab wired to existing Phase 13 backend, types + api module ready for subsequent tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Webhooks tab — CRUD UI, send-test, rotate-secret, one-time SecretRevealModal</name>
  <read_first>
    - resources/js/Pages/Member/Alerts/types.ts (Task 1)
    - resources/js/Pages/Member/Alerts/api.ts (Task 1)
    - resources/js/Pages/Member/CostBreakdown/Index.tsx (form/table patterns)
    - .planning/phases/15-webhook-alerts/15-RESEARCH.md (Pitfall 7 — secret reveal contract)
  </read_first>
  <action>
    1. Create `resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx`:
       - Props: `{ secret: string | null; onClose: () => void; title?: string }`.
       - When `secret` is truthy, renders a modal with:
         - Warning text: "Copy this secret now. It will not be shown again."
         - Read-only input displaying the plaintext secret.
         - "Copy to clipboard" button using `navigator.clipboard.writeText(secret)`.
         - "I've saved my secret" close button (only closes on confirm click).
       - When `secret` is null, renders nothing.

    2. Create `resources/js/Pages/Member/Alerts/components/WebhookEndpointForm.tsx`:
       - Inline create form: `url` (text input, required), `description` (text input, optional, max 200).
       - Client-side validation: URL must start with `https://` (or `http://` only in dev — expose `allowHttp` prop from parent read from a data-attribute on the page set server-side).
       - On submit, call `api.createWebhook(orgId, { url, description })`.
       - On 422, display the backend message inline under the URL field (e.g. "Webhook URL must use HTTPS" or "SSRF: …" or "Maximum 5 webhook endpoints per organization").
       - On 201, lift the `data` (`WebhookEndpointCreatedDTO`) via `onCreated` callback so parent can open SecretRevealModal with `data.secret`.

    3. Create `resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx`:
       - Props: `{ endpoint: WebhookEndpointListDTO; orgId: string; onChange: () => void; onRotate: (plaintext: string) => void }`.
       - Renders: URL (truncated), description, active toggle switch, masked secret, last success/failure timestamps.
       - Actions row: "Send test" button, "Rotate secret" button, "Delete" button (with confirm).
       - Send test: calls `api.testWebhook`, shows toast with `statusCode` or `error`.
       - Rotate: calls `api.rotateSecret`, lifts plaintext to parent via `onRotate` (which opens SecretRevealModal).
       - Toggle active: calls `api.updateWebhook(orgId, endpoint.id, { active: !endpoint.active })` → `onChange()` to refresh.
       - Delete: confirm dialog then `api.deleteWebhook` → `onChange()`.

    4. Create `resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx`:
       - Props: `{ orgId: string; initial: WebhookEndpointListDTO[] }`.
       - State: `endpoints` (array), `revealedSecret` (string | null).
       - Renders: count badge `{endpoints.length} / 5`, Create form at top (disabled when count >= 5), list below.
       - `refresh()` helper: `setEndpoints(await api.listWebhooks(orgId))`.
       - Mount SecretRevealModal with `secret={revealedSecret}` and `onClose={() => setRevealedSecret(null)}`.
       - On create success: setRevealedSecret(created.secret) then refresh().
       - On rotate success: setRevealedSecret(newSecret) then refresh().
       - Show empty state when no endpoints.
  </action>
  <verify>
    <automated>bun run typecheck &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/components/WebhookEndpointForm.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx` succeeds.
    - `grep -q "navigator.clipboard" resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx` matches.
    - `grep -q "shown again\\|won't be shown\\|not be shown" resources/js/Pages/Member/Alerts/components/SecretRevealModal.tsx` matches (one-time warning).
    - `grep -q "createWebhook" resources/js/Pages/Member/Alerts/components/WebhookEndpointForm.tsx` matches.
    - `grep -q "rotateSecret" resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx` matches.
    - `grep -q "testWebhook" resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx` matches.
    - `grep -q "deleteWebhook" resources/js/Pages/Member/Alerts/components/WebhookEndpointListItem.tsx` matches.
    - `grep -q "/ 5\\|5 /\\|max.*5\\|Maximum" resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx` matches (5-endpoint cap surfaced in UI).
    - `grep -q "SecretRevealModal" resources/js/Pages/Member/Alerts/tabs/WebhooksTab.tsx` matches.
    - `bun run typecheck` succeeds.
    - `bun run build` succeeds.
  </acceptance_criteria>
  <done>Manager can register, list, update, test, rotate-secret, and delete webhook endpoints; secrets reveal exactly once via modal.</done>
</task>

<task type="auto">
  <name>Task 3: History tab — timeline, per-channel delivery rows, status badges, resend</name>
  <read_first>
    - resources/js/Pages/Member/Alerts/types.ts (Task 1)
    - resources/js/Pages/Member/Alerts/api.ts (Task 1)
  </read_first>
  <action>
    1. Create `resources/js/Pages/Member/Alerts/components/DeliveryStatusBadge.tsx`:
       - Props: `{ status: 'pending' | 'sent' | 'failed' }`.
       - Renders colored pill (green/yellow/red) with icon.

    2. Create `resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx`:
       - Props: `{ event: AlertEventHistoryDTO; orgId: string; onResent: () => void }`.
       - Collapsed view: tier badge, month, cost/budget/%, createdAt, summary "{N} deliveries ({M} failed)".
       - Expanded view (toggle): list of `event.deliveries`:
         - Each row: channel icon, target (email or URL snapshot), status badge, attempts count, statusCode, dispatchedAt, deliveredAt, errorMessage.
         - For `status === 'failed'`: show "Resend" button → calls `api.resendDelivery(orgId, delivery.id)` → on success, toast + `onResent()`. On 410, toast "Webhook endpoint no longer exists" (disable the button for those going forward).

    3. Create `resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx`:
       - Props: `{ orgId: string; initial: AlertEventHistoryDTO[] }`.
       - State: `events`, `limit=50`, `offset=0`.
       - `refresh()`: `setEvents(await api.listHistory(orgId, { limit, offset }))`.
       - Renders: header + optional filter (tier/month — discretionary per D-16), list of `AlertEventRow` sorted newest-first (backend already sorts; trust order).
       - "Load more" button if `events.length === limit` → increment offset and append.
       - Empty state: "No alerts yet. Alerts appear here when your cost exceeds the configured thresholds."
  </action>
  <verify>
    <automated>bun run typecheck &amp;&amp; bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx` succeeds.
    - `test -f resources/js/Pages/Member/Alerts/components/DeliveryStatusBadge.tsx` succeeds.
    - `grep -q "resendDelivery" resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx` matches.
    - `grep -q "failed" resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx` matches.
    - `grep -q "deliveries" resources/js/Pages/Member/Alerts/components/AlertEventRow.tsx` matches.
    - `grep -q "pending\\|sent\\|failed" resources/js/Pages/Member/Alerts/components/DeliveryStatusBadge.tsx` matches.
    - `grep -q "listHistory" resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx` matches.
    - `grep -q "Load more\\|load more\\|loadMore" resources/js/Pages/Member/Alerts/tabs/HistoryTab.tsx` matches.
    - `bun run typecheck` succeeds.
    - `bun run build` succeeds.
  </acceptance_criteria>
  <done>History tab shows unified timeline with per-channel delivery status; resend works on failed rows and updates the list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end verification of /alerts page flows</name>
  <files>N/A — human verification only, no file changes</files>
  <action>Pause execution. Present the &lt;what-built&gt; + &lt;how-to-verify&gt; sections below to the user. Wait for resume-signal before continuing.</action>
  <verify>Human confirms all 14 checklist items below pass.</verify>
  <done>User types "approved" after completing all 14 verification steps.</done>
  <what-built>
    Complete Phase 15 frontend: `/alerts` page with Budgets / Webhooks / History tabs. Full end-to-end flow:
    1. Register webhook endpoint with HTTPS validation + SSRF rejection.
    2. One-time plaintext secret reveal on create and rotate.
    3. Send-test button hitting real webhook endpoint.
    4. Budget breach triggers email + webhook with HMAC-SHA256 signature.
    5. History tab shows per-channel delivery status; failed deliveries can be resent.
  </what-built>
  <how-to-verify>
    Prerequisites: `bun run dev` is running; logged in as an organization manager; at least one org exists.

    1. **Navigation:** Go to `/alerts`. Verify page loads with three tabs: Budgets, Webhooks, History.

    2. **Budgets tab:** Set a small budget (e.g. `$0.01`). Save. Refresh — budget persists.

    3. **Webhook registration happy path:**
       - On Webhooks tab, click "Create".
       - Enter `https://webhook.site/#!/<your-unique-id>` (open https://webhook.site first to get a unique URL).
       - Description: "Test endpoint".
       - Submit.
       - Expected: SecretRevealModal opens, shows `whsec_...` string. "Copy to clipboard" button works. Warning text says the secret won't be shown again. Close modal.
       - List now shows 1/5 endpoints with masked secret (`whsec_****...<last 4>`).

    4. **HTTPS rejection:**
       - Create another endpoint with `http://example.com/hook`.
       - Expected: Inline error "Webhook URL must use HTTPS". No new row.

    5. **SSRF rejection:**
       - Create with `https://127.0.0.1/hook`.
       - Expected: Inline error containing "SSRF" or "private" / "loopback". No new row.

    6. **Max-5 enforcement:**
       - Create 4 more valid endpoints (5 total). Verify create form disables or shows "Maximum 5 webhook endpoints per organization" when attempting a 6th.

    7. **Send test:**
       - Click "Send test" on the first endpoint.
       - Expected: toast shows `statusCode: 200` within ~2s. Check https://webhook.site: a POST arrived with headers `X-Webhook-Signature`, `X-Webhook-Event: alert.test`, `X-Webhook-Id: <uuid>`. Body: `{ id, event: "alert.test", data: { message: "Test webhook from Draupnir", ... }, timestamp }`.

    8. **Signature verification (ALRT-07):**
       - Copy the request body text and signature header from webhook.site.
       - In a local terminal: `echo -n '<body>' | openssl dgst -sha256 -hmac '<plaintextSecret>'` — compare to the `X-Webhook-Signature` header. MUST match exactly.

    9. **Rotate secret:**
       - Click "Rotate secret" on an endpoint.
       - Expected: new SecretRevealModal with a DIFFERENT `whsec_...` value.
       - Click "Send test" again — webhook.site receives a new POST; signature now valid under the NEW secret (old secret fails verification).

    10. **Active toggle:**
        - Deactivate an endpoint.
        - Expected: list row shows inactive state (greyed/toggle off).

    11. **Real alert dispatch (end-to-end):**
        - Ensure budget is low (from step 2) and organization usage exceeds it.
        - Either wait for Bifrost sync, or trigger sync manually (check project docs — likely `bun orbit sync` or an admin endpoint).
        - Expected within a few seconds:
          - Email received by org managers (check the configured mail transport — Upyo/Console).
          - webhook.site shows a POST with `event: "alert.threshold.breached"`, `data: { orgId, orgName, tier: "warning" or "critical", budgetUsd, actualCostUsd, percentage, month, alertEventId }`.
          - History tab (refresh) shows the new alert_event at the top with TWO delivery rows: email (✓ sent) and webhook (✓ sent).

    12. **Failure handling + resend:**
        - Deactivate the webhook endpoint on webhook.site side (or use a URL returning 500, e.g. https://httpbin.org/status/500).
        - Register a new webhook endpoint pointing at the 500 URL.
        - Trigger another budget breach (or wait for next sync).
        - Expected: History row shows webhook delivery with ✗ failed badge, statusCode 500, attempts=3, errorMessage.
        - Fix the URL (deactivate old, create new pointing at a working webhook.site URL). Note: the original row is frozen; resend uses the CURRENT endpoint resolved by `target` (endpoint id) — so to test resend, keep the failing endpoint but temporarily point httpbin at success, OR this verifies the documented limitation.
        - Click "Resend" on the failed row.
        - Expected: a NEW delivery row appears (old failed row preserved in history); new row ✓ sent if endpoint recovered, else ✗ failed.

    13. **Dedup verification:**
        - After a successful breach alert, trigger another sync without changing conditions (still over budget, same month, same tier).
        - Expected: NO new webhook POST hits webhook.site, NO new email. History shows ONE event row for that (month, tier) combination.

    14. **One-time secret contract (Pitfall 7):**
        - Reload the `/alerts` page in browser.
        - Expected: GET `/api/organizations/:orgId/alerts/webhooks` response body in DevTools Network tab shows `secretMask` only, NOT plaintext `secret`. grep: `grep -v "whsec_[a-f0-9]\{64\}"` — should match (i.e., no 64-hex secrets in the response).

    Checklist summary (mark each):
    - [ ] Page renders with 3 tabs
    - [ ] Create webhook → SecretRevealModal → list updates
    - [ ] HTTPS rejection works
    - [ ] SSRF rejection (127.0.0.1) works
    - [ ] Max-5 enforced
    - [ ] Send test delivers real POST to webhook.site
    - [ ] HMAC signature verifies with openssl
    - [ ] Rotate secret produces new value; signatures change
    - [ ] Active toggle works
    - [ ] Real budget breach triggers email + webhook
    - [ ] History shows per-channel deliveries
    - [ ] Failed delivery surfaces error details
    - [ ] Resend creates new delivery row
    - [ ] Dedup prevents duplicate dispatches
    - [ ] Page reload response does NOT leak plaintext secret
  </how-to-verify>
  <resume-signal>Type "approved" when all 14 checklist items pass, or describe which step failed and paste the error/behavior.</resume-signal>
</task>

</tasks>

<verification>
- `bun run typecheck` + `bun run build` green.
- Human verification checkpoint passes all 14 items.
- Manual spot-check of DB: `SELECT * FROM alert_deliveries ORDER BY created_at DESC LIMIT 10;` — rows present with correct shape.
</verification>

<success_criteria>
- ALRT-06: UI allows registering webhook URL; HTTPS + SSRF + max-5 enforcement surfaces backend errors cleanly — verified in checkpoint steps 3-6.
- ALRT-07: Send-test and real-alert POSTs carry `X-Webhook-Signature` header; signature verifies via openssl HMAC-SHA256 with the plaintext secret — verified in checkpoint step 8.
- ALRT-08: History tab renders per-channel delivery status; resend regenerates failed deliveries — verified in checkpoint steps 11-12.
</success_criteria>

<output>
After completion, create `.planning/phases/15-webhook-alerts/15-02-SUMMARY.md` documenting:
- All frontend files created
- Nav link + page key registration
- Known limitations (e.g. resend requires endpoint to still exist per Plan 01 Pattern 6)
- Backfill script invocation result (if run during this plan)
- Phase 15 complete: mark ALRT-06/07/08 done in `.planning/REQUIREMENTS.md` traceability table
</output>
