---
phase: 06-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Pages/__tests__/Admin/AdminDashboardPage.test.ts
  - src/Pages/__tests__/Admin/AdminUsersPage.test.ts
  - src/Pages/__tests__/Admin/AdminUserDetailPage.test.ts
  - src/Pages/__tests__/Admin/AdminOrganizationsPage.test.ts
  - src/Pages/__tests__/Admin/AdminOrganizationDetailPage.test.ts
  - src/Pages/__tests__/Admin/AdminContractsPage.test.ts
  - src/Pages/__tests__/Admin/AdminContractCreatePage.test.ts
  - src/Pages/__tests__/Admin/AdminContractDetailPage.test.ts
  - src/Pages/__tests__/Admin/AdminModulesPage.test.ts
  - src/Pages/__tests__/Admin/AdminModuleCreatePage.test.ts
  - src/Pages/__tests__/Admin/AdminApiKeysPage.test.ts
  - src/Pages/__tests__/Admin/AdminUsageSyncPage.test.ts
autonomous: true
requirements:
  - PAGE-01
  - PAGE-03
  - PAGE-04
  - PAGE-06

must_haves:
  truths:
    - "Unauthenticated requests to every admin page return a 302 redirect to /login"
    - "Authenticated requests with role 'member' to every admin page return 403"
    - "Authenticated admin requests to GET handlers invoke inertia.render with the correct Inertia component name"
    - "POST handlers (postStatus, store, postAction) redirect on success and re-render with formError on validation failure"
    - "AdminUsageSyncPage returns the static placeholder props (enabled: false)"
  artifacts:
    - path: "src/Pages/__tests__/Admin/AdminDashboardPage.test.ts"
      provides: "Unit tests for AdminDashboardPage"
    - path: "src/Pages/__tests__/Admin/AdminUsersPage.test.ts"
      provides: "Unit tests for AdminUsersPage"
    - path: "src/Pages/__tests__/Admin/AdminUserDetailPage.test.ts"
      provides: "Unit tests for AdminUserDetailPage (handle + postStatus)"
    - path: "src/Pages/__tests__/Admin/AdminOrganizationsPage.test.ts"
      provides: "Unit tests for AdminOrganizationsPage"
    - path: "src/Pages/__tests__/Admin/AdminOrganizationDetailPage.test.ts"
      provides: "Unit tests for AdminOrganizationDetailPage"
    - path: "src/Pages/__tests__/Admin/AdminContractsPage.test.ts"
      provides: "Unit tests for AdminContractsPage"
    - path: "src/Pages/__tests__/Admin/AdminContractCreatePage.test.ts"
      provides: "Unit tests for AdminContractCreatePage (handle + store)"
    - path: "src/Pages/__tests__/Admin/AdminContractDetailPage.test.ts"
      provides: "Unit tests for AdminContractDetailPage (handle + postAction)"
    - path: "src/Pages/__tests__/Admin/AdminModulesPage.test.ts"
      provides: "Unit tests for AdminModulesPage"
    - path: "src/Pages/__tests__/Admin/AdminModuleCreatePage.test.ts"
      provides: "Unit tests for AdminModuleCreatePage (handle + store)"
    - path: "src/Pages/__tests__/Admin/AdminApiKeysPage.test.ts"
      provides: "Unit tests for AdminApiKeysPage"
    - path: "src/Pages/__tests__/Admin/AdminUsageSyncPage.test.ts"
      provides: "Unit tests for AdminUsageSyncPage"
  key_links:
    - from: "test files"
      to: "src/Pages/Admin/*.ts"
      via: "direct import of page class"
      pattern: "import.*Admin.*Page.*from"
    - from: "createMockContext"
      to: "AuthMiddleware store key"
      via: "ctx.get('auth') / ctx.set('auth', ...)"
      pattern: "store.set\\('auth'"
---

<objective>
Write unit tests for all 12 Admin page handler classes in `src/Pages/Admin/`.

Purpose: These classes have zero test coverage. Each handler performs auth gate checks (requireAdmin), calls injected services, and calls inertia.render. Tests verify: unauthenticated → 302, non-admin → 403, admin → correct Inertia component, POST success → redirect, POST validation failure → re-render with formError.

Output: 12 new test files under `src/Pages/__tests__/Admin/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pages/06-RESEARCH.md

<interfaces>
<!-- Auth store key confirmed from src/Shared/Infrastructure/Middleware/AuthMiddleware.ts -->
<!-- ctx.set('auth', authContext) — key is the string 'auth' -->
<!-- ctx.get<AuthContext>('auth') — same key -->

<!-- AuthContext shape (from AuthMiddleware.ts): -->
```typescript
interface AuthContext {
  userId: string
  email: string
  role: string   // 'admin' | 'manager' | 'member'
}
```

<!-- requireAdmin (src/Pages/Admin/helpers/requireAdmin.ts): -->
<!-- - if !auth → redirect('/login') with status 302 -->
<!-- - if auth.role !== 'admin' → Response 403 HTML -->
<!-- - else → { ok: true, auth } -->

<!-- InertiaService.render signature (src/Pages/InertiaService.ts): -->
<!-- render(ctx: IHttpContext, component: string, props: Record<string, unknown>): Response -->

<!-- Test helper pattern from src/Pages/__tests__/InertiaService.test.ts: -->
```typescript
import { describe, expect, test, mock } from 'bun:test'
import type { IHttpContext } from '@/Shared/Presentation/IHttpContext'

function createMockContext(overrides: Partial<IHttpContext> = {}): IHttpContext {
  const store = new Map<string, unknown>()
  return {
    getBodyText: async () => '',
    getJsonBody: async <T>() => ({}) as T,
    getBody: async <T>() => ({}) as T,
    getHeader: () => undefined,
    getParam: () => undefined,
    getPathname: () => '/admin/dashboard',
    getQuery: () => undefined,
    params: {},
    query: {},
    headers: {},
    json: (data: unknown, statusCode?: number) =>
      Response.json(data, { status: statusCode ?? 200 }),
    text: (content: string, statusCode?: number) =>
      new Response(content, { status: statusCode ?? 200 }),
    redirect: (url: string, statusCode?: number) => Response.redirect(url, statusCode ?? 302),
    get: <T>(key: string) => store.get(key) as T | undefined,
    set: (key: string, value: unknown) => { store.set(key, value) },
    ...overrides,
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin page test helpers + simple GET-only admin pages (Dashboard, Users, Organizations, Modules, ApiKeys, UsageSync)</name>
  <read_first>
    - src/Pages/__tests__/InertiaService.test.ts
    - src/Pages/Admin/AdminDashboardPage.ts
    - src/Pages/Admin/AdminUsersPage.ts
    - src/Pages/Admin/AdminOrganizationsPage.ts
    - src/Pages/Admin/AdminModulesPage.ts
    - src/Pages/Admin/AdminApiKeysPage.ts
    - src/Pages/Admin/AdminUsageSyncPage.ts
    - src/Shared/Infrastructure/Middleware/AuthMiddleware.ts
  </read_first>
  <files>
    src/Pages/__tests__/Admin/AdminDashboardPage.test.ts,
    src/Pages/__tests__/Admin/AdminUsersPage.test.ts,
    src/Pages/__tests__/Admin/AdminOrganizationsPage.test.ts,
    src/Pages/__tests__/Admin/AdminModulesPage.test.ts,
    src/Pages/__tests__/Admin/AdminApiKeysPage.test.ts,
    src/Pages/__tests__/Admin/AdminUsageSyncPage.test.ts
  </files>
  <behavior>
    Each file follows the same three-case pattern:
    - Test 1 (PAGE-03): unauthenticated → status 302, Location header contains '/login'
    - Test 2 (PAGE-04): role 'member' authenticated → status 403
    - Test 3 (PAGE-01): role 'admin' authenticated → inertia.render called with correct component string

    AdminDashboardPage component: 'Admin/Dashboard/Index'
      - Injects: InertiaService, ListUsersService, ListOrganizationsService, ListAdminContractsService
      - Mock all three services returning { success: true, data: { meta: { total: 0, page: 1, limit: 1, totalPages: 0 } } }

    AdminUsersPage component: 'Admin/Users/Index'
      - Injects: InertiaService, ListUsersService
      - Mock listService.execute returning { success: true, data: { users: [], meta: { total: 0, page: 1, limit: 20, totalPages: 0 } } }

    AdminOrganizationsPage component: 'Admin/Organizations/Index'
      - Injects: InertiaService, ListOrganizationsService
      - Mock listService.execute returning { success: true, data: { organizations: [], meta: { total: 0, page: 1, limit: 20, totalPages: 0 } } }

    AdminModulesPage component: 'Admin/Modules/Index'
      - Injects: InertiaService, ListModulesService
      - Mock listService.execute returning { success: true, data: [] }

    AdminApiKeysPage component: 'Admin/ApiKeys/Index'
      - Injects: InertiaService, ListApiKeysService, ListOrganizationsService
      - Mock both services; without orgId query → selectedOrgId: null in props
      - Test 4 (PAGE-01): with orgId query → listKeysService.execute called

    AdminUsageSyncPage component: 'Admin/UsageSync/Index'
      - Injects: InertiaService only
      - Test 3 (PAGE-01): verify props contain status.enabled === false (static placeholder)
  </behavior>
  <action>
    Create a shared helper at the TOP of each test file (not a shared module — inline the helpers per file for zero coupling).

    The auth store key is the string 'auth' (confirmed from AuthMiddleware.ts line 77: `ctx.set('auth', authContext)`).

    Helper factories to inline in every test file:

    ```typescript
    import { describe, expect, test, mock } from 'bun:test'
    import type { IHttpContext } from '@/Shared/Presentation/IHttpContext'
    import type { InertiaService } from '../../InertiaService'

    function createMockContext(overrides: Partial<IHttpContext> = {}): IHttpContext {
      const store = new Map<string, unknown>()
      return {
        getBodyText: async () => '',
        getJsonBody: async <T>() => ({}) as T,
        getBody: async <T>() => ({}) as T,
        getHeader: () => undefined,
        getParam: () => undefined,
        getPathname: () => '/admin/test',
        getQuery: () => undefined,
        params: {},
        query: {},
        headers: {},
        json: (data: unknown, statusCode?: number) =>
          Response.json(data, { status: statusCode ?? 200 }),
        text: (content: string, statusCode?: number) =>
          new Response(content, { status: statusCode ?? 200 }),
        redirect: (url: string, statusCode?: number) => Response.redirect(url, statusCode ?? 302),
        get: <T>(key: string) => store.get(key) as T | undefined,
        set: (key: string, value: unknown) => { store.set(key, value) },
        ...overrides,
      }
    }

    function createAdminContext(): IHttpContext {
      return createMockContext({
        get: <T>(key: string) => {
          if (key === 'auth') return { userId: 'admin-1', email: 'admin@test.com', role: 'admin' } as T
          return undefined
        },
      })
    }

    function createMemberContext(): IHttpContext {
      return createMockContext({
        get: <T>(key: string) => {
          if (key === 'auth') return { userId: 'user-1', email: 'user@test.com', role: 'member' } as T
          return undefined
        },
      })
    }

    type InertiaCapture = { component: string; props: Record<string, unknown> } | null

    function createMockInertia(): { inertia: InertiaService; captured: { lastCall: InertiaCapture } } {
      const captured = { lastCall: null as InertiaCapture }
      const inertia = {
        render: (_ctx: IHttpContext, component: string, props: Record<string, unknown>) => {
          captured.lastCall = { component, props }
          return new Response(JSON.stringify({ component, props }), {
            headers: { 'Content-Type': 'application/json' },
          })
        },
      } as unknown as InertiaService
      return { inertia, captured }
    }
    ```

    For each page class, write the three required tests plus any additional behavior tests (e.g., AdminApiKeysPage orgId branch, AdminUsageSyncPage static props).

    Use `mock(() => Promise.resolve(...))` from `bun:test` for all service method mocks.

    AdminApiKeysPage test 4 — with orgId query: use `createMockContext` with `getQuery: (key: string) => key === 'orgId' ? 'org-123' : undefined`.

    AdminUsageSyncPage test 3 — verify static props: after `page.handle(adminCtx)`, assert `captured.lastCall?.props.status` is an object with `enabled === false`.

    TypeScript import paths use `@/` alias (e.g., `import type { IHttpContext } from '@/Shared/Presentation/IHttpContext'`).
    Import the page class with a relative path from the test file location:
    `import { AdminDashboardPage } from '../../Admin/AdminDashboardPage'`
    (test files are in src/Pages/__tests__/Admin/, page classes are in src/Pages/Admin/)
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && bun test src/Pages/__tests__/Admin/AdminDashboardPage.test.ts src/Pages/__tests__/Admin/AdminUsersPage.test.ts src/Pages/__tests__/Admin/AdminOrganizationsPage.test.ts src/Pages/__tests__/Admin/AdminModulesPage.test.ts src/Pages/__tests__/Admin/AdminApiKeysPage.test.ts src/Pages/__tests__/Admin/AdminUsageSyncPage.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 test files exists and is non-empty
    - `bun test src/Pages/__tests__/Admin` exits 0 for these 6 files
    - Each file contains at least 3 `test(` or `it(` calls
    - AdminDashboardPage.test.ts contains `'Admin/Dashboard/Index'`
    - AdminUsersPage.test.ts contains `'Admin/Users/Index'`
    - AdminOrganizationsPage.test.ts contains `'Admin/Organizations/Index'`
    - AdminModulesPage.test.ts contains `'Admin/Modules/Index'`
    - AdminApiKeysPage.test.ts contains `'Admin/ApiKeys/Index'`
    - AdminUsageSyncPage.test.ts contains `enabled` and `false`
    - All 6 files contain `store.set('auth'` or the equivalent auth context injection
  </acceptance_criteria>
  <done>6 test files exist, all tests pass, each page's auth guard and component name are verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin pages with POST handlers (UserDetail, OrganizationDetail, ContractCreate, ContractDetail, ModuleCreate)</name>
  <read_first>
    - src/Pages/Admin/AdminUserDetailPage.ts
    - src/Pages/Admin/AdminOrganizationDetailPage.ts
    - src/Pages/Admin/AdminContractCreatePage.ts
    - src/Pages/Admin/AdminContractDetailPage.ts
    - src/Pages/Admin/AdminModuleCreatePage.ts
    - src/Pages/__tests__/Admin/AdminDashboardPage.test.ts (Task 1 output — helpers pattern)
  </read_first>
  <files>
    src/Pages/__tests__/Admin/AdminUserDetailPage.test.ts,
    src/Pages/__tests__/Admin/AdminOrganizationDetailPage.test.ts,
    src/Pages/__tests__/Admin/AdminContractCreatePage.test.ts,
    src/Pages/__tests__/Admin/AdminContractDetailPage.test.ts,
    src/Pages/__tests__/Admin/AdminModuleCreatePage.test.ts
  </files>
  <behavior>
    Per file, three standard auth cases (unauthenticated=302, member=403, admin=component), plus POST handler tests:

    AdminUserDetailPage:
      - handle: missing userId param → render 'Admin/Users/Show' with user: null
      - handle: userId present → render 'Admin/Users/Show' with user object
      - postStatus: valid status 'suspended' → redirect to /admin/users/:id
      - postStatus: invalid status → redirect to /admin/users/:id (no change)
      Services: GetProfileService, GetUserDetailService, ChangeUserStatusService
      Component: 'Admin/Users/Show'

    AdminOrganizationDetailPage:
      - handle: missing orgId param → render 'Admin/Organizations/Show' with organization: null
      - handle: orgId present → render 'Admin/Organizations/Show'
      Services: GetOrganizationService, ListMembersService
      Component: 'Admin/Organizations/Show'

    AdminContractCreatePage:
      - handle: render 'Admin/Contracts/Create' with formError: null
      - store: incomplete body (missing targetId) → re-render with formError: '請填寫完整欄位（含目標與條款）'
      - store: valid body + service success → redirect to /admin/contracts/:id
      - store: service failure → re-render with formError from result.message
      Service: CreateContractService
      Component: 'Admin/Contracts/Create'

    AdminContractDetailPage:
      - handle: missing contractId param → render 'Admin/Contracts/Show' with contract: null
      - handle: contractId present → render 'Admin/Contracts/Show'
      - postAction: action 'activate' → activateContractService.execute called, redirect to /admin/contracts/:id
      - postAction: action 'terminate' → terminateContractService.execute called, redirect to /admin/contracts/:id
      - postAction: unknown action → neither service called, redirect to /admin/contracts/:id
      Services: GetContractDetailService, ActivateContractService, TerminateContractService
      Component: 'Admin/Contracts/Show'

    AdminModuleCreatePage:
      - handle: render 'Admin/Modules/Create' with formError: null
      - store: empty name → re-render with formError: '模組識別名稱為必填'
      - store: valid name + service success → redirect to /admin/modules
      - store: service failure → re-render with formError from result.message
      Service: RegisterModuleService
      Component: 'Admin/Modules/Create'
  </behavior>
  <action>
    Use the exact same helper factories as Task 1 (inline in each file):
    `createMockContext`, `createAdminContext`, `createMemberContext`, `createMockInertia`.

    For POST tests, create a `createAdminContextWithBody(body: unknown)` helper that returns an `IHttpContext` with:
    - `getJsonBody: async <T>() => body as T`
    - auth context injected via the `get` override

    ```typescript
    function createAdminContextWithBody(body: unknown, overrides: Partial<IHttpContext> = {}): IHttpContext {
      return createMockContext({
        get: <T>(key: string) => {
          if (key === 'auth') return { userId: 'admin-1', email: 'admin@test.com', role: 'admin' } as T
          return undefined
        },
        getJsonBody: async <T>() => body as T,
        getParam: (name: string) => {
          // caller can override with the overrides spread below
          return undefined
        },
        ...overrides,
      })
    }
    ```

    For `getParam` overrides (e.g., to pass `id` = 'contract-123'), use the `overrides` spread:
    ```typescript
    createAdminContextWithBody({ action: 'activate' }, {
      getParam: (name: string) => name === 'id' ? 'contract-123' : undefined,
    })
    ```

    For AdminContractCreatePage.store valid body, use:
    ```typescript
    {
      targetType: 'organization',
      targetId: 'org-abc',
      terms: {
        creditQuota: 100,
        allowedModules: ['module-1'],
        rateLimit: { rpm: 60, tpm: 10000 },
        validityPeriod: { startDate: '2026-01-01', endDate: '2026-12-31' },
      },
    }
    ```
    Mock `createContractService.execute` to return `{ success: true, data: { id: 'contract-new-1' } }`.

    For postStatus tests, mock `changeUserStatusService.execute` to return `{ success: true }`.

    All mock functions: `mock(() => Promise.resolve({ ... }))`.

    Relative import path from `src/Pages/__tests__/Admin/`:
    `import { AdminUserDetailPage } from '../../Admin/AdminUserDetailPage'`
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && bun test src/Pages/__tests__/Admin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist and are non-empty
    - `bun test src/Pages/__tests__/Admin` exits 0 (all 12 admin test files pass)
    - AdminUserDetailPage.test.ts contains `postStatus` and `302`
    - AdminContractCreatePage.test.ts contains `'請填寫完整欄位（含目標與條款）'`
    - AdminContractDetailPage.test.ts contains `'activate'` and `'terminate'`
    - AdminModuleCreatePage.test.ts contains `'模組識別名稱為必填'`
    - Total test count for `bun test src/Pages/__tests__/Admin` is >= 40 passing tests
  </acceptance_criteria>
  <done>All 12 admin page test files exist and pass; every page's auth guard, GET handler, and POST handler behaviors are covered</done>
</task>

</tasks>

<verification>
Run full admin test suite:
`bun test src/Pages/__tests__/Admin`

Expected: all 12 test files pass, zero failures.

Then run full Pages suite to confirm no regressions:
`bun test src/Pages`

Expected: all pre-existing InertiaService + ViteTagHelper tests still pass.
</verification>

<success_criteria>
- 12 test files exist under src/Pages/__tests__/Admin/
- `bun test src/Pages/__tests__/Admin` exits 0
- Every admin page class has: unauthenticated=302, member=403, admin=correct-component tests
- Every admin POST handler has: success path and validation failure path tests
- `bun test src/Pages` exits 0 (no regressions in InertiaService.test.ts or ViteTagHelper.test.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pages/06-01-SUMMARY.md`
</output>
