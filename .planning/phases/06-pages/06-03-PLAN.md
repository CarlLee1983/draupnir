---
phase: 06-pages
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "All 16 admin page routes return a non-404 response in the feature test suite"
    - "All 9 member page routes return a non-404 response in the feature test suite"
    - "routes-existence.test.ts contains describe blocks for Admin Pages and Member Pages modules"
  artifacts:
    - path: "tests/Feature/routes-existence.test.ts"
      provides: "Route existence assertions for all 25 Inertia page routes"
      contains: "Admin Pages Module"
  key_links:
    - from: "tests/Feature/routes-existence.test.ts"
      to: "src/Pages/routing/registerAdminPageRoutes.ts"
      via: "HTTP GET/POST to live test server spawned by setupTestServer()"
      pattern: "client\\.get.*admin|client\\.post.*admin"
---

<objective>
Extend `tests/Feature/routes-existence.test.ts` with 25 new route assertions covering all admin and member Inertia page routes.

Purpose: The feature test suite verifies every API module route exists, but has zero coverage of the 25 Inertia page routes (`/admin/*`, `/member/*`). Unauthenticated requests to these routes return 302 (redirect to /login), which is not 404. The test pattern is: `expect(response.status).not.toBe(404)`.

Output: Updated `tests/Feature/routes-existence.test.ts` with two new `describe` blocks appended at the end of the outer describe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pages/06-RESEARCH.md

<interfaces>
<!-- tests/Feature/routes-existence.test.ts — current pattern (lines 1-15): -->
```typescript
import { describe, it, expect, beforeAll } from 'bun:test'
import { TestClient } from './lib/test-client'
import { setupTestServer, getBaseURL } from './lib/test-server'

setupTestServer()

let client: TestClient

beforeAll(() => {
  client = new TestClient(getBaseURL())
})

describe('Routes Existence Verification (不返回 404 就代表存在)', () => {
  // ... existing describe blocks for Health, Auth, Profile, Organization, etc.
})
```

<!-- TestClient methods available: client.get(path), client.post(path, body), client.put(path, body), client.patch(path, body) -->
<!-- Page routes — unauthenticated requests return 302 (not 404), so the test passes -->

<!-- Admin routes to add (16 routes): -->
<!-- GET  /admin/dashboard -->
<!-- GET  /admin/users -->
<!-- GET  /admin/users/:id  → use /admin/users/test-id -->
<!-- POST /admin/users/:id/status  → use /admin/users/test-id/status -->
<!-- GET  /admin/organizations -->
<!-- GET  /admin/organizations/:id  → use /admin/organizations/test-id -->
<!-- GET  /admin/contracts -->
<!-- GET  /admin/contracts/create  (MUST be before /:id) -->
<!-- POST /admin/contracts  → body {} -->
<!-- GET  /admin/contracts/:id  → use /admin/contracts/test-id -->
<!-- POST /admin/contracts/:id/action  → use /admin/contracts/test-id/action, body {} -->
<!-- GET  /admin/modules -->
<!-- GET  /admin/modules/create  (MUST be before /:id if any) -->
<!-- POST /admin/modules  → body {} -->
<!-- GET  /admin/api-keys -->
<!-- GET  /admin/usage-sync -->

<!-- Member routes to add (9 routes): -->
<!-- GET  /member/dashboard -->
<!-- GET  /member/api-keys -->
<!-- GET  /member/api-keys/create  (MUST be before /:keyId) -->
<!-- POST /member/api-keys  → body {} -->
<!-- POST /member/api-keys/:keyId/revoke  → use /member/api-keys/test-key/revoke, body {} -->
<!-- GET  /member/usage -->
<!-- GET  /member/contracts -->
<!-- GET  /member/settings -->
<!-- PUT  /member/settings  → body {} -->

<!-- test-server.ts spawns real Bun server via `bun run src/index.ts` with ORM=memory -->
<!-- No warmInertiaService() needed — the full server boot handles that -->
<!-- Page routes return 302 for unauthenticated (not 404) — test assertions will pass -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Admin Pages and Member Pages route existence assertions</name>
  <read_first>
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent
    - src/Pages/routing/registerAdminPageRoutes.ts
    - src/Pages/routing/registerMemberPageRoutes.ts
  </read_first>
  <files>tests/Feature/routes-existence.test.ts</files>
  <action>
    Append two new `describe` blocks inside the outer `describe('Routes Existence Verification ...')` block, immediately before its closing `})`.

    The existing file ends with:
    ```typescript
      describe('API Root Route', () => {
        it('GET /api 存在', async () => {
          const response = await client.get('/api')
          expect(response.status).not.toBe(404)
        })
      })
    })
    ```

    Replace that final section with:
    ```typescript
      describe('API Root Route', () => {
        it('GET /api 存在', async () => {
          const response = await client.get('/api')
          expect(response.status).not.toBe(404)
        })
      })

      describe('Admin Pages Module', () => {
        it('GET /admin/dashboard 存在', async () => {
          const response = await client.get('/admin/dashboard')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/users 存在', async () => {
          const response = await client.get('/admin/users')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/users/:id 存在', async () => {
          const response = await client.get('/admin/users/test-id')
          expect(response.status).not.toBe(404)
        })

        it('POST /admin/users/:id/status 存在', async () => {
          const response = await client.post('/admin/users/test-id/status', {})
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/organizations 存在', async () => {
          const response = await client.get('/admin/organizations')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/organizations/:id 存在', async () => {
          const response = await client.get('/admin/organizations/test-id')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/contracts 存在', async () => {
          const response = await client.get('/admin/contracts')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/contracts/create 存在', async () => {
          const response = await client.get('/admin/contracts/create')
          expect(response.status).not.toBe(404)
        })

        it('POST /admin/contracts 存在', async () => {
          const response = await client.post('/admin/contracts', {})
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/contracts/:id 存在', async () => {
          const response = await client.get('/admin/contracts/test-id')
          expect(response.status).not.toBe(404)
        })

        it('POST /admin/contracts/:id/action 存在', async () => {
          const response = await client.post('/admin/contracts/test-id/action', {})
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/modules 存在', async () => {
          const response = await client.get('/admin/modules')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/modules/create 存在', async () => {
          const response = await client.get('/admin/modules/create')
          expect(response.status).not.toBe(404)
        })

        it('POST /admin/modules 存在', async () => {
          const response = await client.post('/admin/modules', {})
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/api-keys 存在', async () => {
          const response = await client.get('/admin/api-keys')
          expect(response.status).not.toBe(404)
        })

        it('GET /admin/usage-sync 存在', async () => {
          const response = await client.get('/admin/usage-sync')
          expect(response.status).not.toBe(404)
        })
      })

      describe('Member Pages Module', () => {
        it('GET /member/dashboard 存在', async () => {
          const response = await client.get('/member/dashboard')
          expect(response.status).not.toBe(404)
        })

        it('GET /member/api-keys 存在', async () => {
          const response = await client.get('/member/api-keys')
          expect(response.status).not.toBe(404)
        })

        it('GET /member/api-keys/create 存在', async () => {
          const response = await client.get('/member/api-keys/create')
          expect(response.status).not.toBe(404)
        })

        it('POST /member/api-keys 存在', async () => {
          const response = await client.post('/member/api-keys', {})
          expect(response.status).not.toBe(404)
        })

        it('POST /member/api-keys/:keyId/revoke 存在', async () => {
          const response = await client.post('/member/api-keys/test-key/revoke', {})
          expect(response.status).not.toBe(404)
        })

        it('GET /member/usage 存在', async () => {
          const response = await client.get('/member/usage')
          expect(response.status).not.toBe(404)
        })

        it('GET /member/contracts 存在', async () => {
          const response = await client.get('/member/contracts')
          expect(response.status).not.toBe(404)
        })

        it('GET /member/settings 存在', async () => {
          const response = await client.get('/member/settings')
          expect(response.status).not.toBe(404)
        })

        it('PUT /member/settings 存在', async () => {
          const response = await client.put('/member/settings', {})
          expect(response.status).not.toBe(404)
        })
      })
    })
    ```

    IMPORTANT: The `client.put` method must exist on TestClient. Before writing, verify `tests/Feature/lib/test-client.ts` has a `put` method. If it does not, add it to `test-client.ts` following the same pattern as `post`:
    `put(path: string, body: unknown): Promise<Response>`

    Do not change any existing test cases. Only append the two new describe blocks.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && bun test tests/Feature/routes-existence.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains the string `'Admin Pages Module'`
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains the string `'Member Pages Module'`
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains `'/admin/dashboard'`
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains `'/admin/usage-sync'`
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains `'/member/settings'`
    - tests/Feature/routes-existence.test.ts
  - tests/Feature/lib/test-client.ts  # conditional: only if put() method is absent contains `'/member/api-keys/test-key/revoke'`
    - `bun test tests/Feature/routes-existence.test.ts` exits 0 with all tests passing
    - No existing tests in routes-existence.test.ts were modified (grep for original first test `'GET /health 存在'` still present)
  </acceptance_criteria>
  <done>
    routes-existence.test.ts has 25 new page route assertions. All assertions pass (page routes return 302 not 404 for unauthenticated requests). Full routes-existence test suite exits 0.
  </done>
</task>

</tasks>

<verification>
Run the routes-existence test:
`bun test tests/Feature/routes-existence.test.ts`

Expected: all existing API module tests + all 25 new page route tests pass.

Then run the full test suite for final gate:
`bun test`

Expected: zero failures across unit + feature tests.

Finally run quality gate:
`bun run lint && bun run typecheck`

Expected: zero errors.
</verification>

<success_criteria>
- tests/Feature/routes-existence.test.ts contains describe blocks for 'Admin Pages Module' and 'Member Pages Module'
- 16 admin route assertions + 9 member route assertions added (25 total)
- `bun test tests/Feature/routes-existence.test.ts` exits 0
- `bun test` exits 0 (full suite)
- `bun run lint` exits 0
- `bun run typecheck` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-pages/06-03-SUMMARY.md`
</output>
