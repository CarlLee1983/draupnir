---
phase: 16-automated-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Foundation/Infrastructure/Ports/IMailer.ts
  - src/Foundation/Infrastructure/Services/Mail/UpyoMailer.ts
  - src/Foundation/Infrastructure/Services/Mail/ConsoleMailer.ts
  - src/Modules/Reports/Domain/Aggregates/ReportSchedule.ts
  - src/Modules/Reports/Domain/Repositories/IReportRepository.ts
  - src/Modules/Reports/Domain/ValueObjects/ReportToken.ts
  - src/Modules/Reports/Infrastructure/Repositories/DrizzleReportRepository.ts
  - src/Modules/Reports/Application/Services/GeneratePdfService.ts
  - src/Modules/Reports/Application/Services/ScheduleReportService.ts
  - src/Modules/Reports/Application/Services/SendReportEmailService.ts
  - src/Modules/Reports/Presentation/Controllers/ReportController.ts
  - src/Modules/Reports/Infrastructure/Providers/ReportsServiceProvider.ts
  - database/migrations/2026_04_13_000001_create_report_schedules_table.ts
autonomous: true
requirements: [REPT-01, REPT-02, REPT-03, REPT-04]
user_setup:
  - service: playwright
    why: "PDF generation requires Playwright Chromium"
    env_vars:
      - name: REPORT_SIGNING_SECRET
        source: "Generate a secure random string"

must_haves:
  truths:
    - "ReportSchedule aggregate generates valid cron strings for weekly/monthly frequencies"
    - "GeneratePdfService renders a URL and returns a PDF buffer"
    - "ScheduleReportService registers and manages jobs in-memory with croner"
  artifacts:
    - path: "src/Modules/Reports/Domain/Aggregates/ReportSchedule.ts"
      provides: "Report schedule domain model"
    - path: "src/Modules/Reports/Application/Services/GeneratePdfService.ts"
      provides: "PDF generation via Playwright"
    - path: "src/Modules/Reports/Application/Services/ScheduleReportService.ts"
      provides: "Timezone-aware scheduling via croner"
  key_links:
    - from: "src/Modules/Reports/Application/Services/ScheduleReportService.ts"
      to: "src/Modules/Reports/Application/Services/GeneratePdfService.ts"
      via: "Method call inside croner job callback"
---

<objective>
Implement the backend foundation for automated reports, including domain models, PDF generation, timezone-aware scheduling, and email delivery.

Purpose: Provide the core engine that generates and sends scheduled reports automatically.
Output: Report schedule repository, PDF generator, scheduler service, and report configuration API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-automated-reports/16-RESEARCH.md
@.planning/phases/16-automated-reports/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Install dependencies and update mailer</name>
  <files>package.json, src/Foundation/Infrastructure/Ports/IMailer.ts, src/Foundation/Infrastructure/Services/Mail/UpyoMailer.ts, src/Foundation/Infrastructure/Services/Mail/ConsoleMailer.ts</files>
  <action>
    1. Install `croner`, `playwright`, and `luxon`.
    2. Run `npx playwright install chromium`.
    3. Update `MailMessage` interface in `IMailer.ts` to include `attachments?: Array<{ filename: string; content: Buffer }>` (per D-05).
    4. Update `UpyoMailer.ts` and `ConsoleMailer.ts` to handle the new attachments field.
  </action>
  <verify>
    <automated>bun install && npx playwright install chromium && bun run typecheck</automated>
  </verify>
  <done>Dependencies installed and IMailer supports attachments</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Domain + Persistence Layer</name>
  <files>src/Modules/Reports/Domain/Aggregates/ReportSchedule.ts, src/Modules/Reports/Domain/Repositories/IReportRepository.ts, src/Modules/Reports/Domain/ValueObjects/ReportToken.ts, database/migrations/2026_04_13_000001_create_report_schedules_table.ts, src/Modules/Reports/Infrastructure/Repositories/DrizzleReportRepository.ts</files>
  <behavior>
    - ReportSchedule: generates correct cron strings ('0 9 * * 1' for weekly Mon 9AM, '0 9 1 * *' for monthly 1st 9AM).
    - ReportToken: signs and verifies tokens with HMAC-SHA256, respects expiry.
    - Migration: creates `report_schedules` table with id, org_id, type (weekly/monthly), day (0-6 or 1-31), time (HH:mm), timezone (IANA), recipients (JSON), enabled (boolean).
  </behavior>
  <action>
    1. Create `ReportSchedule` aggregate and `ReportToken` VO.
    2. Create `IReportRepository` interface.
    3. Create and run migration for `report_schedules` table.
    4. Implement `DrizzleReportRepository`.
  </action>
  <verify>
    <automated>bun test src/Modules/Reports/__tests__/ReportSchedule.test.ts src/Modules/Reports/__tests__/ReportToken.test.ts</automated>
  </verify>
  <done>Domain models and repository implemented with TDD coverage</done>
</task>

<task type="auto">
  <name>Task 2: Application Services + Presentation</name>
  <files>src/Modules/Reports/Application/Services/GeneratePdfService.ts, src/Modules/Reports/Application/Services/ScheduleReportService.ts, src/Modules/Reports/Application/Services/SendReportEmailService.ts, src/Modules/Reports/Presentation/Controllers/ReportController.ts, src/Modules/Reports/Infrastructure/Providers/ReportsServiceProvider.ts, src/routes.ts</files>
  <action>
    1. Implement `GeneratePdfService` using Playwright: launch chromium, goto signed URL with token, wait for networkidle, return buffer from `page.pdf()`. Ensure `isAnimationActive={false}` in URL params (per D-04).
    2. Implement `ScheduleReportService`: on startup, load enabled schedules and register `croner` jobs. On job execution, call `GeneratePdfService` and `SendReportEmailService`.
    3. Implement `SendReportEmailService`: construct email and attach the PDF buffer.
    4. Implement `ReportController` for CRUD on report schedules.
    5. Wire everything in `ReportsServiceProvider` and register admin routes.
  </action>
  <verify>
    <automated>bun test src/Modules/Reports/__tests__/PdfGenerator.integration.test.ts</automated>
  </verify>
  <done>Backend services and API routes operational</done>
</task>

</tasks>

<verification>
1. Verify `ReportSchedule` unit tests pass.
2. Verify `GeneratePdfService` produces a non-empty PDF buffer in integration tests.
3. Verify that `croner` jobs can be scheduled and triggered (mocked time if possible).
</verification>

<success_criteria>
- All backend services are implemented and covered by tests.
- Report schedule CRUD is available at `/v1/admin/reports`.
- PDF generation works correctly with Playwright.
</success_criteria>

<output>
After completion, create `.planning/phases/16-automated-reports/16-01-SUMMARY.md`
</output>
