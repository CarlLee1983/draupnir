---
phase: 16-automated-reports
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/Pages/Admin/Reports/ReportsPage.tsx
  - src/Pages/Admin/Reports/ReportTemplatePage.tsx
  - src/Pages/Admin/Reports/Components/ReportForm.tsx
  - src/Pages/Admin/Reports/Components/TimezonePicker.tsx
  - src/Modules/Reports/Presentation/Controllers/ReportController.ts
autonomous: false
requirements: [REPT-01, REPT-02, REPT-04]
must_haves:
  truths:
    - "Admin can visually configure a report schedule and select an IANA timezone"
    - "Report template renders correctly without animations in 'printer-friendly' mode"
  artifacts:
    - path: "src/Pages/Admin/Reports/ReportsPage.tsx"
      provides: "Reports management dashboard"
    - path: "src/Pages/Admin/Reports/ReportTemplatePage.tsx"
      provides: "Printer-friendly report template"
  key_links:
    - from: "src/Pages/Admin/Reports/Components/ReportForm.tsx"
      to: "src/Modules/Reports/Presentation/Controllers/ReportController.ts"
      via: "Inertia post/put"
---

<objective>
Implement the frontend management UI for automated reports, including schedule configuration and a printer-friendly report template.

Purpose: Allow admins to visually manage report schedules and provide a high-fidelity template for PDF generation.
Output: Reports management dashboard, schedule CRUD form, and PDF-ready report template.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-automated-reports/16-01-SUMMARY.md
@.planning/phases/16-automated-reports/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reports Management UI</name>
  <files>src/Pages/Admin/Reports/ReportsPage.tsx, src/Pages/Admin/Reports/Components/ReportForm.tsx, src/Pages/Admin/Reports/Components/TimezonePicker.tsx</files>
  <action>
    1. Create `ReportsPage` at `/admin/reports` with a list of active schedules.
    2. Create `ReportForm` component:
       - Type selector: Weekly (Mon-Sun) or Monthly (1st-28th).
       - Time selector: HH:mm format.
       - Timezone picker: Use `luxon` to list IANA timezones (standardized search/select).
       - Recipients input: List of email addresses.
    3. Use existing `Inertia` form handling for CRUD operations against `ReportController`.
  </action>
  <verify>
    <automated>bun run typecheck:frontend</automated>
  </verify>
  <done>Admins can configure report schedules via the web UI</done>
</task>

<task type="auto">
  <name>Task 2: PDF Report Template</name>
  <files>src/Pages/Admin/Reports/ReportTemplatePage.tsx, src/Modules/Reports/Presentation/Controllers/ReportController.ts</files>
  <action>
    1. Create `ReportTemplatePage.tsx`:
       - A printer-friendly layout (A4 width optimization).
       - Reuse charts from Phase 10/14 (KPI cards, Trend charts, Cost Breakdown).
       - IMPORTANT: Pass `isAnimationActive={false}` to all Recharts components (per D-04).
       - Hide navigation, sidebars, and interactive elements (printer-only view).
    2. Update `ReportController` to provide a dedicated route for this page, protected by `ReportToken` verification.
    3. Add a "Preview" button in the management UI that opens this page in a new tab.
  </action>
  <verify>
    <automated>bun run typecheck:frontend</automated>
  </verify>
  <done>High-fidelity, animation-free report template available for PDF generation</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual Verification</name>
  <files>src/Pages/Admin/Reports/ReportsPage.tsx</files>
  <action>Verify the UI and template visually.</action>
  <verify>Human inspection</verify>
  <done>UI approved</done>
  <what-built>Automated Reports UI and Template</what-built>
  <how-to-verify>
    1. Navigate to /admin/reports.
    2. Create a weekly report schedule.
    3. Click "Preview" to see the PDF-ready template.
    4. Verify charts are rendered correctly without animations.
    5. Verify the layout looks correct for A4 printing (use browser print preview if needed).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
1. Verify `ReportsPage` renders and form validation works.
2. Verify `ReportTemplatePage` loads with correct data and no animations.
3. Verify timezone selection is correctly passed to the backend.
</verification>

<success_criteria>
- Admins can manage schedules in the dashboard.
- Report template accurately mirrors dashboard data.
- PDF generation (from Plan 01) works against the new template.
</success_criteria>

<output>
After completion, create `.planning/phases/16-automated-reports/16-02-SUMMARY.md`
</output>
