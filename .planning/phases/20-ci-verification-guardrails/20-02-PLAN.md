---
phase: 20-ci-verification-guardrails
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - .github/workflows/ci.yml
  - docs/ci/branch-protection-setup.md
autonomous: false
requirements: [CI-01, CI-02, CI-03, CI-04, CI-05, CI-06, CI-07, CI-08]
user_setup: []

must_haves:
  truths:
    - "每次 push 至 main/develop 或 PR 指向 main/develop 時，GitHub Actions 並行觸發 8 個 jobs：typecheck / lint-format / unit-coverage / migration-drift / routes-check / di-audit / e2e-smoke / commitlint"
    - "任一 job 失敗會在 PR 顯示為 red check"
    - "repo admin 可依 docs/ci/branch-protection-setup.md runbook 執行 `gh api` 指令將所有 8 jobs 設為 required checks"
    - "8 jobs 的命名為 kebab-case 且與 runbook 內列出的 required checks 名稱完全一致"
    - "`services.postgres` container 同時提供給 migration-drift 與 e2e-smoke jobs 使用"
    - "`actions/cache` 快取 `~/.bun/install/cache` 與 `node_modules`，key 含 `hashFiles('bun.lock*')`"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "擴充後的 CI workflow，8 個並行 jobs"
      contains: "jobs:"
      min_lines: 120
    - path: "docs/ci/branch-protection-setup.md"
      provides: "branch protection runbook，含 gh api 完整指令"
      contains: "gh api"
      min_lines: 40
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "bunfig.toml"
      via: "unit-coverage job 執行 `bun test --coverage`，自動採用 bunfig 設定"
      pattern: "bun test --coverage"
    - from: ".github/workflows/ci.yml"
      to: "scripts/di-audit.ts"
      via: "di-audit job 執行 `bun run di:audit`"
      pattern: "bun run di:audit"
    - from: ".github/workflows/ci.yml"
      to: "scripts/migration-drift.ts"
      via: "migration-drift job 執行 `bun run migrate` 後呼叫 `bun run migration:drift`"
      pattern: "bun run migration:drift"
    - from: ".github/workflows/ci.yml"
      to: "commitlint.config.cjs"
      via: "commitlint job 以 PR base.sha...head.sha 範圍驗證"
      pattern: "commitlint.*--from.*--to"
    - from: ".github/workflows/ci.yml"
      to: "e2e/*.e2e.ts (@smoke)"
      via: "e2e-smoke job 執行 `bun run test:e2e:smoke`"
      pattern: "test:e2e:smoke|--grep @smoke"
    - from: ".github/workflows/ci.yml"
      to: "tests/Feature/routes-existence.e2e.ts"
      via: "routes-check job 執行 routes test（依 Task 3 檔頭 @ci-runner 指令）"
      pattern: "routes-existence"
    - from: "docs/ci/branch-protection-setup.md"
      to: ".github/workflows/ci.yml"
      via: "runbook 列出的 contexts 必須與 workflow 的 job name 完全一致"
      pattern: "typecheck|lint-format|unit-coverage|migration-drift|routes-check|di-audit|e2e-smoke|commitlint"
---

<objective>
把 Plan 20-01 產出的所有 guardrail 工具接上 GitHub Actions CI pipeline：擴充 `.github/workflows/ci.yml` 為 8 個並行 jobs、共用 Postgres service container、使用 `actions/cache` 快取 bun install、並產出 `docs/ci/branch-protection-setup.md` runbook 供 repo admin 手動設定 required checks。

Purpose: 每個 PR 必須通過全部 8 個守門點才能合併至 main/develop，建立防回歸護欄（CI-01~08 最終落地）。
Output: 擴充後的 `ci.yml`（8 jobs、kebab-case 命名、快取策略）、`docs/ci/branch-protection-setup.md`（`gh api` 完整指令 + 手動設定步驟）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-ci-verification-guardrails/20-CONTEXT.md
@.planning/phases/20-ci-verification-guardrails/20-RESEARCH.md
@.planning/phases/20-ci-verification-guardrails/20-VALIDATION.md
@.planning/phases/20-ci-verification-guardrails/20-01-SUMMARY.md

<interfaces>
<!-- Plan 20-01 產出的入口點，本 plan 直接呼叫 -->

NPM scripts (from Plan 20-01 Task 2 & 4 & 5):
```
"lint:commits": "commitlint --from HEAD~1 --to HEAD --verbose"
"test:e2e:smoke": "playwright test --grep @smoke"
"di:audit": "ORM=memory ... bun scripts/di-audit.ts"
"migration:drift": "bun scripts/migration-drift.ts"
```

Existing NPM scripts:
```
"typecheck": "tsc --noEmit"
"lint": "biome lint src tests packages"
"format:check": "biome format src tests packages --check"
"test": "bun test src tests/Unit packages"
"migrate": "bun orbit migrate"
```

Config files established by Plan 20-01:
- bunfig.toml → `[test].coverageThreshold = 0.8`（unit-coverage job 自動生效）
- commitlint.config.cjs → extends @commitlint/config-conventional

Existing CI workflow（.github/workflows/ci.yml）：
```yaml
name: CI
on:
  push: { branches: [main, develop] }
  pull_request: { branches: [main, develop] }
jobs:
  check:  # 將被拆成 8 個獨立 jobs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun run lint
      - run: bun run format:check
      - run: bun test --coverage
        env:
          ORM: memory
          BIFROST_API_URL: http://localhost:8080
          BIFROST_MASTER_KEY: ci-test-key
          JWT_SECRET: ci-test-secret
```

Postgres service container pattern (from RESEARCH Pattern 1):
```yaml
services:
  postgres:
    image: postgres:16
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: draupnir_ci
    ports: ['5432:5432']
    options: >-
      --health-cmd pg_isready
      --health-interval 10s
      --health-timeout 5s
      --health-retries 5
```
DATABASE_URL for such jobs: `postgres://postgres:postgres@localhost:5432/draupnir_ci`

Required cache key pattern (from D-15):
```yaml
- uses: actions/cache@v4
  with:
    path: |
      ~/.bun/install/cache
      node_modules
    key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
```

8 required job names (kebab-case, MUST match exactly between ci.yml and runbook):
1. typecheck
2. lint-format
3. unit-coverage
4. migration-drift
5. routes-check
6. di-audit
7. e2e-smoke
8. commitlint
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 擴充 .github/workflows/ci.yml 為 8 個並行 jobs（含 Postgres service + cache）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml（既有單一 check job）
    - package.json（確認所有 npm script 名稱正確）
    - .planning/phases/20-ci-verification-guardrails/20-01-SUMMARY.md（Plan 20-01 產出結論）
    - .planning/phases/20-ci-verification-guardrails/20-CONTEXT.md §D-14~D-16
    - .planning/phases/20-ci-verification-guardrails/20-RESEARCH.md §Pattern 1, §Pattern 2, §Pitfall 4, §Pitfall 5
    - playwright.config.ts（確認 webServer 啟動指令供 e2e-smoke job 參考）
    - tests/Feature/routes-existence.e2e.ts 檔頭 `@ci-runner` 註解（由 Plan 20-01 Task 3 寫入）
  </read_first>
  <action>
    完全重寫 `.github/workflows/ci.yml`，從單一 `check` job 擴充為 8 個並行 jobs。保留現有觸發條件（D-16）。

    **完整 YAML 結構**：

    ```yaml
    name: CI

    on:
      push:
        branches: [main, develop]
      pull_request:
        branches: [main, develop]

    jobs:
      typecheck:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with:
              bun-version: latest
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - run: bun run typecheck

      lint-format:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - run: bun run lint
          - run: bun run format:check

      unit-coverage:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - name: Run tests with coverage threshold (from bunfig.toml)
            run: bun test --coverage
            env:
              ORM: memory
              BIFROST_API_URL: http://localhost:8080
              BIFROST_MASTER_KEY: ci-test-key
              JWT_SECRET: ci-test-secret
          - name: Upload coverage artifact
            if: always()
            uses: actions/upload-artifact@v4
            with:
              name: coverage-lcov
              path: coverage/

      migration-drift:
        runs-on: ubuntu-latest
        services:
          postgres:
            image: postgres:16
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: draupnir_ci
            ports: ['5432:5432']
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/draupnir_ci
          ORM: drizzle
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - name: Apply migrations
            run: bun run migrate
          - name: Check migration schema drift
            run: bun run migration:drift

      routes-check:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - name: Run routes invariance test
            # 指令依 Plan 20-01 Task 3 寫入 tests/Feature/routes-existence.e2e.ts 檔頭的 @ci-runner 註解
            # 若是 bun test runner：bun test tests/Feature/routes-existence.e2e.ts
            # 若是 playwright runner：bunx playwright test tests/Feature/routes-existence.e2e.ts
            run: bun test tests/Feature/routes-existence.e2e.ts
            env:
              ORM: memory
              BIFROST_API_URL: http://localhost:8080
              BIFROST_MASTER_KEY: ci-test-key
              JWT_SECRET: ci-test-secret

      di-audit:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - run: bun run di:audit

      e2e-smoke:
        runs-on: ubuntu-latest
        services:
          postgres:
            image: postgres:16
            env:
              POSTGRES_USER: postgres
              POSTGRES_PASSWORD: postgres
              POSTGRES_DB: draupnir_ci
            ports: ['5432:5432']
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/draupnir_ci
          ORM: drizzle
          BIFROST_API_URL: http://localhost:8080
          BIFROST_MASTER_KEY: ci-test-key
          JWT_SECRET: ci-test-secret
        steps:
          - uses: actions/checkout@v4
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - name: Install Playwright browsers
            run: bunx playwright install --with-deps chromium
          - name: Apply migrations
            run: bun run migrate
          - name: Run E2E smoke tests
            run: bun run test:e2e:smoke
          - name: Upload Playwright report on failure
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: playwright-report
              path: playwright-report/
              retention-days: 7

      commitlint:
        runs-on: ubuntu-latest
        if: github.event_name == 'pull_request'
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0
          - uses: oven-sh/setup-bun@v2
            with: { bun-version: latest }
          - uses: actions/cache@v4
            with:
              path: |
                ~/.bun/install/cache
                node_modules
              key: bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}
              restore-keys: |
                bun-${{ runner.os }}-
          - run: bun install --frozen-lockfile
          - name: Lint commits in PR range
            run: |
              bunx commitlint \
                --from ${{ github.event.pull_request.base.sha }} \
                --to   ${{ github.event.pull_request.head.sha }} \
                --verbose
    ```

    **關鍵要點**：

    1. **Job 命名 MUST 完全符合 kebab-case 清單**：typecheck / lint-format / unit-coverage / migration-drift / routes-check / di-audit / e2e-smoke / commitlint（這些名稱會寫進 Task 2 runbook，必須精確一致）

    2. **Cache key**（D-15）：`bun-${{ runner.os }}-${{ hashFiles('bun.lock', 'bun.lockb') }}`；`restore-keys` 允許回退以加速 cache miss

    3. **commitlint job 僅 PR 觸發**（per RESEARCH Pattern 2 / Pitfall 3）：`if: github.event_name == 'pull_request'` + `fetch-depth: 0`

    4. **Postgres service 只用於 migration-drift + e2e-smoke**（D-05）：其他 jobs 用 `ORM: memory` 避免啟動成本

    5. **env vars 沿用既有 dummy values**（CI-existing pattern）：`ORM: memory` / `BIFROST_API_URL` / `BIFROST_MASTER_KEY` / `JWT_SECRET`

    6. **routes-check 指令**：依 Plan 20-01 Task 3 寫入 `tests/Feature/routes-existence.e2e.ts` 檔頭的 `@ci-runner` 註解決定（預設假設 `bun test`，若該 Task 3 結論為 playwright 則改為 `bunx playwright test tests/Feature/routes-existence.e2e.ts`）

    7. **e2e-smoke Playwright 瀏覽器安裝**：`bunx playwright install --with-deps chromium`（避免全部瀏覽器下載；per Pitfall 5 控時）

    8. **Playwright report artifact** 只在 fail 時上傳，保存 7 天（方便除錯但不污染 storage）

    9. **不抽象 reusable workflow**（per RESEARCH §Alternatives Considered）：copy-paste 8 次 checkout/setup-bun/cache/install 是刻意設計

    **禁止**：
    - 不合併 jobs（如「static checks」包 typecheck + lint）— 違反 D-14
    - 不改 on.push / on.pull_request 條件（D-16）
    - 不添加 branch protection 設定於 YAML（D-21，僅產 runbook）
    - 不引入 `workflow_dispatch` 以外的新觸發條件（此 phase 不需）
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; grep -cE "^\s{2}(typecheck|lint-format|unit-coverage|migration-drift|routes-check|di-audit|e2e-smoke|commitlint):" .github/workflows/ci.yml | grep -q "^8$" &amp;&amp; grep -q "oven-sh/setup-bun@v2" .github/workflows/ci.yml &amp;&amp; grep -q "actions/cache@v4" .github/workflows/ci.yml &amp;&amp; grep -q "postgres:16" .github/workflows/ci.yml &amp;&amp; grep -q "bun run di:audit" .github/workflows/ci.yml &amp;&amp; grep -q "bun run migration:drift" .github/workflows/ci.yml &amp;&amp; grep -q "bun run test:e2e:smoke\|--grep @smoke" .github/workflows/ci.yml &amp;&amp; grep -q "routes-existence" .github/workflows/ci.yml &amp;&amp; grep -q "github.event.pull_request.base.sha" .github/workflows/ci.yml &amp;&amp; grep -q "fetch-depth: 0" .github/workflows/ci.yml &amp;&amp; grep -q "bunx playwright install" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` 存在且為有效 YAML（可 `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` 解析成功）
    - 含 8 個 top-level jobs，名稱精確為：`typecheck` / `lint-format` / `unit-coverage` / `migration-drift` / `routes-check` / `di-audit` / `e2e-smoke` / `commitlint`
    - 每個 job 均 `runs-on: ubuntu-latest`
    - 每個 job 均使用 `actions/checkout@v4` + `oven-sh/setup-bun@v2` + `actions/cache@v4`
    - cache key 含 `hashFiles('bun.lock', 'bun.lockb')`
    - `migration-drift` 與 `e2e-smoke` 均含 `services.postgres` 區塊，`image: postgres:16`
    - `migration-drift` job 順序：`bun install` → `bun run migrate` → `bun run migration:drift`
    - `e2e-smoke` job 順序：`bun install` → `bunx playwright install --with-deps chromium` → `bun run migrate` → `bun run test:e2e:smoke`
    - `di-audit` job 執行 `bun run di:audit`
    - `commitlint` job 含 `if: github.event_name == 'pull_request'` 與 `fetch-depth: 0`
    - `commitlint` job 使用 `${{ github.event.pull_request.base.sha }}` 與 `${{ github.event.pull_request.head.sha }}`
    - `routes-check` job 呼叫 routes-existence 測試檔（grep "routes-existence" 通過）
    - `on.push.branches` 與 `on.pull_request.branches` 仍為 `[main, develop]`
    - workflow 不包含 `branch_protection` / `required_checks` 區塊（D-21）
  </acceptance_criteria>
  <done>CI workflow YAML 完整且有效；推送後 GitHub Actions 應觸發 8 個並行 jobs。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 撰寫 docs/ci/branch-protection-setup.md runbook</name>
  <files>docs/ci/branch-protection-setup.md</files>
  <read_first>
    - .github/workflows/ci.yml（Task 1 產出 — 確認 8 job 名稱）
    - .planning/phases/20-ci-verification-guardrails/20-CONTEXT.md §D-21, D-22
    - .planning/phases/20-ci-verification-guardrails/20-RESEARCH.md §gh api Branch Protection Runbook Snippet
  </read_first>
  <action>
    建立 `docs/ci/branch-protection-setup.md`，內容為繁體中文 runbook（per 專案語言政策）。

    **文件結構**：

    ```markdown
    # Branch Protection 設定 Runbook

    > Phase 20 CI Verification Guardrails 交付文件。
    > Repo admin 依本 runbook 手動設定 `main` 與 `develop` 的 required status checks。
    > Phase 20 本身**不自動設定 branch protection**（per CONTEXT.md D-21），因為：
    > - 需 admin token 權限
    > - 避免 phase 工作被 repo settings 授權問題卡住

    ## 前置條件

    - 具有 repo admin 權限的 GitHub 帳號
    - 本機已安裝 `gh` CLI（`brew install gh`）
    - 已執行 `gh auth login` 並選擇 repo admin 權限 scope

    ## Required Status Checks 清單

    以下 8 個 job 名稱必須全數被設為 required checks（名稱需與 `.github/workflows/ci.yml` 中的 job key 精確一致）：

    | # | Job 名稱 | 守門內容 | Requirement |
    |---|---------|---------|-------------|
    | 1 | `typecheck` | TypeScript 型別檢查 | CI-02 |
    | 2 | `lint-format` | Biome lint + format | CI-04 |
    | 3 | `unit-coverage` | Unit test + 80% coverage threshold | CI-03 |
    | 4 | `migration-drift` | DB migration vs Drizzle schema drift | CI-06 |
    | 5 | `routes-check` | Routes invariance | CI-07 |
    | 6 | `di-audit` | DI binding 完整性 | CI-08 |
    | 7 | `e2e-smoke` | Playwright smoke（admin/member/CLI）| CI-01 |
    | 8 | `commitlint` | Conventional commits 驗證 | CI-05 |

    ## 步驟 A：使用 `gh api` 指令設定（推薦）

    ### A-1. 設定 `main` branch

    ```bash
    gh api -X PUT \
      repos/:owner/:repo/branches/main/protection \
      -F required_status_checks.strict=true \
      -F required_status_checks.contexts[]=typecheck \
      -F required_status_checks.contexts[]=lint-format \
      -F required_status_checks.contexts[]=unit-coverage \
      -F required_status_checks.contexts[]=migration-drift \
      -F required_status_checks.contexts[]=routes-check \
      -F required_status_checks.contexts[]=di-audit \
      -F required_status_checks.contexts[]=e2e-smoke \
      -F required_status_checks.contexts[]=commitlint \
      -F enforce_admins=true \
      -F required_pull_request_reviews.required_approving_review_count=1 \
      -F required_pull_request_reviews.dismiss_stale_reviews=true \
      -F restrictions=
    ```

    > 將 `:owner/:repo` 改為實際 repo，例如 `CMG/Draupnir`。

    ### A-2. 設定 `develop` branch

    重複 A-1，將 URL 的 `main` 改為 `develop`。

    ### A-3. 驗證設定

    ```bash
    gh api repos/:owner/:repo/branches/main/protection | jq '.required_status_checks.contexts'
    ```
    預期輸出為 8 個 context 名稱的 JSON array。

    ## 步驟 B：使用 GitHub Web UI 設定（備用）

    1. 進入 `Settings → Branches → Branch protection rules`
    2. 點擊 `Add rule`（或編輯既有 rule）
    3. `Branch name pattern`：`main`（再為 `develop` 重複一次）
    4. 勾選 `Require status checks to pass before merging`
    5. 勾選 `Require branches to be up to date before merging`
    6. 在 `Status checks that are required` 搜尋框依序加入上表 8 個 job 名稱
    7. 勾選 `Require a pull request before merging` → `Require approvals`（至少 1）
    8. 勾選 `Do not allow bypassing the above settings`（enforce admins）
    9. 點擊 `Create` / `Save changes`

    ## 步驟 C：驗收（Dummy PR 流程）

    Phase 20 交付完成後，建議以下列步驟驗證整體守門正確：

    1. 從 `develop` 建立 feature branch
    2. 故意違反 1 項守門點（例如：刪掉一行測試讓 coverage 掉到 80% 以下）
    3. 推送並開 PR 指向 `develop`
    4. 觀察：
       - 對應 job（`unit-coverage`）顯示 red
       - PR 頁面顯示 `Required status check is failing`
       - `Merge` 按鈕為 disabled
    5. 修正違反項，re-push，8 jobs 全綠後 `Merge` 按鈕變 enabled

    ## 移除 Branch Protection（緊急回滾用）

    ```bash
    gh api -X DELETE repos/:owner/:repo/branches/main/protection
    ```

    > 僅在 CI 守門本身故障（如 GitHub Actions outage）且需緊急合併時使用。事後應立即重新啟用。

    ## 參考

    - GitHub Docs - Branch Protection API：https://docs.github.com/rest/branches/branch-protection
    - Phase 20 CONTEXT.md §D-21, D-22
    - `.github/workflows/ci.yml`（本 runbook 對應的 CI 定義）

    ---

    *Last updated: 2026-04-13（Phase 20 Plan 20-02 交付）*
    ```

    **禁止**：
    - 不要在 runbook 內寫 `gh` 指令自動化腳本（CONTEXT.md D-21 明示僅產文件）
    - 不要列出不在 `.github/workflows/ci.yml` 的 job 名稱
    - 不要用英文撰寫（違反專案語言政策）
  </action>
  <verify>
    <automated>test -f docs/ci/branch-protection-setup.md &amp;&amp; grep -q "gh api" docs/ci/branch-protection-setup.md &amp;&amp; grep -q "required_status_checks" docs/ci/branch-protection-setup.md &amp;&amp; for job in typecheck lint-format unit-coverage migration-drift routes-check di-audit e2e-smoke commitlint; do grep -q "$job" docs/ci/branch-protection-setup.md || { echo "missing $job"; exit 1; }; done &amp;&amp; grep -q "develop" docs/ci/branch-protection-setup.md &amp;&amp; grep -q "Dummy PR\|dummy PR\|故意違反" docs/ci/branch-protection-setup.md
    <automated>test -f docs/ci/branch-protection-setup.md &amp;&amp; grep -q "gh api" docs/ci/branch-protection-setup.md &amp;&amp; grep -q "required_status_checks" docs/ci/branch-protection-setup.md &amp;&amp; for job in typecheck lint-format unit-coverage migration-drift routes-check di-audit e2e-smoke commitlint; do grep -q "$job" docs/ci/branch-protection-setup.md; done &amp;&amp; grep -q "develop" docs/ci/branch-protection-setup.md &amp;&amp; grep -qE "Dummy PR|dummy PR|故意違反" docs/ci/branch-protection-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ci/branch-protection-setup.md` 存在且 ≥ 40 行
    - 文件包含 `gh api` 指令範例（至少一個 `-X PUT` 對 `/branches/.../protection` endpoint）
    - 文件列出全部 8 個 job 名稱：typecheck / lint-format / unit-coverage / migration-drift / routes-check / di-audit / e2e-smoke / commitlint（grep loop 驗證）
    - 文件同時涵蓋 `main` 與 `develop` 兩個 branch
    - 文件包含驗收流程（Dummy PR 故意違反守門點的步驟）
    - 文件使用繁體中文撰寫（非英文 runbook）
    - 文件不含 `#!/usr/bin/env` 或執行腳本（僅文件，per D-21）
    - 文件列出的 job 名稱與 `.github/workflows/ci.yml` 中的 job key 完全一致（手動比對 + grep 雙重確認）
  </acceptance_criteria>
  <done>Runbook 交付，repo admin 可依文件獨立完成 branch protection 設定，無需 planner/implementer 協助。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): 驗證 CI workflow 在實際 GitHub Actions 運行</name>
  <files>.github/workflows/ci.yml, docs/ci/branch-protection-setup.md</files>
  <read_first>
    - .github/workflows/ci.yml（Task 1 產出）
    - docs/ci/branch-protection-setup.md（Task 2 產出）
  </read_first>
  <action>
    將 Plan 20-01 + Plan 20-02 Task 1/2 的全部變更 commit 並推送至 feature branch，開 PR 觸發 GitHub Actions，在 PR 頁面觀察 8 個 jobs 是否被觸發並取得可辨識的 pass/fail 結果。依 `<how-to-verify>` 步驟 1-9 執行。此 task 不修改任何程式碼或 workflow。
  </action>
  <what-built>
    Plan 20-01 產出的 guardrail 工具 + Plan 20-02 Task 1 擴充的 `.github/workflows/ci.yml`（8 並行 jobs）+ Task 2 runbook 文件。
  </what-built>
  <how-to-verify>
    1. Commit 全部變更並推送至 feature branch（如 `git checkout -b phase20-ci-guardrails && git push -u origin phase20-ci-guardrails`）
    2. 開 PR 指向 `develop`（或 `main`）
    3. 等待 GitHub Actions 觸發
    4. 確認下列在 PR 頁面的 Checks 區塊可見：
       - `typecheck` — 執行 `bun run typecheck`
       - `lint-format` — 執行 `bun run lint` + `bun run format:check`
       - `unit-coverage` — 執行 `bun test --coverage`
       - `migration-drift` — Postgres service + `bun run migrate` + `bun run migration:drift`
       - `routes-check` — 執行 routes-existence 測試
       - `di-audit` — 執行 `bun run di:audit`
       - `e2e-smoke` — Postgres service + Playwright smoke
       - `commitlint` — PR commits 驗證
    5. 確認 8 個 job 並行執行（不是序列）
    6. 若任一 job fail：檢查 log 判斷是「工具設計錯誤」還是「真實的既存問題」；若為後者，記錄至 Phase 20 SUMMARY 的 known-failures 區段，本 phase 不修復（out of scope）
    7. 確認 cache 在第二次執行時生效（第二次 CI run 的 `Install dependencies` step 時間應顯著 < 第一次）
    8. 若為 PR，嘗試故意推一個違規 commit（如 `feat no-colon subject` 或破壞 coverage），確認對應 job 紅燈
    9. 將 PR 連結與 Actions 執行結果截圖記錄於 Phase 20 SUMMARY

    **預期狀態**：8 個 job 應可被觸發並執行（pass 或 fail 皆可接受，但必須「可執行」且「可辨識失敗類別」）
  </how-to-verify>
  <resume-signal>輸入 "approved" 並附上 PR 連結，或描述遇到的 workflow 錯誤（YAML parse / job 未觸發 / 等）</resume-signal>
  <verify>
    <automated>MISSING — human-verify checkpoint，需在 PR 頁面觀察 Actions 執行結果（無法 CLI 自動化驗證）</automated>
  </verify>
  <acceptance_criteria>
    - PR 已開啟並指向 main 或 develop
    - GitHub Actions 已觸發，PR Checks 區塊顯示全部 8 個 job 名稱：typecheck / lint-format / unit-coverage / migration-drift / routes-check / di-audit / e2e-smoke / commitlint
    - 8 個 job 並行執行（Checks 時間戳顯示同時開始，或相近 ±30 秒內）
    - 任一 job 的 log 可讀、失敗分類清楚（若失敗）
    - 使用者回覆 "approved" 並附上 PR URL
  </acceptance_criteria>
  <done>PR 已驗證 8 jobs 觸發且執行結束；結果（含 known-failures 若有）記錄於 Phase 20 SUMMARY。</done>
</task>

</tasks>

<verification>
Plan-level checks:
- [ ] `.github/workflows/ci.yml` 有效 YAML，含 8 個 jobs
- [ ] `docs/ci/branch-protection-setup.md` 存在且列出 8 個 required checks
- [ ] 所有 8 個 CI requirement IDs 在 plan frontmatter 的 `requirements` 列出
- [ ] 8 個 job 名稱在 ci.yml 與 runbook 之間精確一致
- [ ] Postgres service container 定義在 migration-drift 與 e2e-smoke 兩處
- [ ] actions/cache 在每個 job 均使用，key 含 `hashFiles('bun.lock*')`
- [ ] commitlint job 僅 PR 觸發（`if: github.event_name == 'pull_request'`）
- [ ] PR 推送後 8 job 可觸發（Checkpoint Task 3）
</verification>

<success_criteria>
1. `.github/workflows/ci.yml` 通過 YAML parse 與 Task 1 automated verify
2. `docs/ci/branch-protection-setup.md` 通過 Task 2 automated verify
3. 實際開 PR 後 8 個 jobs 皆被觸發（Task 3 checkpoint）
4. 全部 8 個 requirements（CI-01~CI-08）在本 phase 完成落地
5. 後續 repo admin 可依 runbook 獨立完成 branch protection 設定
</success_criteria>

<output>
After completion, create `.planning/phases/20-ci-verification-guardrails/20-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`。SUMMARY 須記錄：
- 8 個 job 的執行時間（從 PR Actions 頁面記錄）
- 快取生效前後的 install 時間對比
- 任何 known-failures（若有 job fail 是既存問題而非工具問題）
- PR 連結（phase gate 驗收）
- Branch protection 設定是否由 repo admin 已執行（若已執行）或保留為 todo
</output>
