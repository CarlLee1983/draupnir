---
phase: 20-ci-verification-guardrails
plan: 05
type: execute
wave: 2
depends_on: ["20-04"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [CI-08]

must_haves:
  truths:
    - "Commit `a583918` body lines all ≤ 100 characters"
    - "`bunx commitlint --from <base> --to <head>` exits 0 for PR #4 commit range"
    - "PR #4 `commitlint` job turns green"
    - "Commit content (intent, scope, files) preserved — only wrapping changes"
  artifacts:
    - path: ".git (history)"
      provides: "reworded a583918 with wrapped body"
  key_links:
    - from: "commitlint.config.cjs"
      to: "@commitlint/config-conventional body-max-line-length=100"
      via: "extends"
      pattern: "config-conventional"
---

<objective>
Reword commit `a583918` ("feat(phase20): enforce CI guardrails with repo-owned runners") so its body lines wrap to ≤ 100 characters, satisfying the inherited `body-max-line-length` rule from `@commitlint/config-conventional`.

Diagnosis (from `20-UAT.md` Gap > commitlint):
- Only `a583918` violates; the other two commits in the PR range pass.
- `commitlint.config.cjs` overrides only `type-enum`, `subject-case`, and `header-max-length` — `body-max-line-length: 100` is inherited and active.
- Not auto-fixable: requires interactive rebase + force-push.

This plan is **Wave 2** (depends on 20-04) because force-pushing after 20-04's style commit keeps history linear and ordering predictable.

Purpose: Unblock PR #4 `commitlint` CI job — the final known-failure.
Output: Green `commitlint` check on PR #4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-ci-verification-guardrails/20-UAT.md
@commitlint.config.cjs
@.github/workflows/ci.yml

<interfaces>
<!-- Commitlint rule surface -->

From @commitlint/config-conventional (inherited defaults used by this repo):
- body-max-line-length: 100 characters per line in commit body (enforced)
- footer-max-line-length: 100 characters per line in footer (enforced)
- body-leading-blank: 1 blank line between header and body (enforced)

Repo overrides (commitlint.config.cjs):
- type-enum: feat/fix/docs/style/refactor/perf/test/chore/ci/build/revert
- subject-case: disabled (0)
- header-max-length: 100

Only body-max-line-length is the violation for a583918.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare reworded commit message for a583918</name>
  <files>/tmp/a583918-original.txt, /tmp/a583918-reworded.txt</files>
  <action>
    Do NOT rewrite history in this task. Only PREPARE the new message.

    Steps:
    1. Capture the current message:
       ```bash
       git log -1 --format='%B' a583918 > /tmp/a583918-original.txt
       ```
    2. Inspect for long lines:
       ```bash
       awk 'length > 100 {print NR": "length" chars: "$0}' /tmp/a583918-original.txt
       ```
    3. Create `/tmp/a583918-reworded.txt` containing the same commit message with:
       - Subject line (first line) UNCHANGED.
       - One blank line after subject (preserve `body-leading-blank`).
       - Every body line hard-wrapped at ≤ 100 characters. Preserve bullet structure (`-`, `*`) and paragraph breaks. Do NOT rewrite content, summaries, or semantic meaning — only insert line breaks.
       - Preserve any footer tokens (e.g. `Co-authored-by:`, `Refs:`) unchanged.
    4. Verify no line exceeds 100:
       ```bash
       awk 'length > 100 {exit 1}' /tmp/a583918-reworded.txt && echo OK
       ```
    5. Run commitlint against the candidate message:
       ```bash
       bunx commitlint < /tmp/a583918-reworded.txt
       ```
       Must exit 0.

    Do NOT start `git rebase` yet — that is Task 2 (human-gated).
  </action>
  <verify>
    <automated>awk 'length > 100 {exit 1}' /tmp/a583918-reworded.txt && bunx commitlint < /tmp/a583918-reworded.txt</automated>
  </verify>
  <done>`/tmp/a583918-reworded.txt` exists; all lines ≤ 100 chars; commitlint exits 0 on it; content semantically identical to original.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Interactive rebase reword + force-push (user-action required)</name>
  <files>.git (history on branch phase20-ci-guardrails-final)</files>
  <action>
    PAUSE for user to perform interactive rebase + force-push. This is destructive history rewrite on a shared PR branch and CANNOT be automated per phase constraints.

    Present these steps to the user:

    1. Confirm clean state:
       ```bash
       git status
       git log --oneline -5
       ```
    2. Identify parent of a583918:
       ```bash
       git log --format='%H %s' a583918~1 -1
       ```
    3. Run interactive rebase:
       ```bash
       git rebase -i a583918~1
       ```
       Change `pick a583918 ...` to `reword a583918 ...`, save, exit.
    4. When the editor opens for a583918's message, REPLACE entire contents with `/tmp/a583918-reworded.txt` (prepared in Task 1). In vim:
       ```
       :%d
       :r /tmp/a583918-reworded.txt
       ```
       (then delete the leading blank line that `:r` adds). Save and exit.
    5. Verify rebase result:
       ```bash
       git log --format='%B' HEAD~1 -1 | awk 'length > 100 {print NR": "length" chars"}' && echo OK
       bunx commitlint --from origin/main --to HEAD --verbose
       ```
    6. Force-push with lease (safer than plain --force):
       ```bash
       git push --force-with-lease origin phase20-ci-guardrails-final
       ```
    7. After ~30s:
       ```bash
       gh pr checks 4
       ```
       `commitlint` job should be green.

    IMPORTANT: `--force-with-lease`, not `--force`. If concurrent pushes occurred, it refuses — investigate before retrying. This is a feature-branch rewrite only; main/master untouched.

    Resume signal: user types "approved" after `gh pr checks 4` shows `commitlint` = success. If rebase conflicts or push rejected, user pastes error for troubleshooting.
  </action>
  <verify>
    <automated>MISSING — this checkpoint requires user-only action (interactive rebase + force-push). Post-action automated confirmation: `gh pr checks 4 --watch` shows `commitlint` = success.</automated>
  </verify>
  <done>Reworded commit on branch; force-push succeeded; PR #4 `commitlint` job green; user approved.</done>
</task>

</tasks>

<verification>
Final check after force-push:
```bash
gh pr checks 4
```
All eight CI jobs should be green (`typecheck`, `lint-format`, `unit-coverage`, `migration-drift`, `routes-check`, `di-audit`, `e2e-smoke`, `commitlint`) once 20-03, 20-04, 20-05 all land.
</verification>

<success_criteria>
- [ ] Reworded a583918 passes `bunx commitlint`
- [ ] Force-push with lease succeeded on `phase20-ci-guardrails-final`
- [ ] PR #4 `commitlint` job green
- [ ] Commit content semantically preserved (same facts, different wrapping)
- [ ] User explicitly approved the force-push step
</success_criteria>

<output>
Create `.planning/phases/20-ci-verification-guardrails/20-05-SUMMARY.md` with:
- Original a583918 SHA + new SHA after reword
- Diff of message (before/after wrapping)
- `gh pr checks 4` output showing all 8 jobs green
</output>
