---
phase: 20-ci-verification-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Shared/Infrastructure/Database/__tests__/AggregateParity.test.ts
  - src/Modules/Dashboard/__tests__/DrizzleUsageRepository.test.ts
  - src/Shared/Infrastructure/Database/Adapters/Drizzle/__tests__/DrizzleQueryBuilder.aggregate.test.ts
  - src/Modules/Profile/__tests__/UserProfile.test.ts
  - src/Modules/Profile/__tests__/GetProfileService.test.ts
  - src/Modules/Profile/__tests__/UpdateProfileService.test.ts
  - src/Shared/Infrastructure/Database/Adapters/Memory/__tests__/MemoryQueryBuilder.aggregate.test.ts
autonomous: true
gap_closure: true
requirements: [CI-03]

must_haves:
  truths:
    - "`bun test --coverage` exits 0 locally with ORM=memory"
    - "PR #4 `unit-coverage` job turns green on next push"
    - "No vitest-only APIs (`vi.mocked`, `vi.fn`, etc.) remain in Bun-executed test files"
    - "Profile tests pass regardless of aggregate-generated UUIDs"
    - "MemoryQueryBuilder.insertOrIgnore idempotency test passes without undefined throws"
  artifacts:
    - path: "src/Shared/Infrastructure/Database/__tests__/AggregateParity.test.ts"
      provides: "Parity test using Bun-native mocks"
      contains: "mock.module|import.meta|spyOn"
    - path: "src/Modules/Profile/__tests__/UpdateProfileService.test.ts"
      provides: "Profile tests tolerant of auto-generated IDs"
      contains: "profile.id"
    - path: "src/Shared/Infrastructure/Database/Adapters/Memory/__tests__/MemoryQueryBuilder.aggregate.test.ts"
      provides: "idempotency assertion that passes"
      contains: "insertOrIgnore"
  key_links:
    - from: "tests (Cluster A)"
      to: "bun:test API surface"
      via: "mock / spyOn / mock.module"
      pattern: "from ['\"]bun:test['\"]"
    - from: "tests (Cluster B)"
      to: "UserProfile aggregate identity"
      via: "read profile.id rather than hard-code 'user-123'"
      pattern: "profile\\.id"
---

<objective>
Fix the 9 failing unit tests blocking PR #4 `unit-coverage` CI job. Coverage gate (80% / bunfig.toml `coverageThreshold = 0.8`) is already satisfied (actual 80.81%/87.13%); failures are test-level only.

Purpose: Unblock PR #4 CI so Phase 20 guardrails can be enforced as required checks on `main`/`develop`.
Output: 9 tests pass locally + in CI, `unit-coverage` job green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ci-verification-guardrails/20-UAT.md
@.planning/phases/20-ci-verification-guardrails/20-02-SUMMARY.md
@bunfig.toml
@.github/workflows/ci.yml

# Cluster A — Vitest API in Bun runner
@src/Shared/Infrastructure/Database/__tests__/AggregateParity.test.ts
@src/Modules/Dashboard/__tests__/DrizzleUsageRepository.test.ts
@src/Shared/Infrastructure/Database/Adapters/Drizzle/__tests__/DrizzleQueryBuilder.aggregate.test.ts

# Cluster B — Profile tests assume fixed user-id
@src/Modules/Profile/__tests__/UserProfile.test.ts
@src/Modules/Profile/__tests__/GetProfileService.test.ts
@src/Modules/Profile/__tests__/UpdateProfileService.test.ts

# Cluster C — MemoryQueryBuilder idempotency
@src/Shared/Infrastructure/Database/Adapters/Memory/__tests__/MemoryQueryBuilder.aggregate.test.ts
@src/Shared/Infrastructure/Database/Adapters/Memory/MemoryDatabaseAccess.ts

<interfaces>
<!-- Bun test API replacements for vitest idioms -->

From bun:test (native):
```typescript
import { describe, expect, it, mock, spyOn, beforeAll, beforeEach } from 'bun:test'

// Replacement for vi.mocked(fn):
//   - If the function is module-level and you need to stub it, use mock.module():
//     mock.module('path/to/module', () => ({ exportedFn: mock(() => stubValue) }))
//   - If already replaced via mock.module, just call the mocked export directly.
//   - If wanting to assert call args: use `spyOn(obj, 'method')` and `spy.mock.calls`.
// Do NOT import anything from 'vitest' in tests executed by `bun test`.
```

UserProfile aggregate — identity behaviour (Cluster B root cause):
```typescript
// src/Modules/Profile/Domain/UserProfile.ts
// createDefault(userId, email) → the `id` field is auto-generated (UUID),
// NOT the `userId` passed in. Tests must read profile.id from returned object
// rather than asserting profile.id === 'user-123'.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cluster A — replace vitest API with bun:test in 3 files</name>
  <files>
    src/Shared/Infrastructure/Database/__tests__/AggregateParity.test.ts,
    src/Modules/Dashboard/__tests__/DrizzleUsageRepository.test.ts,
    src/Shared/Infrastructure/Database/Adapters/Drizzle/__tests__/DrizzleQueryBuilder.aggregate.test.ts
  </files>
  <action>
    For each of the 3 files:
    1. Change `import { ... vi ... } from 'vitest'` → `import { ... mock, spyOn ... } from 'bun:test'` (keep describe/it/expect/beforeAll/beforeEach).
    2. Replace every `vi.mocked(fnRef).mockReturnValue(x)` with a `mock.module()` call at the top of the test file that stubs the exported function, OR refactor to `spyOn(moduleNs, 'fnName').mockReturnValue(x)` if the function is imported as a namespace.
    3. Run `grep -n "vi\\." <file>` after edits — zero matches required.
    4. Preserve all existing assertions and test names; do NOT change what is being tested.
    5. If a test depends on vitest-specific `vi.fn()`, replace with `mock(() => returnValue)` from bun:test.

    Do not install or re-enable vitest; the project standardises on `bun test` (see bunfig.toml, package.json `test` script).
  </action>
  <verify>
    <automated>bun test src/Shared/Infrastructure/Database/__tests__/AggregateParity.test.ts src/Modules/Dashboard/__tests__/DrizzleUsageRepository.test.ts src/Shared/Infrastructure/Database/Adapters/Drizzle/__tests__/DrizzleQueryBuilder.aggregate.test.ts</automated>
  </verify>
  <done>All 3 files pass under `bun test`; `grep -rn "from 'vitest'" src/` on these files returns empty; no `vi.` references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Cluster B — make Profile tests tolerant of auto-generated UUID</name>
  <files>
    src/Modules/Profile/__tests__/UserProfile.test.ts,
    src/Modules/Profile/__tests__/GetProfileService.test.ts,
    src/Modules/Profile/__tests__/UpdateProfileService.test.ts
  </files>
  <action>
    Root cause: `UserProfile.createDefault(userId, email)` now auto-generates the aggregate `id` (UUID). The 3 test files hard-code expectations like `expect(profile.id).toBe('user-123')` or pass `'user-123'` to service methods that internally look up by the generated id.

    For each file:
    1. Where the test asserts `profile.id` equality to `'user-123'` (or similar literal), replace with one of:
       - Capture the created profile first: `const profile = UserProfile.createDefault(...); ... expect(result.data?.id).toBe(profile.id)`.
       - Or assert shape: `expect(result.data?.id).toEqual(expect.any(String))` + assert other invariant fields (email, displayName).
    2. Where the test passes `'user-123'` into a service method (e.g. `getService.execute('user-123')`), update to pass `profile.id` (the generated one) after seeding the repository with that same profile.
    3. For UserProfile.test.ts line 32 (object literal `{ id: 'user-123', ... }`): keep literal but assert equality against that exact input, not against a separately-constructed aggregate.
    4. Do NOT change the UserProfile aggregate or services — behavior change is intentional; only tests adapt.
    5. Preserve all other assertions (email, displayName, bio, timezone, phone validation error).

    Reference: Phase 18/19 aggregate-identity work standardised auto-generated IDs across modules.
  </action>
  <verify>
    <automated>bun test src/Modules/Profile/__tests__/</automated>
  </verify>
  <done>All Profile tests pass; no literal `'user-123'` expectation survives against a constructed aggregate's `id`.</done>
</task>

<task type="auto">
  <name>Task 3: Cluster C — fix MemoryQueryBuilder.insertOrIgnore idempotency test</name>
  <files>
    src/Shared/Infrastructure/Database/Adapters/Memory/__tests__/MemoryQueryBuilder.aggregate.test.ts,
    src/Shared/Infrastructure/Database/Adapters/Memory/MemoryDatabaseAccess.ts
  </files>
  <action>
    Root cause: The idempotency test around line 255 (`insertOrIgnore(data, { conflictTarget: 'bifrost_log_id' })` expected `resolves.not.toThrow()`) is throwing `undefined`. Investigate whether:
    (a) MemoryDatabaseAccess.insertOrIgnore throws when conflict detected (should silently skip per Drizzle parity), or
    (b) The conflictTarget option is not honored and a generic constraint violation bubbles up.

    Fix plan:
    1. Read `src/Shared/Infrastructure/Database/Adapters/Memory/MemoryDatabaseAccess.ts` — locate `insertOrIgnore` implementation.
    2. If the implementation throws on conflict: change it to return silently (or return a count/rowsAffected = 0) when a row matching `conflictTarget` already exists. Match Drizzle `insertOrIgnore` semantics (ON CONFLICT DO NOTHING).
    3. If implementation is correct but test setup is wrong (e.g. seed row missing): fix the test seeding so the first insert succeeds and the second is the conflict.
    4. Preserve all other tests in the file (lines 228-245 for the "inserts a new row" case must still pass).
    5. Keep immutability — return new state objects, do not mutate existing records.

    Commit as a bugfix to MemoryDatabaseAccess if production code changes, separate commit if test-only change.
  </action>
  <verify>
    <automated>bun test src/Shared/Infrastructure/Database/Adapters/Memory/__tests__/MemoryQueryBuilder.aggregate.test.ts</automated>
  </verify>
  <done>Both `insertOrIgnore` describe blocks pass; no undefined throws; semantics match Drizzle adapter parity.</done>
</task>

</tasks>

<verification>
Run full suite locally to confirm no regression:

```bash
ORM=memory BIFROST_API_URL=http://localhost:8080 BIFROST_MASTER_KEY=ci-test-key JWT_SECRET=ci-test-secret bun test --coverage
```

Must exit 0 and report coverage ≥ 80%.

Then push to `phase20-ci-guardrails-final` and confirm `gh pr checks 4` shows `unit-coverage` = success.
</verification>

<success_criteria>
- [ ] `bun test --coverage` exits 0 locally with the CI env vars
- [ ] 9 previously-failing tests now pass
- [ ] Coverage threshold ≥ 80% maintained
- [ ] No vitest imports remain in edited files
- [ ] PR #4 `unit-coverage` job turns green
</success_criteria>

<output>
Create `.planning/phases/20-ci-verification-guardrails/20-03-SUMMARY.md` documenting each cluster's fix and file diffs.
</output>
