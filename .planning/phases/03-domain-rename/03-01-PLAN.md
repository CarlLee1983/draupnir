---
phase: 03-domain-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts
  - src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts
  - src/Modules/ApiKey/Infrastructure/Repositories/ApiKeyRepository.ts
  - src/Modules/AppApiKey/Infrastructure/Repositories/AppApiKeyRepository.ts
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-03

must_haves:
  truths:
    - "ApiKey aggregate exposes gatewayKeyId getter and no bifrostVirtualKeyId getter"
    - "AppApiKey aggregate exposes gatewayKeyId and previousGatewayKeyId getters and no bifrost-named getters"
    - "Both aggregates write bifrost_virtual_key_id to DB rows (column name frozen)"
    - "Both repositories pass row mapping through aggregate fromDatabase/toDatabaseRow without change to query logic"
    - "bun test and bun run typecheck pass after this plan"
  artifacts:
    - path: "src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts"
      provides: "ApiKey aggregate with gatewayKeyId field"
      contains: "gatewayKeyId"
    - path: "src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts"
      provides: "AppApiKey aggregate with gatewayKeyId and previousGatewayKeyId"
      contains: "previousGatewayKeyId"
  key_links:
    - from: "ApiKey.toDatabaseRow()"
      to: "bifrost_virtual_key_id column"
      via: "this.props.gatewayKeyId"
      pattern: "bifrost_virtual_key_id.*gatewayKeyId"
    - from: "AppApiKey.fromDatabase()"
      to: "props.gatewayKeyId"
      via: "row.bifrost_virtual_key_id"
      pattern: "gatewayKeyId.*bifrost_virtual_key_id"
---

<objective>
Rename gateway-specific TypeScript field names in the two domain aggregates and expose updated getters. The DB column names are frozen — only the TS-side of the ORM mapping changes.

Purpose: Establishes the renamed field contracts (gatewayKeyId) that all downstream call sites depend on. Must be complete before Wave 2 plans can touch sync services and DTOs.
Output: ApiKey.ts and AppApiKey.ts with renamed props/params/getters/fromDatabase/toDatabaseRow; both repositories unchanged except they now call the updated aggregate methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-domain-rename/03-CONTEXT.md
</context>

<interfaces>
<!-- Current ApiKey field names to rename (from src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts) -->
<!-- Props interface field:       bifrostVirtualKeyId → gatewayKeyId -->
<!-- CreateApiKeyParams field:    bifrostVirtualKeyId → gatewayKeyId -->
<!-- static create() param:       bifrostVirtualKeyId → gatewayKeyId -->
<!-- static fromDatabase() prop:  bifrostVirtualKeyId: row.bifrost_virtual_key_id → gatewayKeyId: row.bifrost_virtual_key_id -->
<!-- getter:                      get bifrostVirtualKeyId() → get gatewayKeyId() -->
<!-- toDatabaseRow():             bifrost_virtual_key_id: this.props.bifrostVirtualKeyId → bifrost_virtual_key_id: this.props.gatewayKeyId -->
<!-- toDTO():                     bifrostVirtualKeyId: this.props.bifrostVirtualKeyId → gatewayKeyId: this.props.gatewayKeyId -->

<!-- Current AppApiKey field names to rename (from src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts) -->
<!-- Props interface fields:
     bifrostVirtualKeyId → gatewayKeyId
     previousBifrostVirtualKeyId → previousGatewayKeyId -->
<!-- CreateAppApiKeyParams:       bifrostVirtualKeyId → gatewayKeyId -->
<!-- static create() prop:        bifrostVirtualKeyId → gatewayKeyId, previousBifrostVirtualKeyId → previousGatewayKeyId -->
<!-- static fromDatabase():
     bifrostVirtualKeyId: row.bifrost_virtual_key_id → gatewayKeyId: row.bifrost_virtual_key_id
     previousBifrostVirtualKeyId: row.previous_bifrost_virtual_key_id → previousGatewayKeyId: row.previous_bifrost_virtual_key_id -->
<!-- rotate() param:              newBifrostVirtualKeyId → newGatewayKeyId -->
<!-- rotate() body:
     bifrostVirtualKeyId: newBifrostVirtualKeyId → gatewayKeyId: newGatewayKeyId
     previousBifrostVirtualKeyId: this.props.bifrostVirtualKeyId → previousGatewayKeyId: this.props.gatewayKeyId -->
<!-- completeRotation():          previousBifrostVirtualKeyId: null → previousGatewayKeyId: null -->
<!-- getters:
     get bifrostVirtualKeyId() → get gatewayKeyId()
     get previousBifrostVirtualKeyId() → get previousGatewayKeyId() -->
<!-- toDatabaseRow():
     bifrost_virtual_key_id: this.props.bifrostVirtualKeyId → bifrost_virtual_key_id: this.props.gatewayKeyId
     previous_bifrost_virtual_key_id: this.props.previousBifrostVirtualKeyId → previous_bifrost_virtual_key_id: this.props.previousGatewayKeyId -->
<!-- toDTO():
     bifrostVirtualKeyId: this.props.bifrostVirtualKeyId → gatewayKeyId: this.props.gatewayKeyId -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename ApiKey aggregate — props, params, fromDatabase, getter, toDatabaseRow, toDTO</name>
  <read_first>
    src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts
  </read_first>
  <files>src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts</files>
  <action>
Apply exactly the following renames inside ApiKey.ts. No other logic changes.

1. In `ApiKeyProps` interface:
   - `readonly bifrostVirtualKeyId: string` → `readonly gatewayKeyId: string`

2. In `CreateApiKeyParams` interface:
   - `bifrostVirtualKeyId: string` → `gatewayKeyId: string`

3. In `static async create(params: CreateApiKeyParams)`:
   - `bifrostVirtualKeyId: params.bifrostVirtualKeyId,` → `gatewayKeyId: params.gatewayKeyId,`

4. In `static fromDatabase(row: Record<string, unknown>)`:
   - `bifrostVirtualKeyId: row.bifrost_virtual_key_id as string,` → `gatewayKeyId: row.bifrost_virtual_key_id as string,`
   (DB column name `bifrost_virtual_key_id` stays unchanged — per D-P03-02)

5. Getter rename:
   - `get bifrostVirtualKeyId(): string { return this.props.bifrostVirtualKeyId }` →
     `get gatewayKeyId(): string { return this.props.gatewayKeyId }`

6. In `toDatabaseRow()`:
   - `bifrost_virtual_key_id: this.props.bifrostVirtualKeyId,` → `bifrost_virtual_key_id: this.props.gatewayKeyId,`
   (key in the returned object stays `bifrost_virtual_key_id` — column is frozen)

7. In `toDTO()`:
   - `bifrostVirtualKeyId: this.props.bifrostVirtualKeyId,` → `gatewayKeyId: this.props.gatewayKeyId,`
  </action>
  <verify>
    <automated>grep -n "bifrostVirtualKeyId" /Users/carl/Dev/CMG/Draupnir/src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts && echo "FOUND — fix required" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bifrostVirtualKeyId" src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts` returns zero matches
    - `grep "gatewayKeyId" src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts` returns at least 7 matches (props, param, create, fromDatabase, getter, toDatabaseRow, toDTO)
    - `grep "bifrost_virtual_key_id" src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts` returns exactly 2 matches (fromDatabase and toDatabaseRow — column name preserved)
  </acceptance_criteria>
  <done>ApiKey.ts exposes gatewayKeyId everywhere in TypeScript and maps it to/from DB column bifrost_virtual_key_id.</done>
</task>

<task type="auto">
  <name>Task 2: Rename AppApiKey aggregate — props, params, fromDatabase, rotate, getters, toDatabaseRow, toDTO</name>
  <read_first>
    src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts
  </read_first>
  <files>src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts</files>
  <action>
Apply exactly the following renames inside AppApiKey.ts. No other logic changes.

1. In `AppApiKeyProps` interface:
   - `readonly bifrostVirtualKeyId: string` → `readonly gatewayKeyId: string`
   - `readonly previousBifrostVirtualKeyId: string | null` → `readonly previousGatewayKeyId: string | null`

2. In `CreateAppApiKeyParams` interface:
   - `bifrostVirtualKeyId: string` → `gatewayKeyId: string`

3. In `static async create(params: CreateAppApiKeyParams)`:
   - `bifrostVirtualKeyId: params.bifrostVirtualKeyId,` → `gatewayKeyId: params.gatewayKeyId,`
   - `previousBifrostVirtualKeyId: null,` → `previousGatewayKeyId: null,`

4. In `static fromDatabase(row: Record<string, unknown>)`:
   - `bifrostVirtualKeyId: row.bifrost_virtual_key_id as string,` → `gatewayKeyId: row.bifrost_virtual_key_id as string,`
   - `previousBifrostVirtualKeyId: (row.previous_bifrost_virtual_key_id as string) ?? null,` → `previousGatewayKeyId: (row.previous_bifrost_virtual_key_id as string) ?? null,`
   (Both DB column names stay unchanged — per D-P03-02)

5. In `async rotate(newRawKey: string, newBifrostVirtualKeyId: string)` — rename parameter and all usages:
   - Signature: `async rotate(newRawKey: string, newGatewayKeyId: string): Promise<AppApiKey>`
   - `bifrostVirtualKeyId: newBifrostVirtualKeyId,` → `gatewayKeyId: newGatewayKeyId,`
   - `previousBifrostVirtualKeyId: this.props.bifrostVirtualKeyId,` → `previousGatewayKeyId: this.props.gatewayKeyId,`

6. In `completeRotation()`:
   - `previousBifrostVirtualKeyId: null,` → `previousGatewayKeyId: null,`

7. Getter renames:
   - `get bifrostVirtualKeyId(): string { return this.props.bifrostVirtualKeyId }` →
     `get gatewayKeyId(): string { return this.props.gatewayKeyId }`
   - `get previousBifrostVirtualKeyId(): string | null { return this.props.previousBifrostVirtualKeyId }` →
     `get previousGatewayKeyId(): string | null { return this.props.previousGatewayKeyId }`

8. In `toDatabaseRow()`:
   - `bifrost_virtual_key_id: this.props.bifrostVirtualKeyId,` → `bifrost_virtual_key_id: this.props.gatewayKeyId,`
   - `previous_bifrost_virtual_key_id: this.props.previousBifrostVirtualKeyId,` → `previous_bifrost_virtual_key_id: this.props.previousGatewayKeyId,`
   (Both DB column keys stay frozen — per D-P03-02)

9. In `toDTO()`:
   - `bifrostVirtualKeyId: this.props.bifrostVirtualKeyId,` → `gatewayKeyId: this.props.gatewayKeyId,`
  </action>
  <verify>
    <automated>grep -n "bifrostVirtualKeyId\|previousBifrostVirtualKeyId" /Users/carl/Dev/CMG/Draupnir/src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts && echo "FOUND — fix required" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bifrostVirtualKeyId\|previousBifrostVirtualKeyId" src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts` returns zero matches
    - `grep "gatewayKeyId" src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts` returns matches in: props interface, create param, create body, fromDatabase, rotate param, rotate body, completeRotation, getter, toDatabaseRow, toDTO
    - `grep "previousGatewayKeyId" src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts` returns matches in: props interface, create body, fromDatabase, rotate body, completeRotation, getter, toDatabaseRow
    - `grep "bifrost_virtual_key_id\|previous_bifrost_virtual_key_id" src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts` returns exactly 4 matches (fromDatabase x2, toDatabaseRow x2 — column names preserved)
  </acceptance_criteria>
  <done>AppApiKey.ts exposes gatewayKeyId and previousGatewayKeyId everywhere in TypeScript while mapping to/from frozen DB column names.</done>
</task>

<task type="auto">
  <name>Task 3: Verify repositories still compile and run tests</name>
  <read_first>
    src/Modules/ApiKey/Infrastructure/Repositories/ApiKeyRepository.ts
    src/Modules/AppApiKey/Infrastructure/Repositories/AppApiKeyRepository.ts
  </read_first>
  <files>
    src/Modules/ApiKey/Infrastructure/Repositories/ApiKeyRepository.ts
    src/Modules/AppApiKey/Infrastructure/Repositories/AppApiKeyRepository.ts
  </files>
  <action>
Both repositories delegate row-mapping entirely to the aggregate's `fromDatabase()` and `toDatabaseRow()` methods — they do NOT reference `bifrostVirtualKeyId` directly. After Tasks 1 and 2 the repositories require no code changes.

Confirm this by reading each file and checking that neither contains `bifrostVirtualKeyId` or `previousBifrostVirtualKeyId`. If either file does contain such a reference (e.g., a direct row field access that bypasses the aggregate), remove it and replace with the equivalent aggregate accessor call.

Then run:
  cd /Users/carl/Dev/CMG/Draupnir && bun run typecheck
  cd /Users/carl/Dev/CMG/Draupnir && bun test

Both commands must exit 0. If typecheck fails, read the error output carefully — it will point to a remaining `bifrostVirtualKeyId` reference missed in Tasks 1 or 2, or a call site in the repos. Fix the specific line only.
  </action>
  <verify>
    <automated>cd /Users/carl/Dev/CMG/Draupnir && bun run typecheck 2>&1 | tail -5 && bun test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` exits 0 with no errors mentioning `bifrostVirtualKeyId` or `gatewayKeyId`
    - `bun test` exits 0 (all tests pass)
    - `grep "bifrostVirtualKeyId" src/Modules/ApiKey/Infrastructure/Repositories/ApiKeyRepository.ts` returns zero matches
    - `grep "bifrostVirtualKeyId" src/Modules/AppApiKey/Infrastructure/Repositories/AppApiKeyRepository.ts` returns zero matches
  </acceptance_criteria>
  <done>Repositories compile clean; full test suite passes with renamed aggregate fields.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
cd /Users/carl/Dev/CMG/Draupnir

# Aggregates must be clean
grep "bifrostVirtualKeyId\|previousBifrostVirtualKeyId" \
  src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts \
  src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts
# Expected: no output (zero matches)

# DB column names must be preserved
grep "bifrost_virtual_key_id" \
  src/Modules/ApiKey/Domain/Aggregates/ApiKey.ts \
  src/Modules/AppApiKey/Domain/Aggregates/AppApiKey.ts
# Expected: 4 lines total (fromDatabase + toDatabaseRow in each file)

bun run typecheck   # must exit 0
bun test            # must exit 0
```
</verification>

<success_criteria>
- ApiKey aggregate: `bifrostVirtualKeyId` → `gatewayKeyId` in props, params, fromDatabase, getter, toDatabaseRow, toDTO
- AppApiKey aggregate: `bifrostVirtualKeyId` → `gatewayKeyId`, `previousBifrostVirtualKeyId` → `previousGatewayKeyId` in all locations
- DB column keys `bifrost_virtual_key_id` and `previous_bifrost_virtual_key_id` unchanged in toDatabaseRow() and fromDatabase()
- `rotate()` method signature updated to `newGatewayKeyId: string`
- Both repositories require zero code changes
- `bun run typecheck` exits 0
- `bun test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-domain-rename/03-01-SUMMARY.md`
</output>
